# Requirements

Active milestone: `v2.7 Inspection Replay Diagnostics Closure`

Inspection-replay-diagnostics requirement numbering starts at `IRD-01`.

- [x] `IRD-01` Before implementation starts, lock `v2.7` to inspection bundle/replay/support-diagnostics coherence and freeze explicit non-goals around new bundle/archive formats, compatibility/fallback replay, partial restore, `VideraView` API widening, renderer/material/backend breadth, and editor-style project persistence.
- [x] `IRD-02` `VideraInspectionBundleService` remains the single replay truth: export results expose `CanReplayScene` and `ReplayLimitation`, callers do not infer replayability from bundle internals, and non-replayable imports fail before mutating the target view.
- [x] `IRD-03` Diagnostics snapshots, samples, support docs, and localized docs use one shared meaning for `VideraDiagnosticsSnapshotFormatter`, `TransparentFeatureStatus`, last-frame object counts, and inspection-bundle guidance.
- [x] `IRD-04` Repository tests and targeted verification suites fail on drift in inspection replay/support truth without expanding into broad visual-rendering or editor-project coverage.

## Out of Scope

| Item | Excluded from `v2.7` |
|------|------------------------|
| New bundle/archive format | Not part of this milestone. |
| Compatibility replay, fallback replay, partial restore, or alternate import paths | Not part of this milestone. |
| `VideraView` public API widening | Not part of this milestone. |
| Renderer, material, glTF, backend, or platform breadth | Not part of this milestone. |
| Editor-style project/workspace persistence | Not part of this milestone. |
| New chart families or `SurfaceCharts` widening | Not part of this milestone. |

## Traceability

| Requirement | Phase |
|-------------|-------|
| `IRD-01` | Phase 160 |
| `IRD-02` | Phase 161 |
| `IRD-03` | Phase 162 |
| `IRD-04` | Phase 163 |
