# Phase 249 Plan: Interaction Quality Refine Policy

## Goal

Expose deterministic interaction/refine truth on the scatter proof path using the existing SurfaceCharts interaction-quality contract.

## Tasks

1. Extend `ScatterChartView` with read-only `InteractionQuality` and `InteractionQualityChanged`.
2. Publish `InteractionQuality` through `ScatterChartRenderingStatus`.
3. Update demo status, diagnostics, and support-summary text to use the new scatter state.
4. Add focused integration and demo contract tests.

## Verification

- `dotnet test tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/Videra.SurfaceCharts.Avalonia.IntegrationTests.csproj -c Release --filter FullyQualifiedName~ScatterChartViewIntegrationTests`
- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~SurfaceChartsDemoConfigurationTests|FullyQualifiedName~SurfaceChartsDemoViewportBehaviorTests"`

## Non-Goals

- No renderer rewrite.
- No broad scheduling abstraction.
- No `VideraView` contract changes.
- No compatibility layer for the previous "not exposed" demo wording.
