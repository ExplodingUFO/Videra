# Phase 247 Plan

## Goal

Harden `ScatterColumnarSeries` append/FIFO behavior and validation.

## Tasks

1. Add minimal streaming/FIFO counters to `ScatterColumnarSeries`.
2. Track dropped points for append and replace paths.
3. Enforce `IsSortedX` for append streams while letting `ReplaceRange` reset sorted state.
4. Add focused unit tests for FIFO trimming, counters, and sorted validation.
5. Run the focused SurfaceCharts core test slice.

## Verification

- `dotnet test tests/Videra.SurfaceCharts.Core.Tests/Videra.SurfaceCharts.Core.Tests.csproj -c Release --filter FullyQualifiedName~ScatterRendererTests`
