---
phase: 16-rendering-host-seam-and-gpu-main-path
plan: 03
type: execute
wave: 3
depends_on:
  - "16-01"
  - "16-02"
files_modified:
  - src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderState.cs
  - src/Videra.SurfaceCharts.Rendering/SurfaceChartResidentTile.cs
  - src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderChangeSet.cs
  - src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderHost.cs
  - src/Videra.SurfaceCharts.Rendering/Software/SurfaceChartSoftwareRenderBackend.cs
  - src/Videra.SurfaceCharts.Avalonia/README.md
  - samples/Videra.SurfaceCharts.Demo/README.md
  - tests/Videra.SurfaceCharts.Core.Tests/Rendering/SurfaceChartRenderStateTests.cs
  - tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/SurfaceChartIncrementalRenderingTests.cs
  - tests/Videra.Core.Tests/Repository/SurfaceChartsRepositoryArchitectureTests.cs
autonomous: true
requirements:
  - REND-02
must_haves:
  truths:
    - "Viewport / projection changes stop forcing a full `SurfaceRenderScene` rebuild for unchanged resident tiles."
    - "Tile residency, color-map changes, and full source reset have explicit, separate dirty paths."
    - "README/demo/guards state the renderer seam, GPU-first path, software fallback, and any native-host limits truthfully."
  artifacts:
    - path: "src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderState.cs"
      provides: "Resident render-state model keyed by tile"
      contains: "class SurfaceChartRenderState"
    - path: "src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderChangeSet.cs"
      provides: "Explicit delta model for tile/color/projection/full-reset updates"
      contains: "class SurfaceChartRenderChangeSet"
    - path: "tests/Videra.Core.Tests/Repository/SurfaceChartsRepositoryArchitectureTests.cs"
      provides: "Guards for renderer truth and sibling-boundary preservation"
      contains: "SurfaceChartRenderHost"
  key_links:
    - from: "src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderHost.cs"
      to: "src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderState.cs"
      via: "render host applies tile/color/projection deltas instead of full-scene rebuild on every view change"
      pattern: "SurfaceChartRenderState"
    - from: "src/Videra.SurfaceCharts.Avalonia/README.md"
      to: "tests/Videra.Core.Tests/Repository/SurfaceChartsRepositoryArchitectureTests.cs"
      via: "repository guards freeze the shipped renderer/fallback truth"
      pattern: "GPU-first"
---

<objective>
完成 Phase 16 的第二层闭环：把 renderer state 从 full-snapshot 改到 explicit delta/residency path，并把 README / demo / repository guards 同步到新的 renderer truth。

这里的目标不是 Phase 17 级别的大数据 scheduler 重写，而是把 renderer 自身的 dirty boundaries 锁住。
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-rendering-host-seam-and-gpu-main-path/16-CONTEXT.md
@.planning/phases/16-rendering-host-seam-and-gpu-main-path/16-RESEARCH.md
@.planning/phases/16-rendering-host-seam-and-gpu-main-path/16-01-PLAN.md
@.planning/phases/16-rendering-host-seam-and-gpu-main-path/16-02-PLAN.md
</execution_context>

<guardrails>
- 不在本 plan 中推进 Phase 17 的 scheduler cancellation / prioritization / batch-I/O / Rust hotspot 工作。
- 增量路径必须围绕 renderer state 自身展开，而不是把问题重新丢回 `SurfaceTileScheduler`。
- README/demo 只更新 renderer/fallback/host-limit 真相，不夸大未实现的 interaction/runtime 能力。
</guardrails>

<tasks>

<task id="16-03-01" type="auto" tdd="true">
  <name>先补 incremental render-state 的 failing tests</name>
  <read_first>
    - src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderHost.cs
    - src/Videra.SurfaceCharts.Rendering/Software/SurfaceChartSoftwareRenderBackend.cs
    - src/Videra.SurfaceCharts.Core/Rendering/SurfaceRenderer.cs
    - tests/Videra.SurfaceCharts.Core.Tests/Rendering/SurfaceRendererInputTests.cs
    - tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/SurfaceScenePainterTests.cs
  </read_first>
  <files>
    tests/Videra.SurfaceCharts.Core.Tests/Rendering/SurfaceChartRenderStateTests.cs
    tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/SurfaceChartIncrementalRenderingTests.cs
  </files>
  <action>
    1. 新增 `SurfaceChartRenderStateTests.cs`，固定 renderer dirty boundary：
       - 纯 viewport / projection change 不会重建 unchanged resident tile geometry
       - `ColorMap` change 会更新颜色，但不会改变 sample-position mapping
       - tile add/remove 只处理 changed keys
       - source / metadata replacement 触发 full reset
    2. 新增 `SurfaceChartIncrementalRenderingTests.cs`，通过 counting fake backend 或 counting software backend 固定：
       - repeated viewport changes 不再导致 full resident scene rebuild 次数随交互线性增长
       - detail tile 替换 overview tile 时只更新相关 resident keys
       - fallback path 与 GPU path 都读取同一套 render-state dirty truth
    3. 在实现前，新测试必须因为缺少 `SurfaceChartRenderState` / `SurfaceChartRenderChangeSet` / resident-tile delta path 而失败。
  </action>
  <acceptance_criteria>
    - `SurfaceChartRenderStateTests.cs` 包含 `ColorMap`、`Viewport`、`FullReset`
    - `SurfaceChartIncrementalRenderingTests.cs` 包含 `resident` 或 `delta` 相关测试名
    - `dotnet test tests/Videra.SurfaceCharts.Core.Tests/Videra.SurfaceCharts.Core.Tests.csproj -c Release --filter "FullyQualifiedName~SurfaceChartRenderStateTests"` 在实现前失败，失败原因是缺少增量 render-state seam
  </acceptance_criteria>
</task>

<task id="16-03-02" type="auto" tdd="true">
  <name>实现 resident render-state / change-set，并让 renderer 按 dirty boundary 增量更新</name>
  <read_first>
    - src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderHost.cs
    - src/Videra.SurfaceCharts.Rendering/Software/SurfaceChartSoftwareRenderBackend.cs
    - src/Videra.SurfaceCharts.Core/Rendering/SurfaceRenderer.cs
    - tests/Videra.SurfaceCharts.Core.Tests/Rendering/SurfaceChartRenderStateTests.cs
    - tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/SurfaceChartIncrementalRenderingTests.cs
  </read_first>
  <files>
    src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderState.cs
    src/Videra.SurfaceCharts.Rendering/SurfaceChartResidentTile.cs
    src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderChangeSet.cs
    src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderHost.cs
    src/Videra.SurfaceCharts.Rendering/Software/SurfaceChartSoftwareRenderBackend.cs
    tests/Videra.SurfaceCharts.Core.Tests/Rendering/SurfaceChartRenderStateTests.cs
    tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/SurfaceChartIncrementalRenderingTests.cs
  </files>
  <action>
    1. 新增 `SurfaceChartRenderState.cs`，用 `SurfaceTileKey` 作为主键维护 resident render tiles；resident entry 至少显式区分：
       - geometry/sample-position truth
       - color truth
       - current residency membership
    2. 新增 `SurfaceChartResidentTile.cs` 与 `SurfaceChartRenderChangeSet.cs`，把下列 dirty bucket 建模成显式 change set：
       - `FullResetRequired`
       - `ResidencyDirty`
       - `ColorDirty`
       - `ProjectionDirty`
    3. 在 `SurfaceChartRenderHost.cs` 中改写 update path：
       - source / metadata replacement -> full reset
       - loaded tile key set change -> 仅 add/remove changed resident tiles
       - `ColorMap` change -> 仅更新颜色，不重建 sample positions
       - viewport / projection / view-size change -> 仅更新 projection-dirty path，不重建 resident geometry
    4. 在 `SurfaceChartSoftwareRenderBackend.cs` 中取消“每次都从全部 loaded tiles 重建整份 `SurfaceRenderScene`”的策略；改为从 `SurfaceChartRenderState` 读取 resident tiles，仅在 delta 需要时重建受影响的 render tiles。
    5. 若 GPU backend 需要单独的 upload/update path，必须共享同一份 `SurfaceChartRenderChangeSet` truth，而不是再定义第二套 dirty 语义。
  </action>
  <acceptance_criteria>
    - `rg -n "class SurfaceChartRenderState|class SurfaceChartResidentTile|class SurfaceChartRenderChangeSet" src/Videra.SurfaceCharts.Rendering` 找到全部三个类型
    - `rg -n "FullResetRequired|ResidencyDirty|ColorDirty|ProjectionDirty" src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderChangeSet.cs src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderHost.cs` 找到四个 dirty bucket
    - `rg -n "BuildScene\\(" src/Videra.SurfaceCharts.Rendering/Software/SurfaceChartSoftwareRenderBackend.cs` 不能表现为“每次 update 都对全部 loaded tiles 直接全量重建”的单一路径
    - `dotnet test tests/Videra.SurfaceCharts.Core.Tests/Videra.SurfaceCharts.Core.Tests.csproj -c Release --filter "FullyQualifiedName~SurfaceChartRenderStateTests|FullyQualifiedName~SurfaceRendererInputTests"` 通过
    - `dotnet test tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/Videra.SurfaceCharts.Avalonia.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~SurfaceChartIncrementalRenderingTests|FullyQualifiedName~SurfaceChartViewGpuFallbackTests"` 通过
  </acceptance_criteria>
</task>

<task id="16-03-03" type="auto" tdd="true">
  <name>更新 README/demo truth，并用 repository guards 固定 renderer/fallback 边界</name>
  <read_first>
    - src/Videra.SurfaceCharts.Avalonia/README.md
    - samples/Videra.SurfaceCharts.Demo/README.md
    - tests/Videra.Core.Tests/Repository/SurfaceChartsRepositoryArchitectureTests.cs
    - .planning/phases/16-rendering-host-seam-and-gpu-main-path/16-CONTEXT.md
  </read_first>
  <files>
    src/Videra.SurfaceCharts.Avalonia/README.md
    samples/Videra.SurfaceCharts.Demo/README.md
    tests/Videra.Core.Tests/Repository/SurfaceChartsRepositoryArchitectureTests.cs
  </files>
  <action>
    1. 更新 `src/Videra.SurfaceCharts.Avalonia/README.md`，明确写出这些已发货 truth：
       - `SurfaceChartView` 通过 chart-local renderer seam 工作，而不是 `VideraView` mode
       - renderer 是 GPU-first，并保留 software fallback
       - host/app 可以检查 active backend、fallback 状态与 fallback reason
       - 某些 native-host GPU 路径若存在 overlay/host limit，必须写成具体限制语句，而不是泛泛地说“可能受限”
    2. 更新 `samples/Videra.SurfaceCharts.Demo/README.md`，同步 demo 当前对 renderer/fallback 的真实口径；如果 demo 仍未覆盖完整 GPU host 场景，要明确写出“what the demo shows today / does not show yet”。
    3. 扩展 `SurfaceChartsRepositoryArchitectureTests.cs`，要求：
       - `VideraView.cs` / `VideraView.Overlay.cs` 中仍不出现 `SurfaceChartRenderHost`、`SurfaceChartGpuRenderBackend`、`Videra.SurfaceCharts.Rendering`
       - `src/Videra.SurfaceCharts.Avalonia/README.md` 与 `samples/Videra.SurfaceCharts.Demo/README.md` 都包含 `GPU-first` 与 `software fallback`
       - README 中仍明确保留 surface-chart 与 `VideraView` 的 sibling boundary
    4. 如果执行阶段发现 GPU host 限制的真实文案与当前计划假设不同，必须把 README 和 repository guards 一起更新到“具体字符串一致”，不能让测试继续接受模糊表述。
  </action>
  <acceptance_criteria>
    - `rg -n "GPU-first|software fallback" src/Videra.SurfaceCharts.Avalonia/README.md samples/Videra.SurfaceCharts.Demo/README.md` 找到两份文档的 renderer truth
    - `rg -n "SurfaceChartRenderHost|SurfaceChartGpuRenderBackend|Videra\\.SurfaceCharts\\.Rendering" tests/Videra.Core.Tests/Repository/SurfaceChartsRepositoryArchitectureTests.cs` 找到新的 boundary guards
    - `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~SurfaceChartsRepositoryArchitectureTests"` 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test tests/Videra.SurfaceCharts.Core.Tests/Videra.SurfaceCharts.Core.Tests.csproj -c Release --filter "FullyQualifiedName~SurfaceChartRenderStateTests|FullyQualifiedName~SurfaceChartGpuFallbackTests|FullyQualifiedName~SurfaceRendererInputTests"`
- `dotnet test tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/Videra.SurfaceCharts.Avalonia.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~SurfaceChartIncrementalRenderingTests|FullyQualifiedName~SurfaceChartViewGpuFallbackTests|FullyQualifiedName~SurfaceChartRenderHostIntegrationTests"`
- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~SurfaceChartsRepositoryArchitectureTests"`
</verification>

<success_criteria>
1. renderer state 对 viewport/projection、tile residency、color-map、full reset 有明确而可验证的 dirty boundary。
2. Phase 16 的 renderer/fallback/host-limit truth 已被 README、demo 和 repo guards 同步固定。
3. Phase 16 交付没有提前吞进 Phase 17 的 scheduler / cache / Rust hotspot 工作。
</success_criteria>

<output>
After completion, create `.planning/phases/16-rendering-host-seam-and-gpu-main-path/16-03-SUMMARY.md`
</output>
