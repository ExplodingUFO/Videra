# 113-01 Plan

## Goal

Retain static glTF tangent input on the importer/runtime path and reuse one retained imported asset for repeated unchanged static-scene imports, without widening into renderer redesign or new backend APIs.

## Tasks

1. Add one direct tangent-bearing field to the mesh/runtime path:
   - extend `MeshData`
   - extend `MeshPayload`
   - preserve tangent data through `ImportedSceneAssetPayloadBuilder`
2. Update the glTF importer to read `TANGENT` and retain it on imported mesh/runtime data:
   - validate tangent count against `POSITION`
   - keep tangent XYZ direction plus handedness
3. Make repeated unchanged static-scene imports resolve to one retained imported asset on the shipped runtime path:
   - reuse the same `ImportedSceneAsset` for repeated identical imports
   - ensure repeated deferred objects still remain separate scene objects while sharing retained asset truth
4. Add focused tests proving:
   - imported glTF tangent data survives importer/runtime retention
   - repeated imports of the same unchanged asset reuse the same retained `ImportedSceneAsset`
   - existing upload/runtime paths remain green

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --no-restore -m:1 --filter "FullyQualifiedName~ModelImporterTests|FullyQualifiedName~Object3DTests"`
- `dotnet test tests/Videra.Avalonia.Tests/Videra.Avalonia.Tests.csproj -c Release --no-restore -m:1 --filter "FullyQualifiedName~SceneImportServiceTests|FullyQualifiedName~SceneUploadQueueTests|FullyQualifiedName~SceneResidencyRegistryTests|FullyQualifiedName~RuntimeFramePreludeTests"`
- `git diff --check`
