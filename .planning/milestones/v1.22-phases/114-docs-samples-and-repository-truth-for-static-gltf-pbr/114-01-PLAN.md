# Phase 114 Plan: Docs, Samples, and Repository Truth for Static glTF/PBR

## Success criteria

1. Top-level docs and package/readme guidance explicitly describe the shipped static glTF/PBR baseline on the viewer/runtime path.
2. Demo/sample truth teaches the same baseline in viewer-first language without leaking backend-specific types.
3. Repository guards fail if those docs/samples drift away from the shipped baseline or its deliberate exclusions.

## Execution steps

### 114-01 Docs baseline

- Update `README.md`, `ARCHITECTURE.md`, `docs/package-matrix.md`, `docs/hosting-boundary.md`, `src/Videra.Core/README.md`, and `src/Videra.Avalonia/README.md`.
- Describe:
  - static glTF/PBR as the shipped viewer/runtime baseline
  - metallic-roughness, alpha, emissive, normal-map-ready, and tangent-aware inputs
  - retained imported-scene reuse for repeated unchanged imports
  - deliberate exclusions such as animation/skeletons/morph targets

### 114-02 Demo/sample truth

- Update `samples/Videra.Demo/README.md`.
- Update `samples/Videra.Demo/Views/MainWindow.axaml` Scene Pipeline Lab copy so it matches the docs.

### 114-03 Repository guards

- Update repository/sample truth tests in `tests/Videra.Core.Tests`.
- Keep assertions narrow and vocabulary-focused so the guards are stable without becoming brittle prose snapshots.

## Verification

- Focused `Videra.Core.Tests` repository/sample truth tests.
- `dotnet build samples/Videra.Demo/Videra.Demo.csproj -c Release`.
- `git diff --check`.
