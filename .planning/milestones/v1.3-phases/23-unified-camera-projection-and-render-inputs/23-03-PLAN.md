---
phase: 23-unified-camera-projection-and-render-inputs
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceChartProjection.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceAxisOverlayPresenter.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/SurfaceChartView.Overlay.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/SurfaceScenePainter.cs
  - tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/SurfaceAxisOverlayTests.cs
autonomous: true
requirements:
  - CAM-02
---

# Phase 23-03: Overlay Projection Migration

## Objective

Move software projection and axis overlay geometry onto the shared camera-frame math so later picking and LOD work can reuse the same project/unproject pipeline.

## Tasks

1. Add failing overlay tests that verify camera-driven edge switching still works through the new projection path.
2. Refactor `SurfaceChartProjection` into a camera-frame-backed wrapper instead of a separate math system.
3. Update `SurfaceScenePainter` and `SurfaceAxisOverlayPresenter` to use the shared frame/projection path.
4. Keep current labels, legend range behavior, and render-host ownership intact.

## Acceptance Criteria

- Overlay integration tests fail first, then pass.
- `SurfaceAxisOverlayPresenter` and software painting share camera-frame projection instead of standalone yaw/pitch math.
- Public control behavior remains stable enough for the existing view-state and interaction tests to stay green.
