# Phase 275 Plan: Worktree Coordination Contract

## Goal

Document and validate the redirect pattern that lets parallel phase worktrees share one Beads issue truth while keeping branch isolation.

## Tasks

1. Add a worktree coordination section to `docs/beads-coordination.md`.
2. Include `bd worktree list`, main/shared, worktree/redirect, and representative `bd context --json` validation guidance.
3. Document that Beads coordinates issues but does not own Git merge/conflict/release decisions.
4. Verify against the existing `.worktrees/phase231` redirect context.

## Verification

- `bd worktree list`
- `bd context --json` from `.worktrees/phase231`

