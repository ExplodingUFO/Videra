# Phase 274 Plan: Beads Service Contract and Agent Onboarding

## Goal

Lock the repository-facing Beads service contract and agent command guidance around the Docker-backed `Videra` Dolt database.

## Tasks

1. Add `docs/beads-coordination.md` with the Docker/Dolt service contract, project id, command workflow, export guidance, and non-goals.
2. Link the new document from `docs/index.md`.
3. Add short pointers in `AGENTS.md` and `CLAUDE.md` so agents start from the same Beads service truth.
4. Verify `bd context --json` resolves to the expected Docker-backed database.

## Verification

- `bd context --json`
- `rg` checks for service contract, project id, onboarding commands, and `AgentDialog` warning.

