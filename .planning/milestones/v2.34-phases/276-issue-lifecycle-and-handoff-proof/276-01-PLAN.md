# Phase 276 Plan: Issue Lifecycle and Handoff Proof

## Goal

Prove the multi-agent issue lifecycle from create/claim/discover/close through Docker-backed observation and session handoff guidance.

## Tasks

1. Create and claim a Beads phase issue.
2. Create a discovered follow-up issue with `--deps discovered-from:<parent-id>`.
3. Close the proof follow-up with a concrete reason.
4. Observe the issue and dependency rows from the Docker-backed `Videra` database.
5. Record proof in `eng/beads-lifecycle-proof.json`.
6. Extend `docs/beads-coordination.md` with lifecycle and handoff guidance.

## Verification

- `docker exec dolt-sql-server dolt sql -q "select issue_id,depends_on_id,type from Videra.dependencies where issue_id='Videra-4yl';"`
- JSON parse of `eng/beads-lifecycle-proof.json`
- `rg` checks for lifecycle/handoff guidance

