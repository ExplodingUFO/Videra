# Phase 66 Plan 01

## Goal

Create one repository script that runs the viewer and surface-chart benchmark suites consistently.

## Steps

1. Add a benchmark runner script that selects viewer or surface-chart suites.
2. Build the selected benchmark project and run BenchmarkDotNet with artifact exporters.
3. Produce a short summary alongside the raw benchmark artifacts.
