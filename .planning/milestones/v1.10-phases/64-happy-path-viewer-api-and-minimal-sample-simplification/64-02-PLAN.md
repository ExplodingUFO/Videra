# Phase 64 Plan 02

## Goal

Rewrite the default public onboarding docs so the happy path is the first story a new alpha consumer sees.

## Steps

1. Update the root README to route first-scene setup through the minimal sample.
2. Update `src/Videra.Avalonia/README.md` so the main code sample uses only the happy path.
3. Move advanced extensibility language behind explicit opt-in sections and links.
