# Phase 65 Plan 01

## Goal

Create a consumer smoke app that proves the public package surface from install to first scene.

## Steps

1. Add a dedicated smoke app that uses `PackageReference` only.
2. Load a known small model, frame it, reset the camera, and capture diagnostics.
3. Write a machine-readable smoke report for workflow consumption.
