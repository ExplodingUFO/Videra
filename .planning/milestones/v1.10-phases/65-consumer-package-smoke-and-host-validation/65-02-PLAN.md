# Phase 65 Plan 02

## Goal

Turn consumer smoke into a repeatable host-validation entrypoint.

## Steps

1. Add a script that packs current public packages and restores the smoke app from a dedicated local package source.
2. Add a workflow that runs smoke validation on supported host targets.
3. Publish smoke artifacts so package-consumer regressions remain inspectable.
