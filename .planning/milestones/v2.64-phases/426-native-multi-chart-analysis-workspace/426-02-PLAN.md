---
phase: 426-native-multi-chart-analysis-workspace
plan: 02
type: execute
wave: 2
depends_on:
  - 426-01
files_modified:
  - src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartLinkGroup.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceStatus.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceEvidence.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspace.cs
  - tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/Workspace/SurfaceChartLinkGroupTests.cs
  - samples/Videra.SurfaceCharts.Demo/Services/SurfaceChartWorkspaceService.cs
  - samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoScenario.cs
  - samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml
  - samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03

must_haves:
  truths:
    - "SurfaceChartLinkGroup links N charts via pairwise ViewState synchronization with a re-entrancy guard"
    - "SurfaceChartWorkspaceStatus reports chart count, active chart id, per-chart status panels, link group count, and all-ready flag"
    - "SurfaceChartWorkspaceEvidence.Create returns a bounded text block with workspace chart info, link groups, dataset scale, and timestamp"
    - "Demo AnalysisWorkspace scenario shows 4 charts in a grid with workspace status and copy-evidence button"
    - "Demo workspace logic lives in SurfaceChartWorkspaceService, not in MainWindow code-behind"
  artifacts:
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartLinkGroup.cs"
      provides: "N-chart link group with pairwise ViewState sync and dispose cleanup"
      contains: "sealed class SurfaceChartLinkGroup"
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceStatus.cs"
      provides: "Aggregate status snapshot record and per-panel status record"
      contains: "sealed record SurfaceChartWorkspaceStatus"
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceEvidence.cs"
      provides: "Bounded text evidence formatter for workspace state"
      contains: "static class SurfaceChartWorkspaceEvidence"
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspace.cs"
      provides: "Workspace with CaptureWorkspaceStatus and CreateWorkspaceEvidence fully implemented"
      contains: "CaptureWorkspaceStatus"
    - path: "tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/Workspace/SurfaceChartLinkGroupTests.cs"
      provides: "Contract tests for link group lifecycle, policy, and disposal"
      contains: "class SurfaceChartLinkGroupTests"
    - path: "samples/Videra.SurfaceCharts.Demo/Services/SurfaceChartWorkspaceService.cs"
      provides: "Demo-owned workspace state helper that wires chart views to workspace"
      contains: "class SurfaceChartWorkspaceService"
    - path: "samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoScenario.cs"
      provides: "AnalysisWorkspace scenario id added to scenario catalog"
      contains: "AnalysisWorkspaceId"
    - path: "samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml"
      provides: "AnalysisWorkspace grid layout with 4 charts, workspace toolbar, and copy-evidence button"
      contains: "AnalysisWorkspacePanel"
    - path: "samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs"
      provides: "MainWindow wires AnalysisWorkspace scenario using SurfaceChartWorkspaceService"
      contains: "SurfaceChartWorkspaceService"
  key_links:
    - from: "SurfaceChartLinkGroup.cs"
      to: "VideraChartView.LinkViewWith"
      via: "pairwise links created in Add()"
      pattern: "LinkViewWith"
    - from: "SurfaceChartWorkspaceEvidence.cs"
      to: "SurfaceChartWorkspaceStatus"
      via: "evidence formatter reads status snapshot"
      pattern: "SurfaceChartWorkspaceStatus"
    - from: "SurfaceChartWorkspaceService.cs"
      to: "SurfaceChartWorkspace"
      via: "service creates and manages workspace instance"
      pattern: "new SurfaceChartWorkspace"
    - from: "MainWindow.axaml.cs"
      to: "SurfaceChartWorkspaceService"
      via: "MainWindow uses service for workspace operations"
      pattern: "SurfaceChartWorkspaceService"
---

<objective>
Implement the link group, aggregate status, and evidence layer on top of the workspace contracts from Plan 01, then wire the AnalysisWorkspace demo scenario.

Purpose: Complete Phase 426 by delivering the full multi-chart analysis workspace with link groups, status snapshots, workspace evidence, and a bounded demo scenario that separates layout, catalog, and support summary responsibilities.
Output: Five library/test files (link group, status, evidence, link group tests, workspace impl update) and four demo files (service, scenario update, AXAML, code-behind).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/426-native-multi-chart-analysis-workspace/426-CONTEXT.md
@.planning/phases/426-native-multi-chart-analysis-workspace/426-RESEARCH.md
@.planning/phases/426-native-multi-chart-analysis-workspace/426-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — SurfaceChartPanelInfo and SurfaceChartWorkspace -->

From src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartPanelInfo.cs:
```csharp
namespace Videra.SurfaceCharts.Avalonia.Controls.Workspace;
public sealed record SurfaceChartPanelInfo(
    string ChartId,
    string Label,
    Plot3DSeriesKind ChartKind,
    string? RecipeContext = null);
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspace.cs:
```csharp
namespace Videra.SurfaceCharts.Avalonia.Controls.Workspace;
public sealed class SurfaceChartWorkspace : IDisposable
{
    public void Register(VideraChartView chart, SurfaceChartPanelInfo info);
    public void Unregister(string chartId);
    public void SetActiveChart(string chartId);
    public IReadOnlyList<(VideraChartView Chart, SurfaceChartPanelInfo Info)> GetRegisteredCharts();
    public string? GetActiveChartId();
    public SurfaceChartPanelInfo GetPanelInfo(VideraChartView chart);
    // CaptureWorkspaceStatus() and CreateWorkspaceEvidence() are NOT YET IMPLEMENTED — this plan implements them
    public void Dispose();
}
```

From src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.LinkedViews.cs:
```csharp
public IDisposable LinkViewWith(VideraChartView peer);
public sealed class VideraChartViewLink : IDisposable
{
    private readonly VideraChartView _first;
    private readonly VideraChartView _second;
    private bool _isSynchronizing;
    // CopyViewState has re-entrancy guard via _isSynchronizing flag
}
```

From src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs:
```csharp
public SurfaceChartRenderingStatus RenderingStatus { get; private set; }
public ScatterChartRenderingStatus ScatterRenderingStatus { get; private set; }
public BarChartRenderingStatus BarRenderingStatus { get; private set; }
public ContourChartRenderingStatus ContourRenderingStatus { get; private set; }
public Plot3D Plot { get; }
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs:
```csharp
public Plot3DDatasetEvidence CreateDatasetEvidence();
public Plot3DOutputEvidence CreateOutputEvidence(
    SurfaceChartRenderingStatus surface,
    ScatterChartRenderingStatus scatter,
    BarChartRenderingStatus bar,
    ContourChartRenderingStatus contour);
```

From samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoScenario.cs:
```csharp
internal static class SurfaceDemoScenarios
{
    public const string StartId = "surface-start";
    // ... other ids ...
    public static IReadOnlyList<SurfaceDemoScenario> All { get; }
}
```

From samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoSupportSummary.cs:
```csharp
internal static class SurfaceDemoSupportSummary
{
    internal static string Create(SurfaceDemoSupportSummaryContext context);
    // Pattern: builds bounded text evidence from chart state
}
```

From samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs:
```csharp
public partial class MainWindow : Window
{
    private readonly VideraChartView _surfaceChartView;
    private readonly VideraChartView _waterfallChartView;
    private readonly VideraChartView _scatterChartView;
    private readonly VideraChartView _barChartView;
    private readonly VideraChartView _contourPlotView;
    // SetActiveChartView toggles IsVisible per chart
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create link group, aggregate status, evidence formatter, and complete workspace status/evidence</name>
  <files>
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartLinkGroup.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceStatus.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceEvidence.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspace.cs,
    tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/Workspace/SurfaceChartLinkGroupTests.cs
  </files>
  <read_first>
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspace.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartPanelInfo.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.LinkedViews.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs,
    src/Videra.SurfaceCharts.Rendering/SurfaceChartRenderingStatus.cs,
    samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoSupportSummary.cs
  </read_first>
  <behavior>
    - SurfaceChartLinkGroup: constructor accepts SurfaceChartLinkPolicy enum (FullViewState only for Phase 426; CameraOnly and AxisOnly throw NotSupportedException)
    - SurfaceChartLinkGroup.Add(chart): creates pairwise links to all existing members using chart.LinkViewWith(existing)
    - SurfaceChartLinkGroup.Remove(chart): disposes pairwise links involving that chart, removes from member list
    - SurfaceChartLinkGroup.Dispose(): disposes all pairwise links, clears member list, sets _disposed
    - SurfaceChartLinkGroup.Members: returns IReadOnlyList of member charts
    - SurfaceChartLinkGroup throws ObjectDisposedException on Add/Remove after dispose
    - SurfaceChartLinkGroup.Add throws InvalidOperationException if chart is already a member
    - SurfaceChartWorkspaceStatus: sealed record with ChartCount (int), ActiveChartId (string?), Panels (IReadOnlyList<SurfaceChartPanelStatus>), LinkGroupCount (int), AllReady (bool)
    - SurfaceChartPanelStatus: sealed record with ChartId (string), Label (string), ChartKind (Plot3DSeriesKind), IsReady (bool), SeriesCount (int), PointCount (long)
    - SurfaceChartWorkspaceEvidence.Create(SurfaceChartWorkspaceStatus status, string? activeRecipeContext): returns bounded text with "SurfaceCharts workspace evidence" header, GeneratedUtc, ActiveChart, per-chart panels, link group count, overall readiness
    - SurfaceChartWorkspace.CaptureWorkspaceStatus(): iterates registered charts, reads RenderingStatus.IsReady, Plot.CreateDatasetEvidence().Series.Count, builds SurfaceChartPanelStatus list, returns SurfaceChartWorkspaceStatus
    - SurfaceChartWorkspace.CreateWorkspaceEvidence(): calls CaptureWorkspaceStatus then SurfaceChartWorkspaceEvidence.Create
    - Link group tests: Add two charts, verify Members has count 2; Dispose unlinks all; Add duplicate throws; Add after dispose throws; Remove reduces count
  </behavior>
  <action>
    Create 4 new files and modify 1 existing file (SurfaceChartWorkspace.cs from Plan 01).

    File 1: `src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartLinkGroup.cs`
    - Namespace: `Videra.SurfaceCharts.Avalonia.Controls.Workspace`
    - Usings: `Videra.SurfaceCharts.Avalonia.Controls`
    - `public enum SurfaceChartLinkPolicy { FullViewState, CameraOnly, AxisOnly }`
    - `public sealed class SurfaceChartLinkGroup : IDisposable`
    - Private fields: `private readonly List<VideraChartView> _members = [];`, `private readonly List<IDisposable> _pairwiseLinks = [];`, `private readonly SurfaceChartLinkPolicy _policy;`, `private bool _disposed;`
    - Constructor: `public SurfaceChartLinkGroup(SurfaceChartLinkPolicy policy = SurfaceChartLinkPolicy.FullViewState)` — if policy is CameraOnly or AxisOnly, throw `NotSupportedException("Only FullViewState link policy is supported in Phase 426. CameraOnly and AxisOnly will be added in Phase 427.")`
    - `public IReadOnlyList<VideraChartView> Members => _members.AsReadOnly();`
    - `public void Add(VideraChartView chart)`:
      - `ObjectDisposedException.ThrowIf(_disposed, this);`
      - `ArgumentNullException.ThrowIfNull(chart);`
      - `if (_members.Any(m => ReferenceEquals(m, chart))) throw new InvalidOperationException("Chart is already a member of this link group.");`
      - `foreach (var existing in _members) { _pairwiseLinks.Add(existing.LinkViewWith(chart)); }`
      - `_members.Add(chart);`
    - `public void Remove(VideraChartView chart)`:
      - `var index = _members.FindIndex(m => ReferenceEquals(m, chart));`
      - `if (index < 0) return;`
      - Dispose all pairwise links involving this chart (filter links where source or target matches chart — since links are created per-pair, track which links correspond to which member index)
      - Actually simpler: since pairwise links are created in order, rebuild: collect links to dispose, remove chart from members, dispose old links, recreate links among remaining members
      - Simpler approach: just dispose all links, clear, re-add all remaining members:
        ```
        var remaining = _members.Where(m => !ReferenceEquals(m, chart)).ToList();
        foreach (var link in _pairwiseLinks) link.Dispose();
        _pairwiseLinks.Clear();
        _members.Clear();
        foreach (var m in remaining) Add(m);
        ```
      - But this re-triggers Add's duplicate check which is fine since members is cleared
      - Actually even simpler: just dispose all and rebuild. Use this approach.
    - `public void Dispose()`:
      - `if (_disposed) return;`
      - `foreach (var link in _pairwiseLinks) link.Dispose();`
      - `_pairwiseLinks.Clear();`
      - `_members.Clear();`
      - `_disposed = true;`

    File 2: `src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceStatus.cs`
    - Namespace: `Videra.SurfaceCharts.Avalonia.Controls.Workspace`
    - Usings: `Videra.SurfaceCharts.Core` for `Plot3DSeriesKind`
    - `public sealed record SurfaceChartPanelStatus(string ChartId, string Label, Plot3DSeriesKind ChartKind, bool IsReady, int SeriesCount, long PointCount);`
    - `public sealed record SurfaceChartWorkspaceStatus(int ChartCount, string? ActiveChartId, IReadOnlyList<SurfaceChartPanelStatus> Panels, int LinkGroupCount, bool AllReady);`

    File 3: `src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceEvidence.cs`
    - Namespace: `Videra.SurfaceCharts.Avalonia.Controls.Workspace`
    - Usings: `System.Globalization`
    - `public static class SurfaceChartWorkspaceEvidence`
    - `public static string Create(SurfaceChartWorkspaceStatus status, string? activeRecipeContext)`:
      - Build a bounded text block using StringBuilder or string concatenation:
      - Header: `"SurfaceCharts workspace evidence\n"`
      - `$"GeneratedUtc: {DateTimeOffset.UtcNow:O}\n"`
      - `$"ChartCount: {status.ChartCount}\n"`
      - `$"ActiveChartId: {status.ActiveChartId ?? "none"}\n"`
      - `$"ActiveRecipeContext: {activeRecipeContext ?? "none"}\n"`
      - For each panel in status.Panels:
        - `$"Panel: {panel.ChartId} | {panel.Label} | {panel.ChartKind} | Ready={panel.IsReady} | Series={panel.SeriesCount} | Points={panel.PointCount}\n"`
      - `$"LinkGroupCount: {status.LinkGroupCount}\n"`
      - `$"AllReady: {status.AllReady}\n"`
      - Return the assembled string

    File 4 (modify): `src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspace.cs`
    - Replace the `NotImplementedException` stub for `CaptureWorkspaceStatus()` with:
      - Iterate `_panels.Values`, for each (chart, info):
        - `var datasetEvidence = chart.Plot.CreateDatasetEvidence();`
        - `var isReady = chart.RenderingStatus.IsReady;`
        - `var seriesCount = datasetEvidence.Series.Count;`
        - `var pointCount = datasetEvidence.Series.Sum(s => s.SampleCount + s.PointCount);`
        - Create `SurfaceChartPanelStatus(info.ChartId, info.Label, info.ChartKind, isReady, seriesCount, pointCount)`
      - `var allReady = panels.All(p => p.IsReady);`
      - Return `new SurfaceChartWorkspaceStatus(_panels.Count, _activeChartId, panels, linkGroupCount: 0, allReady)`
      - Note: linkGroupCount is 0 because the workspace does not track link groups directly; consumers pass their own link group count. This keeps workspace and link group decoupled.
    - Replace the `NotImplementedException` stub for `CreateWorkspaceEvidence()` with:
      - `var status = CaptureWorkspaceStatus();`
      - `var activeInfo = _activeChartId is { } id && _panels.TryGetValue(id, out var entry) ? entry.Info : null;`
      - `return SurfaceChartWorkspaceEvidence.Create(status, activeInfo?.RecipeContext);`

    File 5: `tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/Workspace/SurfaceChartLinkGroupTests.cs`
    - Namespace: `Videra.SurfaceCharts.Avalonia.IntegrationTests.Workspace`
    - Usings: `Videra.SurfaceCharts.Avalonia.Controls`, `Videra.SurfaceCharts.Avalonia.Controls.Workspace`, `Xunit`, `FluentAssertions`
    - All tests use `AvaloniaHeadlessTestSession.Run(() => { ... })`
    - Test methods (each `[Fact]`):
      - `Add_single_chart_to_group`: create group, add chart, Members has count 1
      - `Add_multiple_charts_creates_pairwise_links`: create group, add 3 charts, Members has count 3
      - `Add_duplicate_chart_throws`: add chart, add same chart again, throws InvalidOperationException
      - `Remove_chart_reduces_member_count`: add 3, remove 1, Members has count 2
      - `Dispose_clears_all_members`: add 3, dispose, Members should throw or return empty (check Members after dispose — since class is disposed, accessing Members should still return the cleared list or throw; decide based on implementation — safest is Members returns empty after dispose)
      - `Add_after_dispose_throws`: dispose group, add chart, throws ObjectDisposedException
      - `CameraOnly_policy_throws_NotSupported`: `new SurfaceChartLinkGroup(SurfaceChartLinkPolicy.CameraOnly)` throws NotSupportedException
      - `AxisOnly_policy_throws_NotSupported`: same for AxisOnly
  </action>
  <verify>
    <automated>dotnet test tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/ --filter "FullyQualifiedName~Workspace" --no-restore 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartLinkGroup.cs exists and contains `sealed class SurfaceChartLinkGroup : IDisposable`
    - SurfaceChartLinkGroup.cs contains `public enum SurfaceChartLinkPolicy`
    - SurfaceChartLinkGroup.cs contains `LinkViewWith` in the Add method
    - SurfaceChartLinkGroup.cs contains `NotSupportedException` for CameraOnly/AxisOnly
    - src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceStatus.cs exists and contains `sealed record SurfaceChartWorkspaceStatus`
    - SurfaceChartWorkspaceStatus.cs contains `sealed record SurfaceChartPanelStatus`
    - src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceEvidence.cs exists and contains `static class SurfaceChartWorkspaceEvidence`
    - SurfaceChartWorkspaceEvidence.cs contains `SurfaceCharts workspace evidence`
    - SurfaceChartWorkspace.cs contains `CaptureWorkspaceStatus()` without `NotImplementedException`
    - SurfaceChartWorkspace.cs contains `CreateWorkspaceEvidence()` without `NotImplementedException`
    - SurfaceChartWorkspace.cs contains `CreateDatasetEvidence` call
    - tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/Workspace/SurfaceChartLinkGroupTests.cs exists and contains at least 7 test methods
    - All tests matching `FullyQualifiedName~Workspace` pass (exit 0)
  </acceptance_criteria>
  <done>Link group with pairwise ViewState sync, aggregate status snapshot, workspace evidence formatter, and link group contract tests are all implemented and passing. SurfaceChartWorkspace now fully implements CaptureWorkspaceStatus and CreateWorkspaceEvidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire AnalysisWorkspace demo scenario with workspace service</name>
  <files>
    samples/Videra.SurfaceCharts.Demo/Services/SurfaceChartWorkspaceService.cs,
    samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoScenario.cs,
    samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml,
    samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs
  </files>
  <read_first>
    samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml,
    samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs,
    samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoScenario.cs,
    samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoSupportSummary.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspace.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceStatus.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Workspace/SurfaceChartWorkspaceEvidence.cs
  </read_first>
  <behavior>
    - SurfaceChartWorkspaceService owns a SurfaceChartWorkspace instance and manages chart registration
    - SurfaceChartWorkspaceService.RegisterCharts(VideraChartView[] charts) registers each chart with SurfaceChartPanelInfo derived from chart's active series kind
    - SurfaceChartWorkspaceService.GetWorkspaceStatus() returns CaptureWorkspaceStatus()
    - SurfaceChartWorkspaceService.GetWorkspaceEvidence() returns CreateWorkspaceEvidence()
    - SurfaceChartWorkspaceService.Dispose() disposes the workspace
    - SurfaceDemoScenario adds AnalysisWorkspaceId = "analysis-workspace" const
    - SurfaceDemoScenarios.All includes the new AnalysisWorkspace scenario
    - MainWindow.axaml has a new AnalysisWorkspacePanel Grid (initially hidden) with 4 chart views in a 2x2 grid, a workspace toolbar, and a "Copy workspace evidence" button
    - MainWindow.axaml.cs has a new _workspaceService field (SurfaceChartWorkspaceService)
    - When AnalysisWorkspace scenario is selected: hide single-chart panels, show AnalysisWorkspacePanel, register charts with workspace, populate workspace status text
    - When any other scenario is selected: hide AnalysisWorkspacePanel, show single-chart panels as before
    - MainWindow code-behind does NOT accumulate workspace logic — it delegates to SurfaceChartWorkspaceService
  </behavior>
  <action>
    Create 1 new file and modify 3 existing files.

    File 1: `samples/Videra.SurfaceCharts.Demo/Services/SurfaceChartWorkspaceService.cs`
    - Namespace: `Videra.SurfaceCharts.Demo.Services`
    - Usings: `Videra.SurfaceCharts.Avalonia.Controls`, `Videra.SurfaceCharts.Avalonia.Controls.Workspace`, `Videra.SurfaceCharts.Core`
    - `internal sealed class SurfaceChartWorkspaceService : IDisposable`
    - Private field: `private readonly SurfaceChartWorkspace _workspace = new();`
    - `public void RegisterCharts(IReadOnlyList<(VideraChartView Chart, string Label, Plot3DSeriesKind Kind)> chartEntries)`:
      - `_workspace` is already created
      - For each entry, create `new SurfaceChartPanelInfo(Guid.NewGuid().ToString("N"), entry.Label, entry.Kind)` and call `_workspace.Register(entry.Chart, info)`
    - `public void SetActiveChart(string chartId) => _workspace.SetActiveChart(chartId);`
    - `public SurfaceChartWorkspaceStatus GetWorkspaceStatus() => _workspace.CaptureWorkspaceStatus();`
    - `public string GetWorkspaceEvidence() => _workspace.CreateWorkspaceEvidence();`
    - `public void Dispose() => _workspace.Dispose();`

    File 2 (modify): `samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoScenario.cs`
    - Add constant: `public const string AnalysisWorkspaceId = "analysis-workspace";` to `SurfaceDemoScenarios`
    - Add to the `All` list:
      ```
      new(
          AnalysisWorkspaceId,
          "Try next: Analysis workspace",
          "Multi-chart",
          "Analysis workspace",
          "Shows four VideraChartView instances in a bounded 2x2 grid with workspace status and evidence.")
      ```
    - Keep all existing scenarios unchanged

    File 3 (modify): `samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml`
    - Read the existing AXAML first to understand the layout structure
    - Add a new `Grid` named `AnalysisWorkspacePanel` with `IsVisible="False"` alongside the existing chart panels
    - Inside AnalysisWorkspacePanel, create a 2x2 grid layout:
      ```xml
      <Grid Name="AnalysisWorkspacePanel" IsVisible="False" RowDefinitions="*,*" ColumnDefinitions="*,*">
          <vcd:VideraChartView Name="WorkspaceChartA" Grid.Row="0" Grid.Column="0" Margin="2"/>
          <vcd:VideraChartView Name="WorkspaceChartB" Grid.Row="0" Grid.Column="1" Margin="2"/>
          <vcd:VideraChartView Name="WorkspaceChartC" Grid.Row="1" Grid.Column="0" Margin="2"/>
          <vcd:VideraChartView Name="WorkspaceChartD" Grid.Row="1" Grid.Column="1" Margin="2"/>
      </Grid>
      ```
    - Add a workspace toolbar section (StackPanel) below the grid with:
      - `TextBlock Name="WorkspaceStatusText"` for status display
      - `Button Name="CopyWorkspaceEvidenceButton" Content="Copy workspace evidence"`
    - Place the AnalysisWorkspacePanel in the same location as the existing chart content area (where ChartView, WaterfallPlotView, etc. live)
    - Ensure the namespace prefix for VideraChartView (`vcd:` or whatever the existing AXAML uses) matches the existing file

    File 4 (modify): `samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs`
    - Add field: `private readonly VideraChartView _workspaceChartA;` (and B, C, D) — find by name using `FindControl`
    - Add field: `private readonly Grid _analysisWorkspacePanel;`
    - Add field: `private readonly TextBlock _workspaceStatusText;`
    - Add field: `private readonly Button _copyWorkspaceEvidenceButton;`
    - Add field: `private SurfaceChartWorkspaceService? _workspaceService;`
    - In constructor, find all new controls by name
    - Wire `_copyWorkspaceEvidenceButton.Click += OnCopyWorkspaceEvidenceClicked;`
    - In `OnSourceSelectionChanged`, add case for `AnalysisWorkspaceId`:
      - Call `ApplyAnalysisWorkspace(scenario)`
    - `ApplyAnalysisWorkspace(SurfaceDemoScenario scenario)`:
      - Call `SetActiveChartView`-like logic to hide all single-chart panels and show `_analysisWorkspacePanel`
      - Dispose old `_workspaceService` if any
      - Create new `SurfaceChartWorkspaceService`
      - Register the 4 workspace charts with labels and kinds:
        - Chart A: "Surface A", Plot3DSeriesKind.Surface — load in-memory surface source
        - Chart B: "Bar B", Plot3DSeriesKind.Bar — load bar data
        - Chart C: "Scatter C", Plot3DSeriesKind.Scatter — load scatter data
        - Chart D: "Contour D", Plot3DSeriesKind.Contour — load contour data
      - Call `_workspaceService.SetActiveChart(chartAId)`
      - Update `_workspaceStatusText` with workspace status summary
    - `OnCopyWorkspaceEvidenceClicked`: copy `_workspaceService.GetWorkspaceEvidence()` to clipboard
    - In `SetActiveChartView`, add logic: if switching away from workspace, hide `_analysisWorkspacePanel`; if switching to workspace, hide all single-chart panels
    - Keep all existing MainWindow logic unchanged — the workspace is additive
  </action>
  <verify>
    <automated>dotnet build samples/Videra.SurfaceCharts.Demo/Videra.SurfaceCharts.Demo.csproj --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - samples/Videra.SurfaceCharts.Demo/Services/SurfaceChartWorkspaceService.cs exists
    - SurfaceChartWorkspaceService.cs contains `class SurfaceChartWorkspaceService : IDisposable`
    - SurfaceChartWorkspaceService.cs contains `SurfaceChartWorkspace _workspace`
    - SurfaceChartWorkspaceService.cs contains `GetWorkspaceStatus()` and `GetWorkspaceEvidence()`
    - samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoScenario.cs contains `AnalysisWorkspaceId`
    - samples/Videra.SurfaceCharts.Demo/Services/SurfaceDemoScenario.cs contains `"analysis-workspace"`
    - samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml contains `Name="AnalysisWorkspacePanel"`
    - samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml contains `Name="WorkspaceChartA"`
    - samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml contains `CopyWorkspaceEvidenceButton`
    - samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs contains `SurfaceChartWorkspaceService`
    - samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs contains `ApplyAnalysisWorkspace`
    - samples/Videra.SurfaceCharts.Demo/Views/MainWindow.axaml.cs contains `GetWorkspaceEvidence`
    - MainWindow.axaml.cs does NOT contain workspace registration logic inline — delegates to SurfaceChartWorkspaceService
    - dotnet build of the demo project exits 0
    - dotnet test with workspace filter exits 0
  </acceptance_criteria>
  <done>AnalysisWorkspace demo scenario is wired: 4 charts in a 2x2 grid, workspace service manages registration/status/evidence, MainWindow delegates to service (no god-code), "Copy workspace evidence" button works, scenario catalog includes the new entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> link group | Host adds charts to link group; group creates pairwise links but does not own chart lifecycle |
| demo -> workspace service | MainWindow delegates workspace operations to service; service owns workspace instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-426-03 | Tampering | SurfaceChartLinkGroup | accept | Link group only syncs ViewState via existing pairwise API; no external input surface |
| T-426-04 | Denial of Service | Link group re-entrancy | mitigate | Use _isSynchronizing guard pattern from existing VideraChartViewLink; links are disposable |
| T-426-05 | Information Disclosure | WorkspaceEvidence | accept | Evidence is diagnostic text copied to clipboard by host; no sensitive data |
</threat_model>

<verification>
After all tasks complete:
- `dotnet build samples/Videra.SurfaceCharts.Demo/Videra.SurfaceCharts.Demo.csproj` exits 0
- `dotnet test tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/ --filter "FullyQualifiedName~Workspace"` exits 0 with all workspace and link group tests passing
- `dotnet test tests/Videra.SurfaceCharts.Avalonia.IntegrationTests/` exits 0 (full integration suite still passes)
- `git diff --check` shows no whitespace errors
</verification>

<success_criteria>
- SurfaceChartLinkGroup implements N-chart pairwise ViewState sync with dispose cleanup and re-entrancy guard
- SurfaceChartWorkspaceStatus and SurfaceChartPanelStatus records provide aggregate workspace snapshot
- SurfaceChartWorkspaceEvidence produces bounded diagnostic text
- SurfaceChartWorkspace.CaptureWorkspaceStatus and CreateWorkspaceEvidence are fully implemented
- 7+ link group contract tests pass
- Demo AnalysisWorkspace scenario shows 4 charts in 2x2 grid with workspace status and evidence copy
- SurfaceChartWorkspaceService separates workspace logic from MainWindow code-behind (WORK-03)
- No changes to existing library test files — all additive
</success_criteria>

<output>
After completion, create `.planning/phases/426-native-multi-chart-analysis-workspace/426-02-SUMMARY.md`
</output>
