# Plan 45-03

## Goal

Narrow `LegacyGraphicsBackendAdapter` usage in the orchestrator and verify the direct path.

## Files

- `src/Videra.Avalonia/Rendering/RenderSessionOrchestrator.cs`
- `tests/Videra.Core.IntegrationTests/Rendering/GraphicsDeviceSurfaceIntegrationTests.cs`

## Acceptance

1. The orchestrator prefers direct `IGraphicsDevice` implementations from built-in backends.
2. The legacy adapter is only used for non-migrated or custom monolithic backends.
3. Diagnostics and software-backend detection remain truthful after the migration.
