# Plan 45-01

## Goal

Let the software backend satisfy the graphics device/surface contracts directly.

## Files

- `src/Videra.Core/Videra.Core.csproj`
- `src/Videra.Core/Graphics/Software/SoftwareBackend.cs`

## Acceptance

1. The software backend implements `IGraphicsDevice` and `IRenderSurface` directly.
2. Frame scheduling can obtain a direct frame context from the software backend.
3. Core internals needed for the migration are visible to platform packages without widening public API.
