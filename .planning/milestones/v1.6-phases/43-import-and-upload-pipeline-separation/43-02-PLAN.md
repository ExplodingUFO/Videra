# Plan 43-02

## Goal

Make batch import bounded-parallel and apply atomic replace semantics only on full success.

## Files

- `src/Videra.Avalonia/Runtime/VideraViewRuntime.Scene.cs`

## Acceptance

1. `LoadModelsAsync()` uses bounded concurrency instead of serial `foreach await`.
2. Result ordering remains deterministic for callers.
3. Active scene replacement happens only when every requested import succeeds.
