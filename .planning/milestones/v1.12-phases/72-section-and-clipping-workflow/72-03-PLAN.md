# Phase 72 Plan 03

## Scope

- 让 clipping 对 overlays / exported snapshots 也生效
- 保持 `VideraViewRuntime` 薄壳，不把 clipping mechanics 堆回 shell
- 用 integration coverage 锁定 scene/diagnostics contract

## Verification

- `dotnet test tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~VideraViewInspectionIntegrationTests"`
