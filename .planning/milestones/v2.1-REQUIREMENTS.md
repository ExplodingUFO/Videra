# Requirements

Active milestone: `v2.1 Scatter Chart Family`

Chart requirement numbering continues from `CHRT-07`.

- [x] `CHRT-08` Before implementation, lock `ScatterChartView` as the one concrete third chart family and freeze the explicit non-goals.
- [x] `CHRT-09` Reuse only the minimal kernel/runtime seams needed for `ScatterChartView`, without god abstractions or generic `Chart3D`.
- [x] `CHRT-10` Prove `ScatterChartView` through one thin direct Avalonia path with chart-local interaction, render, and support flow.
- [ ] `CHRT-11` Keep docs, support, package, release, and repository truth aligned on the same tri-family chart story.

## Out of Scope

| Item | Excluded from `v2.1` |
|------|------------------------|
| Generic `Chart3D` | Not part of the milestone. |
| Separate top-level `PointLine` family | Not part of the milestone; connected lines do not get a separate family boundary in `v2.1`. |
| `SurfaceMesh` | Not part of the milestone. |
| Fourth chart family | Not part of the milestone. |
| New UI adapters | Not part of the milestone. |
| `Wayland` / `OpenGL` work | Not part of the milestone. |
| Transparency / lighting / post-processing expansion | Not part of the milestone. |

## Traceability

| Requirement | Phase |
|-------------|-------|
| `CHRT-08` | Phase 136 |
| `CHRT-09` | Phase 137 |
| `CHRT-10` | Phase 138 |
| `CHRT-11` | Phase 139 |
