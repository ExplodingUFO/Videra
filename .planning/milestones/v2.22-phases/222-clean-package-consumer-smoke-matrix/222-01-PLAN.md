# Phase 222 Plan: Clean Package Consumer Smoke Matrix

**Status:** complete
**Requirement:** `CPS-01`, `CPS-02`, `CPS-03`, `CPS-04`

## Success Criteria

1. Consumer smoke uses isolated local package artifacts and does not validate through source references.
2. Viewer-only, OBJ, glTF, and SurfaceCharts scenarios each have an explicit command path and artifact output.
3. Per-scenario results identify package set, scenario name, success/failure, and support artifact paths.
4. SurfaceCharts packaged smoke captures `surfacecharts-support-summary.txt`.
5. Repository tests or scripts guard the scenario matrix from drifting.

## Tasks

1. Extend `Invoke-ConsumerSmoke.ps1` with explicit scenario selection and scenario package metadata.
2. Update `smoke/Videra.ConsumerSmoke` so importer packages are conditional by model format.
3. Add a minimal tracked glTF asset for the `ViewerGltf` path.
4. Update repository guard tests for the clean package scenario matrix.
5. Verify targeted tests, build-only smoke scenarios, warnings-as-errors smoke scenarios, and solution build.

## Verification

- Repository tests:
  `dotnet test tests\Videra.Core.Tests\Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~AlphaConsumerIntegrationTests|FullyQualifiedName~VideraDoctorRepositoryTests" -p:RestoreIgnoreFailedSources=true`
- Build-only scenario matrix:
  `ViewerOnly`, `ViewerObj`, `ViewerGltf`, `SurfaceCharts`
- Warnings-as-errors build-only scenario matrix:
  `ViewerOnly`, `ViewerObj`, `ViewerGltf`, `SurfaceCharts`
- Solution build:
  `dotnet build Videra.slnx -c Release -p:RestoreIgnoreFailedSources=true`
