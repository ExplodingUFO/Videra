# Phase 225 Plan

## Scope

Make targeted documentation and repository-test updates only. No workflow, package, or release behavior changes.

## Tasks

1. Add an alpha candidate checklist to `docs/releasing.md`.
2. Add release-note guidance for validation status and known non-blockers.
3. Expand `docs/alpha-feedback.md` support artifact routing for viewer, import, backend, package, native-host, packaged viewer, and SurfaceCharts issues.
4. Add a changelog entry for alpha candidate validation guidance.
5. Extend repository tests to lock script names, artifact names, support-routing categories, changelog wording, and non-publishing language.

## Verification

- Run release-readiness repository tests.
- Run release-readiness plus Doctor/release-dry-run repository tests.
- Run full solution build.
- Run `git diff --check`.
