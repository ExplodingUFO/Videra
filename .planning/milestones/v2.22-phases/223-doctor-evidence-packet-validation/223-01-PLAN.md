# Phase 223 Plan

## Scope

Add a small structured evidence packet to Doctor output and tests. Avoid changing validator behavior, release dry-run behavior, or package build behavior.

## Tasks

1. Extend `scripts/Invoke-VideraDoctor.ps1` with helper functions that report existing check status and artifact presence.
2. Add `evidencePacket` to `doctor-report.json` with repository, machine, package contract, validation script, support artifact, Doctor output, and release-readiness artifact references.
3. Add a human-readable evidence packet section to `doctor-summary.txt`.
4. Update `docs/videra-doctor.md` to describe the packet and ownership boundary.
5. Extend `VideraDoctorRepositoryTests` to lock the packet shape and validation field contract.

## Verification

- Run default Doctor script.
- Run Doctor repository tests.
- Run Doctor plus consumer-smoke repository tests.
- Run full solution build.
- Run `git diff --check`.
