# Phase 224 Plan

## Scope

Strengthen the existing release dry-run scripts without changing package publication behavior or adding new release automation.

## Tasks

1. Add structured `status`, `steps`, and `artifactPaths` to `release-dry-run-summary.json`.
2. Include dry-run status, artifacts, and validation steps in `release-candidate-evidence-index.json`.
3. Make evidence-index generation fail closed when package validation artifacts or required summary fields are missing.
4. Fix script command-success checks so successful PowerShell child scripts do not fail because `$LASTEXITCODE` is `$null`.
5. Add repository tests for structured evidence fields and fail-closed behavior.

## Verification

- Run ReleaseDryRun repository tests.
- Run a real release dry run for current repository version `0.1.0-alpha.7`.
- Run ReleaseDryRun plus Doctor repository tests.
- Run full solution build.
- Run `git diff --check`.
