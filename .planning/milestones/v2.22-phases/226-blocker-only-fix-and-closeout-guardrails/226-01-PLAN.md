# Phase 226 Plan

## Scope

Add closeout classification guardrails, run the alpha candidate validation loop, and fix only blocker findings discovered by that evidence.

## Tasks

1. Add blocker/environment/deferred classification language to the release-candidate cutover guide.
2. Extend repository tests to guard the closeout classification categories and scope language.
3. Run Doctor, release dry-run, clean package consumer smoke, targeted repository tests, full build, and Windows native validation.
4. Classify validation findings.
5. Fix only release blockers that fail validation and are directly tied to release-readiness evidence.
6. Re-run targeted and closeout validation after fixes.

## Verification

- Run closeout classification repository tests.
- Run combined release-readiness repository tests.
- Run full solution build.
- Run repo-only Doctor.
- Run release dry-run for `0.1.0-alpha.7`.
- Run packaged consumer smoke matrix in build-only mode.
- Run Windows native validation.
- Run `git diff --check`.
