# Plan 50-01

## Goal

Implement `SceneItemsAdapter` for incremental collection mutations

## Files

- `src/Videra.Avalonia/Runtime/Scene/SceneItemsAdapter.cs`
- `tests/Videra.Avalonia.Tests/Scene/SceneItemsAdapterTests.cs`

## Acceptance

1. Add/remove/replace/move no longer force full document rebuilds.
2. Only reset-like changes rebuild the whole document.
3. Collection-event parsing lives in a focused adapter instead of the runtime partial.
