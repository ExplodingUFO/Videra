# Plan 47-01

## Goal

Create the dedicated Avalonia runtime test project and render-session baselines

## Files

- `Videra.slnx`
- `tests/Videra.Avalonia.Tests/Videra.Avalonia.Tests.csproj`
- `tests/Videra.Avalonia.Tests/Rendering/RenderSessionRuntimeTests.cs`

## Acceptance

1. The new test surface covers runtime-scene behavior without routing through the demo.
2. The plan keeps production behavior unchanged while adding focused evidence.
3. The resulting tests can be reused by later scene-pipeline phases.
