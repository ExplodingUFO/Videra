# Phase 171 Plan: Release Abort and Cutover Runbook Truth

## Goal

Make failed-candidate abort handling and human release cutover boundaries explicit and guarded.

## Tasks

1. Add a focused runbook.
   - Define abort criteria.
   - Define manual cutover prerequisites.
   - State that no publish/tag happens before human approval.

2. Link the runbook from release docs and evidence contract.
   - Keep candidate review starting from evidence index.
   - Make abort/cutover docs discoverable.

3. Add repository tests.
   - Guard runbook terms.
   - Guard release dry-run remains non-publishing.
   - Guard manual cutover language.

4. Verify.
   - Run focused repository tests.
   - Confirm phase branch clean after commit.

## Acceptance

- Abort criteria and failure handling are explicit.
- Human cutover boundaries are explicit.
- Docs and repository guards align with final-validation truth.
- `RCV-04` is covered.
