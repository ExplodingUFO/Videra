# Phase 168 Plan: Release Candidate Final Validation Scope Lock

## Goal

Make the `v2.9` final-validation scope explicit and safe before implementation starts.

## Tasks

1. Freeze the milestone boundary.
   - Keep scope to release-candidate validation, evidence review, and abort/cutover truth.
   - Exclude runtime/rendering/material/chart/backend/UI breadth, compatibility paths, publishing, and tag creation.

2. Pin candidate vocabulary.
   - Use these terms consistently: candidate version, tag simulation, release evidence packet, release dry run, package validation evidence, abort criteria, human cutover.

3. Define implementation split.
   - Phase 169: candidate version and tag simulation guardrails.
   - Phase 170: release evidence packet and review index.
   - Phase 171: release abort and cutover runbook truth.

4. Verify planning closure.
   - Confirm `RCV-01` is covered.
   - Update roadmap, requirements, and state.

## Acceptance

- `v2.9` is locked to final release-candidate validation.
- `RCV-01` is complete.
- No code changes are introduced in this phase.
