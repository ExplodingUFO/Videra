# Phase 170 Plan: Release Evidence Packet and Review Index

## Goal

Generate one release-candidate evidence index from the dry-run path.

## Tasks

1. Add an evidence contract.
   - List required checks.
   - List required artifact families.
   - List support docs that reviewers should consult.

2. Add an evidence index generator.
   - Read the evidence contract.
   - Read `release-dry-run-summary.json`.
   - Emit `release-candidate-evidence-index.json`.
   - Emit `release-candidate-evidence-index.txt`.

3. Integrate with release dry run.
   - Generate the evidence index after dry-run summary validation.
   - Keep the path read-only and non-publishing.

4. Add tests/docs.
   - Guard script wiring and evidence vocabulary.
   - Update release docs to mention the evidence index.

## Acceptance

- Candidate reviewers have one evidence index.
- The evidence index is generated by release dry-run.
- The evidence index remains read-only and non-publishing.
- `RCV-03` is covered.
