# Plan 116-01: Inspection Replay and Export Hardening

**Phase:** 116
**Goal:** make inspection bundle replay/export boundaries explicit, direct, and auditable

## Tasks

1. Harden `VideraInspectionBundleService.ImportAsync(...)` so any non-replayable bundle fails explicitly before stale object-backed state can be applied.
   Verify: integration coverage proves non-replayable host-owned-object bundles return a clear import failure instead of mutating the target view.

2. Surface replay limitations through the export result and interaction sample so callers can explain non-replayable bundles directly.
   Verify: targeted tests/builds and sample wording reflect both `CanReplayScene` and the limitation text.

3. Align support/troubleshooting docs with the shipped replay/export contract.
   Verify: repository truth/docs tests still pass after support guidance is updated.

## Success Criteria

1. Non-replayable bundles cannot be imported silently.
2. Export callers can read the replay limitation without parsing bundle internals manually.
3. The interaction sample and support docs describe exportable-but-non-replayable bundles explicitly.
4. Targeted integration tests and repository truth tests pass.
