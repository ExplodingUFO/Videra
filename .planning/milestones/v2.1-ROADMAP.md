# Roadmap

## Active Milestone

### `v2.1 Scatter Chart Family`

- Status: Phase 139 planned next
- Scope: add one concrete third chart family (`ScatterChartView`) on top of shipped `Surface + Waterfall`, then align truth across code, docs, support, package, and release surfaces

## Operational Baseline

- `Surface + Waterfall` is already shipped as the current chart baseline.
- `v2.0` is closed, and transparency is not reopening in this milestone.
- `ScatterChartView` is now locked as the only third-family target in scope for `v2.1`.
- `v1.27` archived the dual-family chart story; `v1.26` validated a second UI adapter without changing chart scope.

## Phases

### Phase 136: Third Chart Family Scope Lock

- Goal: choose the concrete third chart family and freeze explicit non-goals before kernel/UI changes.
- Depends on: `v2.0` closeout plus the shipped `Surface + Waterfall` baseline
- Status: `complete`
- Chosen target: `ScatterChartView`
- Plans: `136-01` completed

Plans:

- [x] 136-01: confirm `ScatterChartView` as the only third-family target in scope
- [x] 136-02: freeze the non-goals that stay out of `v2.1`
- [x] 136-03: capture the phase boundary before any kernel or Avalonia edits start

### Phase 137: Minimal Kernel Reuse for Scatter

- Goal: extend only the minimal chart contracts/runtime seams needed for `ScatterChartView`.
- Depends on: Phase 136
- Target family: `ScatterChartView`
- Status: `complete`
- PR: `#51`
- Merge commit on `master`: `655ff2e70ea8071a2ef295af7e69c4da6c9ecfb2`

Plans:

- [x] Reuse the existing chart kernel seams instead of introducing a new abstraction layer.
- [x] Add only the discrete data, axis, and interaction hooks `ScatterChartView` actually needs.
- [x] Keep generic `Chart3D` and broader runtime breadth out of the implementation path.

### Phase 138: Thin Scatter Chart Proof on Avalonia

- Goal: ship one thin direct `ScatterChartView` path on the existing Avalonia chart line.
- Depends on: Phase 137
- Status: `complete`
- PR: `#52`
- Merge commit on `master`: `332907600b224251815e02108be051c789904b4e`

- [x] Prove the family through one direct Avalonia rendering path.
- [x] Keep interaction and support flow chart-local rather than shared across a new product layer.
- [x] Verify the proof path against the chosen family without widening to a second UI adapter.

### Phase 139: Tri-Family Truth and Product Guardrails

- Goal: align docs/support/package/release truth and repository guards around `Surface + Waterfall + Scatter`.
- Depends on: Phase 137 and Phase 138
- [ ] Update product wording so every surface tells the same tri-family story.
- [ ] Align package and release references with the shipped chart baseline.
- [ ] Add repository guardrails that keep the milestone from drifting into broader scope.

## Recently Shipped

- `v2.0 Advanced Runtime Features` — shipped `2026-04-22`; phases `131-135`; requirements `5/5`; audit `passed`; archive: `.planning/milestones/v2.0-ROADMAP.md`, `.planning/milestones/v2.0-REQUIREMENTS.md`, `.planning/v2.0-MILESTONE-AUDIT.md`
- `v1.27 Additional Chart Breadth` — shipped `2026-04-22`; phases `127-130`; requirements `3/3`; audit `passed`; archive: `.planning/milestones/v1.27-ROADMAP.md`, `.planning/milestones/v1.27-REQUIREMENTS.md`, `.planning/v1.27-MILESTONE-AUDIT.md`
- `v1.26 Second UI Adapter Validation` — shipped `2026-04-22`; phases `124-126`; requirements `3/3`; audit `passed`; archive: `.planning/milestones/v1.26-ROADMAP.md`, `.planning/milestones/v1.26-REQUIREMENTS.md`, `.planning/v1.26-MILESTONE-AUDIT.md`
