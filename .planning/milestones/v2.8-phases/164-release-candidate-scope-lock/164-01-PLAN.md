# Phase 164 Plan: Release Candidate Scope Lock

## Goal

Make the `v2.8` release-candidate scope explicit and safe before implementation starts.

## Tasks

1. Freeze the milestone boundary.
   - Keep scope to public API/package/release/docs truth.
   - Exclude runtime/rendering/chart/backend breadth and actual publishing.

2. Pin candidate vocabulary.
   - Use these terms consistently: public package line, public API contract, release dry run, package assets, generated release notes, support boundary.

3. Define implementation split.
   - Phase 165: public API contract guardrails.
   - Phase 166: release dry run and package asset evidence.
   - Phase 167: candidate docs and repository truth closure.

4. Verify planning closure.
   - Confirm `RC-01` is covered.
   - Update roadmap, requirements, and state.

## Acceptance

- `v2.8` is locked to release-candidate contract closure.
- `RC-01` is complete.
- No code changes are introduced in this phase.
