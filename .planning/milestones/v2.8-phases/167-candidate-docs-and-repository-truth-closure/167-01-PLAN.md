# Phase 167 Plan: Candidate Docs and Repository Truth Closure

## Goal

Align docs, changelog/release notes guidance, support wording, localized docs, and repository guards with the release-candidate contract.

## Tasks

1. Update candidate-facing docs to name the release dry-run workflow, script, evidence artifact, public API contract manifest, and package validation script.
2. Update localized Chinese entry docs with the same release-candidate dry-run boundary at navigation level.
3. Add a focused repository truth test that ensures the docs and workflow/script stay aligned.
4. Verify with focused repository tests and whitespace checks.

## Acceptance Criteria

- README, capability matrix, package matrix, support matrix, release policy, releasing runbook, changelog, and Chinese entry docs describe the same release-candidate evidence path.
- Repository tests fail if docs drop the release-candidate dry-run evidence vocabulary.
- The phase does not alter package publishing behavior, runtime behavior, chart behavior, backend behavior, compatibility, or fallback paths.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --no-restore -m:1 --filter "FullyQualifiedName~ReleaseCandidateTruthRepositoryTests|FullyQualifiedName~ReleaseDryRunRepositoryTests|FullyQualifiedName~RepositoryReleaseReadinessTests|FullyQualifiedName~RepositoryLocalizationTests"`
- `git diff --check`
