# Plan 195-01: Direct-Lighting Truth and Guardrails

## Success Criteria

1. Public and repository-facing docs describe one bounded style-driven direct-lighting baseline on the native static-scene viewer path.
2. The same docs keep shadows, environment maps, post-processing, animation, and broader advanced-runtime breadth deferred.
3. Native-validation/support wording describes the proof-mode `10`-second hold honestly as repository-owned validation evidence, not a product feature expansion.
4. Repository guardrails are updated and passing.

## Assumptions

- The milestone needs wording corrections, not more code-path changes.
- Repository architecture/readiness/localization tests are the narrowest useful guardrail set.
- Chinese mirrors should stay aligned in the same phase so doc truth does not drift.

## Steps

1. Update boundary docs and package/support docs to reflect the direct-lighting baseline.
2. Update native-validation/support wording to mention the proof-mode `10`-second hold honestly.
3. Update repository guard tests to match the new wording.
4. Verify with focused repository doc/guardrail tests and diff checks.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~RepositoryArchitectureTests|FullyQualifiedName~RepositoryReleaseReadinessTests|FullyQualifiedName~RepositoryLocalizationTests"`
- `git diff --check master...HEAD`
