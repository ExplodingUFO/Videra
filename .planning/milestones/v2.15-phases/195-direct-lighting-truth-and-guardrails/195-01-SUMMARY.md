# Phase 195 Summary

Phase `195` aligned the viewer/runtime boundary docs, support docs, Chinese mirrors, and repository guardrails with the `v2.15` direct-lighting baseline.

Delivered:

- Boundary docs now describe the shipped baseline as one bounded style-driven direct-lighting path on the native static-scene viewer/runtime route.
- The same docs keep shadows, environment maps, post-processing, animation, and broader advanced-runtime breadth deferred.
- Native-validation/support docs now describe the proof-mode `10`-second hold as repository-owned validation evidence on the selected smoke hosts.
- Repository architecture/readiness guard tests were updated to pin the new wording.

Boundaries kept:

- No runtime or script behavior changes in this phase.
- No product-surface widening beyond the already-shipped direct-lighting baseline.
- No new package or release-path promises.

Implementation commit on the phase branch:

- `a5f5fa3` `docs: align direct-lighting baseline guardrails`
