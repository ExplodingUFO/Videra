# Phase 217 Plan

## Goal

Align benchmark threshold tests and docs with the current committed hard-gate slice without changing benchmark semantics.

## Steps

1. Reproduce the stale benchmark threshold test failure.
2. Update `BenchmarkThresholdRepositoryTests` so it expects the actual committed threshold slice.
3. Guard each committed threshold name against `benchmarks/benchmark-contract.json`.
4. Remove duplicated/stale benchmark gate documentation language and clarify evidence-only benchmark semantics.
5. Run targeted repository tests plus solution build.
6. Commit the phase on an isolated worktree branch and fast-forward merge only from a clean branch.

## Success Criteria

- The benchmark threshold repository test passes against the real threshold file.
- Benchmark docs describe the current seven-name hard gate and evidence-only entries.
- No threshold names are removed and no threshold budgets are widened.
