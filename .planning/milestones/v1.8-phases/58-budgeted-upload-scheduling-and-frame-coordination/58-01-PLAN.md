# Phase 58 Plan 01

## Goal

Make upload budget resolution respond to runtime mode and queue pressure.

## Steps

1. Extend `SceneUploadBudget` with `Resolve(...)`.
2. Keep interactive budgets tighter than idle budgets for the same backlog.
3. Add focused tests around queue-pressure-driven budget changes.

