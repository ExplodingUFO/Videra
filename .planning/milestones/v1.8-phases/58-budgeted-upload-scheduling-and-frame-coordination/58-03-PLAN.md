# Phase 58 Plan 03

## Goal

Keep upload coordination entirely inside frame-prelude cadence.

## Steps

1. Resolve budgets inside `RuntimeFramePrelude`.
2. Drain uploads and apply ready scene objects there.
3. Keep `LoadModelAsync()`, `LoadModelsAsync()`, `AddObject()`, `ReplaceScene()`, and `ItemsChanged()` free of synchronous GPU resource creation.

