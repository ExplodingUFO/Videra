# Phase 56 Plan 03

## Goal

Close the runtime integration seam so backend-ready events still trigger rehydrate work without reopening cadence-driven dirtiness.

## Steps

1. Route backend-ready/resource-epoch events through the new residency dirty API.
2. Keep queue dedupe behavior intact so already in-flight ids are not repeatedly enqueued.
3. Add focused runtime tests that prove resident entries stay resident during steady-state frames.

