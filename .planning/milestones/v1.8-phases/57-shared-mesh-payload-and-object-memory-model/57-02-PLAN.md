# Phase 57 Plan 02

## Goal

Move `Object3D` onto the shared payload model and expose explicit retention metadata.

## Steps

1. Replace duplicated cached mesh arrays with a single retained `MeshPayload`.
2. Keep wireframe, recolor, and reupload behavior working against the shared payload.
3. Expose explicit internal retention-policy metadata without widening the public API.

