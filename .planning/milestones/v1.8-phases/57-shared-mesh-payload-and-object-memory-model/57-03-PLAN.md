# Phase 57 Plan 03

## Goal

Switch deferred object creation onto shared payload semantics.

## Steps

1. Update `SceneObjectFactory` to bind imported assets directly to their shared payload.
2. Preserve deferred object creation semantics and viewer/import entrypoints.
3. Add focused tests that prove multiple deferred objects can share one payload instance.
