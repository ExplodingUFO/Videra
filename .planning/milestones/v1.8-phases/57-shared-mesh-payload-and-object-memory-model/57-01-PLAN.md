# Phase 57 Plan 01

## Goal

Introduce a shared mesh payload model at the imported-asset boundary.

## Steps

1. Add `MeshPayload` as the shared vertex/index/topology/bounds carrier.
2. Attach payload and metrics generation to `ImportedSceneAsset` / `ModelImporter`.
3. Keep public import entrypoints unchanged.

