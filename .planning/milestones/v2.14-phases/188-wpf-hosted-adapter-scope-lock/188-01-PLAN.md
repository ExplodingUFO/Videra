# Phase 188 Plan: WPF Hosted Adapter Scope Lock

## Goal

Freeze `v2.14` as one bounded, repository-only `WPF on Windows` hosted-adapter validation slice before implementation begins.

## Tasks

1. Define the milestone boundary.
   - Keep the milestone inside repository-only WPF host validation on the existing viewer/runtime line.
   - Reuse the current `RenderSessionOrchestrator -> D3D11 -> diagnostics` path instead of widening runtime, renderer, backend, chart, or importer scope.
   - Avoid a general multi-shell abstraction layer, a second public UI package line, release-path changes, compatibility work, or migration adapters.

2. Pin canonical vocabulary.
   - repository-only WPF smoke proof
   - hosted-adapter validation
   - shared host seam
   - diagnostics parity
   - support evidence only

3. Define implementation phases.
   - Phase 189: shared host-seam tightening for WPF.
   - Phase 190: repo-only WPF golden path and diagnostics parity.
   - Phase 191: WPF repository truth and guardrails.

4. Update local planning.
   - Rewrite the active roadmap and requirements for `v2.14`.
   - Mark `WPF-01` as covered by this planning-only phase.
   - Reset `STATE.md` so execution resumes from Phase 189 after the scope lock is complete.

## Acceptance

- `v2.14` has a bounded, falsifiable scope centered on one repository-only WPF validation path.
- Explicit non-goals block public UI expansion, runtime/chart/backend/import breadth, and compatibility work.
- The next implementation phase can start from clear requirements and a narrow file surface.
