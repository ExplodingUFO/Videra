# Plan 190-01: Repo-Only WPF Golden Path and Diagnostics Parity

## Success Criteria

1. `smoke/Videra.WpfSmoke` renders one bounded scene on the current Windows/D3D11 path.
2. The generated diagnostics artifact carries the viewer/runtime support vocabulary needed for repository-native validation.
3. Repository-native verification can invoke the WPF smoke proof without implying a public package contract.

## Steps

1. Seed one bounded smoke scene inside `Videra.WpfSmoke`.
2. Expand the diagnostics artifact with render-pipeline and host-binding truth.
3. Add one repository script path to invoke the WPF smoke proof and assert the artifact contents.
4. Tighten repository tests around the new smoke truth.

## Verification

- `dotnet test tests\Videra.Core.Tests\Videra.Core.Tests.csproj -c Release --no-restore -m:1 --filter "FullyQualifiedName~WpfSmokeConfigurationTests|FullyQualifiedName~RepositoryNativeValidationTests"`
- `pwsh -NoProfile -File scripts\Invoke-WpfSmoke.ps1 -Configuration Release -OutputRoot artifacts\wpf-smoke-phase190`
- `git diff --check`
