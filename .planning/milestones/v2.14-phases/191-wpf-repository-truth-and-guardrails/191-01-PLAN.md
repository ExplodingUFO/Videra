# Plan 191-01: WPF Repository Truth and Guardrails

## Success Criteria

1. Repository docs describe `smoke/Videra.WpfSmoke` as validation/support evidence only on the Avalonia-first viewer path.
2. The same docs do not imply a second public UI package or release path.
3. Native-validation docs explicitly describe the hosted Windows invocation path and its repository-only diagnostics artifact.
4. Repository tests lock the new wording and the hosted-plus-local WPF truth.

## Steps

1. Tighten WPF wording in `README.md`, `docs/package-matrix.md`, and `docs/support-matrix.md`.
2. Make the hosted Windows WPF invocation path explicit in `docs/native-validation.md` and `docs/zh-CN/native-validation.md`.
3. Update repository tests to assert the new wording and hosted/local validation truth.

## Verification

- `rg -n "smoke/Videra.WpfSmoke|Hosted and local Windows WPF smoke proof|not a second public UI package or release path|Invoke-WpfSmoke.ps1|wpf-smoke-diagnostics.txt" README.md docs tests`
- `dotnet test tests\Videra.Core.Tests\Videra.Core.Tests.csproj -c Release --no-restore -m:1 --filter "FullyQualifiedName~RepositoryNativeValidationTests|FullyQualifiedName~RepositoryReleaseReadinessTests"`
- `git diff --check`
