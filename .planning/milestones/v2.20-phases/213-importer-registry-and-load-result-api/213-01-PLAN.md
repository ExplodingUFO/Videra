# Phase 213 Plan

## Scope

1. Add Core importer contracts: `IVideraModelImporter`, `ModelImportRequest`, `ModelImportResult`, and diagnostics.
2. Add `Create()` registry adapters to OBJ and glTF import packages.
3. Add `VideraViewOptions.ModelImporters` plus `UseModelImporter(...)`.
4. Update the Avalonia runtime import service to select registered importers by `CanImport(...)`, preserve cancellation, cache imported assets by importer identity, and preserve legacy delegate behavior.
5. Extend load results with diagnostics, import duration, and per-file batch results.
6. Update docs, samples, consumer smoke, and contract tests to teach the registry path.

## Verification

- RED/GREEN tests for importer registry options, importer selection, no-match failures, OBJ adapter diagnostics, and batch per-file results.
- Targeted Avalonia and Core tests.
- Release solution build.
- Packaged consumer smoke compile using temporary local packages.

