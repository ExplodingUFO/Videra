# Phase 215 Plan

## Steps

1. Add optional backend capability-provider interfaces for resource factory and command executor advanced seams.
2. Project capability provider values into `RenderCapabilitySnapshot`.
3. Mirror advanced capability flags into `VideraBackendDiagnostics` and diagnostics formatting.
4. Implement explicit flags in software and native backend factories/executors.
5. Update docs, ExtensibilitySample, Demo diagnostics output, and focused tests.

## Verification

- Run targeted Core integration tests for render capabilities and diagnostics projection.
- Run targeted Avalonia diagnostics formatter test.
- Run repository/sample contract tests.
- Run platform smoke tests for native advanced seam capability flags.
- Build the full solution and run `git diff --check`.

