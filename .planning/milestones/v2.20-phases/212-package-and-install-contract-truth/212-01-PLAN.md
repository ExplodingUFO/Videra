# 212-01 Plan: Package and Install Contract Truth

## Goal

Make package install docs agree with current package references and lock that contract with a repository test.

## Tasks

1. Add a failing repository test for import README transitive-dependency drift, root install-by-scenario guidance, and Avalonia/import dependency consistency.
2. Update root README with scenario install guidance.
3. Correct `Videra.Import.Gltf` and `Videra.Import.Obj` README text.
4. Correct stale architecture wording about import package composition.
5. Run focused repository tests and record verification.

## Success Criteria

- `PackageDocsContractTests` fails before docs are fixed.
- `PackageDocsContractTests` passes after docs are fixed.
- `git diff --check` reports no whitespace errors.
- Phase branch is committed and merged cleanly.
