# Phase 216 Plan

## Steps

1. Add a dedicated `RenderingStatus` diagnostics panel to `Videra.SurfaceCharts.Demo`.
2. Add a single formatting path for surface and scatter rendering diagnostics.
3. Reuse the diagnostics formatter in the copyable support summary.
4. Update sample tests to verify visible diagnostics, support summary content, and boundary constraints.
5. Update the demo README to describe active-path diagnostics and the non-exhaustive GPU host/fallback boundary.

## Verification

- Run targeted SurfaceCharts demo configuration, behavior, and repository tests.
- Run the SurfaceCharts demo build.
- Run existing SurfaceCharts fallback integration tests.
- Build the full solution.
- Run `git diff --check`.
