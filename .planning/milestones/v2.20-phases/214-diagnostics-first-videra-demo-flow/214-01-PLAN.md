# Phase 214 Plan

## Steps

1. Add a small Demo-local support report builder for diagnostics bundle, import report, and minimal reproduction metadata.
2. Persist the last backend diagnostics, render capabilities, and load result in `MainWindowViewModel`.
3. Add a compact support panel to `MainWindow` with copy actions and the latest import report.
4. Surface asset metrics through per-file batch load results so failed atomic replacement still reports successfully imported file metrics.
5. Update Demo documentation and focused repository tests.

## Verification

- Build `samples/Videra.Demo`.
- Run focused Core sample/demo contract tests.
- Run affected Avalonia import-service tests.
- Build the solution.
- Run `git diff --check`.

