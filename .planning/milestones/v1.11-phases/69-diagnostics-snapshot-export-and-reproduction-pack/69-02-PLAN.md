# Phase 69 Plan 02

Goal: make the diagnostics snapshot usable from the happy path.

Tasks:
- Expose copy/export behavior in `Videra.MinimalSample`.
- Emit the same snapshot from `Videra.ConsumerSmoke`.
- Make the consumer smoke script verify the snapshot artifact exists.
