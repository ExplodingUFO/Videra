# Phase 69 Plan 01

Goal: provide a public diagnostics snapshot formatter.

Tasks:
- Add a reusable formatter over `VideraBackendDiagnostics`.
- Include package/version, platform, backend resolution, fallback, display server, and scene upload telemetry.
- Cover the formatter with dedicated unit tests.
