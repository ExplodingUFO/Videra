# Phase 68 Plan 01

Goal: align root and Avalonia docs on one canonical alpha consumer story.

Tasks:
- Collapse public onboarding language onto `Options -> LoadModelAsync(...) -> FrameAll()/ResetCamera() -> BackendDiagnostics`.
- Move compatibility and advanced entrypoints out of the default path.
- Add repository guards so the canonical wording stays aligned.
