# Phase 71 Plan 02

Goal: align release validation with the canonical consumer path.

Tasks:
- Make publish workflow prove the same package-based smoke path used in docs.
- Keep release docs tied to consumer smoke and benchmark evidence.
- Ensure release readiness tests guard that alignment.
