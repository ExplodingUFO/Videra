# Requirements

Active milestone: `v2.2 Scatter Productization` (complete locally)

Chart requirement numbering continues from `CHRT-11`.

- [x] `CHRT-12` Before implementation, lock scatter productization to the existing `ScatterChartView` family boundary and freeze explicit non-goals.
- [x] `CHRT-13` Add line-capable presentation under `ScatterChartView` without creating a separate top-level `PointLine` family or generic `Chart3D`.
- [x] `CHRT-14` Prove one repo-owned scatter path with truthful demo/support behavior on the existing Avalonia chart line.
- [x] `CHRT-15` Keep docs, support, package, release, and repository truth aligned on the same productized scatter story.

## Out of Scope

| Item | Excluded from `v2.2` |
|------|------------------------|
| Generic `Chart3D` | Not part of the milestone. |
| Separate top-level `PointLine` family | Not part of the milestone; connected lines stay under the existing scatter family boundary in `v2.2`. |
| `SurfaceMesh` | Not part of the milestone. |
| Fourth chart family | Not part of the milestone. |
| New UI adapters | Not part of the milestone. |
| `Wayland` / `OpenGL` work | Not part of the milestone. |
| Transparency / lighting / post-processing expansion | Not part of the milestone. |

## Traceability

| Requirement | Phase |
|-------------|-------|
| `CHRT-12` | Phase 140 |
| `CHRT-13` | Phase 141 |
| `CHRT-14` | Phase 142 |
| `CHRT-15` | Phase 143 |
