# Phase 172 Plan: Imported Asset Runtime Tightening Scope Lock

## Goal

Make the `v2.10` boundary explicit and narrow before implementation starts.

## Tasks

1. Freeze the milestone boundary.
   - Keep scope to imported-asset/runtime tightening only.
   - Exclude runtime/product breadth, chart/UI/backend expansion, compatibility shims, migration adapters, and fallback runtime paths.

2. Pin milestone vocabulary.
   - Use these terms consistently: imported asset truth, primitive-level participation, mixed opaque/transparent bridge, change kinds, upload queue coalescing, visibility/interaction-aware prioritization, primitive-centric runtime truth.

3. Define implementation split.
   - Phase 173: primitive-level upload and mixed transparency bridge.
   - Phase 174: scene delta and upload queue granularity.
   - Phase 175: primitive-centric runtime truth and guardrails.

4. Verify planning closure.
   - Confirm `IAT-01` is covered.
   - Update roadmap, requirements, and state.

## Acceptance

- `v2.10` is locked to imported-asset/runtime tightening.
- `IAT-01` is complete.
- No product-code changes are introduced in this phase.
