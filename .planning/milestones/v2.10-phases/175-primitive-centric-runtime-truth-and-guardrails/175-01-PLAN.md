# Phase 175 Plan: Primitive-Centric Runtime Truth and Guardrails

## Goal

Make repository truth match the shipped primitive-centric imported-runtime hot path without widening product scope.

## Tasks

1. Update primary docs and module READMEs.
   - Replace stale “mixed Blend/non-Blend imports remain guarded” wording where the runtime hot path now supports mixed participation internally.
   - Describe the current boundary precisely: the public single-object convenience seam remains narrow, but runtime entries may expand to multiple internal objects.

2. Update scene-delta and upload-queue wording.
   - Replace coarse reupload/FIFO descriptions with explicit retained-entry change kinds, queue coalescing, and attached-first interactive draining.
   - Keep backend rebind on the same queue path explicit.

3. Update repository guardrails.
   - Adjust repository tests that assert docs/readme truth so they enforce the new primitive-centric vocabulary.
   - Keep deferred renderer/import breadth explicitly out of scope.

4. Verify with focused repository tests.
   - Run repository readiness/architecture/localization tests affected by doc truth.
   - Run `git diff --check` in the isolated phase worktree.

## Acceptance

- Docs and repository guards describe the primitive-centric runtime boundary honestly.
- Mixed participation and queue vocabulary are explicit.
- The milestone is ready for audit and closeout after this phase merges.
