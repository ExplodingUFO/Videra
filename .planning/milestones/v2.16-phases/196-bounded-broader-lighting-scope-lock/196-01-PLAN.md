# Plan 196-01: Bounded Broader Lighting Scope Lock

## Success Criteria

1. `v2.16` is explicitly limited to one broader-lighting baseline on the existing style/uniform static-scene viewer/runtime line.
2. The milestone does not widen into shadows, environment maps, post-processing, emissive/normal-map renderer consumption, animation, or a new public package line.
3. The implementation phases stay narrow: one style/uniform contract phase, one repo-owned proof/validation phase, and one docs/guardrails phase.
4. Any desktop proof app used in the milestone validation keeps the window alive for `10` seconds without crashing.

## Assumptions

- Existing style/uniform lighting seams are still the intended extension point, not a new scene-lighting runtime model.
- The smallest honest proof surface remains `ConsumerSmoke` viewer mode plus `WpfSmoke`.
- Validation entrypoints may need explicit `LightingProofHoldSeconds=10` wiring when those proof hosts are used.

## Steps

1. Lock the milestone to style/uniform broader lighting only.
2. Keep the code phase focused on the minimum shader/uniform contract change across the current native backends.
3. Keep the proof phase focused on repository-owned viewer proof hosts plus explicit `10`-second no-crash validation wiring.
4. Keep the final phase focused on docs/support/repository truth only.

## Verification

- `git status --short --branch`
- manual review of milestone scope against current lighting seams and deferred-feature boundary
