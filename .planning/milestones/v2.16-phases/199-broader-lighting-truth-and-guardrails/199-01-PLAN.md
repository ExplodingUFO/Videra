# Phase 199 Plan

## Goal

Align docs, support wording, and repository guardrails with the shipped bounded broader-lighting baseline and its explicit repo-owned `10`-second desktop proof-host validation path.

## Scope

- Update English docs and package READMEs that still say `direct-lighting baseline`.
- Update Chinese mirrors that still reflect the old baseline wording.
- Update repository architecture/release/native-validation tests to lock the new truth.

## Tasks

1. Update public and repository docs
   - Files: `README.md`, `ARCHITECTURE.md`, `docs/capability-matrix.md`, `docs/support-matrix.md`, `docs/native-validation.md`, `docs/package-matrix.md`, `docs/hosting-boundary.md`, `src/Videra.Core/README.md`, `src/Videra.Avalonia/README.md`
   - Outcome: shipped boundary consistently describes the bounded broader-lighting baseline and repo-owned proof hosts.

2. Update Chinese mirrors
   - Files: `docs/zh-CN/README.md`, `docs/zh-CN/ARCHITECTURE.md`, `docs/zh-CN/index.md`, `docs/zh-CN/native-validation.md`, `docs/zh-CN/modules/videra-core.md`, `docs/zh-CN/modules/videra-avalonia.md`
   - Outcome: localized docs match the English truth closely enough for repository parity tests.

3. Update repository guardrails
   - Files: `tests/Videra.Core.Tests/Repository/RepositoryArchitectureTests.cs`, `tests/Videra.Core.Tests/Repository/RepositoryReleaseReadinessTests.cs`, `tests/Videra.Core.Tests/Repository/RepositoryNativeValidationTests.cs`
   - Optional: `tests/Videra.Core.Tests/Repository/RepositoryLocalizationTests.cs` if explicit localized guard coverage is needed.
   - Outcome: repository tests lock the broader-lighting + proof-host wording.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~RepositoryArchitectureTests|FullyQualifiedName~RepositoryReleaseReadinessTests|FullyQualifiedName~RepositoryNativeValidationTests|FullyQualifiedName~RepositoryLocalizationTests"`
- `git diff --check`

## Non-Goals

- Runtime, shader, backend, workflow, or packaging behavior changes
- Any new public lighting surface beyond the bounded broader-lighting baseline
- Any broad doc cleanup outside the strings needed for truth alignment
