---
phase: 09-render-pipeline-inventory-and-contract-extraction
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - ARCHITECTURE.md
  - src/Videra.Core/README.md
  - docs/zh-CN/modules/videra-core.md
  - tests/Videra.Core.Tests/Repository/RepositoryArchitectureTests.cs
  - tests/Videra.Core.Tests/Repository/RepositoryLocalizationTests.cs
autonomous: true
requirements: [PIPE-01, PIPE-02]

must_haves:
  truths:
    - "The documented pipeline contract uses the same stage vocabulary as the code and diagnostics."
    - "Docs describe the extracted pipeline contract without overclaiming that public extensibility is already shipped."
    - "Repository guards prevent stage-vocabulary and docs truth from silently drifting."
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Top-level architecture description of the render pipeline contract"
      contains: "PrepareFrame"
    - path: "src/Videra.Core/README.md"
      provides: "Consumer-facing explanation of current render-pipeline contract and diagnostics"
      contains: "RenderPipelineProfile"
    - path: "docs/zh-CN/modules/videra-core.md"
      provides: "Chinese mirror of the core pipeline truth"
      contains: "PrepareFrame"
---

<objective>
把 Phase 9 提炼出的 pipeline contract 写进 architecture/core docs，并用 repository-level tests 固定“当前只暴露 pipeline truth，不宣称 public custom-pass API 已经存在”。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/09-render-pipeline-inventory-and-contract-extraction/09-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/09-render-pipeline-inventory-and-contract-extraction/09-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/09-render-pipeline-inventory-and-contract-extraction/09-01-PLAN.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/09-render-pipeline-inventory-and-contract-extraction/09-02-PLAN.md
</execution_context>

<guardrails>
- 文档不能暗示 `IRenderPassContributor`、custom pass registration、frame hook API 已经在 Phase 9 落地。
- 中英文核心文档必须使用同一套 stage vocabulary。
- repository guards 要固定关键 stage 名称和 diagnostics 字段，而不是只检查“文档存在”。
</guardrails>

<tasks>

<task id="09-03-01" type="auto" tdd="true">
  <name>先补 repository guards，固定 pipeline vocabulary 和 docs truth</name>
  <files>
    tests/Videra.Core.Tests/Repository/RepositoryArchitectureTests.cs
    tests/Videra.Core.Tests/Repository/RepositoryLocalizationTests.cs
  </files>
  <read_first>
    tests/Videra.Core.Tests/Repository/RepositoryArchitectureTests.cs
    tests/Videra.Core.Tests/Repository/RepositoryLocalizationTests.cs
    ARCHITECTURE.md
    src/Videra.Core/README.md
    docs/zh-CN/modules/videra-core.md
  </read_first>
  <action>
    1. 扩展 `RepositoryArchitectureTests.cs`，要求 `ARCHITECTURE.md` 与 `src/Videra.Core/README.md` 都包含以下字符串中的关键子集：
       - `PrepareFrame`
       - `BindSharedFrameState`
       - `GridPass`
       - `SolidGeometryPass`
       - `WireframePass`
       - `AxisPass`
       - `PresentFrame`
       - `RenderPipelineProfile`
    2. 扩展 `RepositoryLocalizationTests.cs`，要求 `docs/zh-CN/modules/videra-core.md` 至少镜像 stage vocabulary 和 pipeline diagnostics truth。
    3. 增加负向 guard：文档不得声称 `custom render pass registration` 或 `frame hook API` 已在当前 phase shipped，可通过不存在 `RegisterPass(` / `FrameHook` / `IRenderPassContributor` 等公共承诺字符串来约束。
  </action>
  <acceptance_criteria>
    - `RepositoryArchitectureTests.cs` 包含对七个 stage 名称和 `RenderPipelineProfile` 的文档断言
    - `RepositoryLocalizationTests.cs` 包含中文镜像 guard
    - 新增 guards 在文档更新前失败
  </acceptance_criteria>
</task>

<task id="09-03-02" type="auto">
  <name>更新 architecture/core docs，统一描述当前 pipeline contract</name>
  <files>
    ARCHITECTURE.md
    src/Videra.Core/README.md
    docs/zh-CN/modules/videra-core.md
  </files>
  <read_first>
    ARCHITECTURE.md
    src/Videra.Core/README.md
    docs/zh-CN/modules/videra-core.md
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
  </read_first>
  <action>
    1. 在 `ARCHITECTURE.md` 中新增或改写 render-pipeline 说明，明确一帧的稳定 stage vocabulary 为：
       - `PrepareFrame`
       - `BindSharedFrameState`
       - `GridPass`
       - `SolidGeometryPass`
       - `WireframePass`
       - `AxisPass`
       - `PresentFrame`
    2. 在 `src/Videra.Core/README.md` 中补充当前 pipeline truth 与 diagnostics truth，明确：
       - 当前可以读取 `RenderPipelineProfile` 和 `LastFrameStageNames`
       - 当前 phase 只是显式化 contract，并未提供 public custom-pass registration
    3. 在 `docs/zh-CN/modules/videra-core.md` 中同步相同事实；如果表述简化，也必须保留 stage vocabulary 和 diagnostics truth。
  </action>
  <acceptance_criteria>
    - `ARCHITECTURE.md` 包含七个 stage 名称
    - `src/Videra.Core/README.md` 包含 `RenderPipelineProfile` 和 `LastFrameStageNames`
    - `docs/zh-CN/modules/videra-core.md` 包含 pipeline stage vocabulary 的中文镜像
    - repository guards 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~RepositoryArchitectureTests|FullyQualifiedName~RepositoryLocalizationTests"`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/verify.ps1 -Configuration Release`
</verification>

<success_criteria>
1. 文档、diagnostics 和代码使用同一套 pipeline vocabulary。
2. 仓库 guards 能阻止文档回退或过度承诺。
3. Phase 9 的成果为后续 public extensibility API 留出清晰边界。
</success_criteria>
