---
phase: 09-render-pipeline-inventory-and-contract-extraction
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/Videra.Avalonia/Rendering/RenderSession.cs
  - src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
  - src/Videra.Avalonia/Controls/VideraView.cs
  - tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
autonomous: true
requirements: [PIPE-01, PIPE-02]

must_haves:
  truths:
    - "The extracted pipeline contract is observable through session/view diagnostics, not only through internal engine tests."
    - "Phase 9 exposes pipeline summary data, but does not yet expose a public custom-pass registration API."
    - "Software presentation copy and active frame stages can be distinguished in diagnostics."
  artifacts:
    - path: "src/Videra.Avalonia/Rendering/RenderSession.cs"
      provides: "Session-level consumption of engine pipeline snapshot"
      contains: "LastPipelineSnapshot"
    - path: "src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs"
      provides: "Public diagnostics fields for pipeline truth"
      contains: "RenderPipelineProfile"
    - path: "src/Videra.Avalonia/Controls/VideraView.cs"
      provides: "Backend diagnostics refresh that includes pipeline summary"
      contains: "LastFrameStageNames"
---

<objective>
让 Phase 9 提炼出的 pipeline contract 能被 `RenderSession` 和 `VideraView` 的 diagnostics 消费，从而让 tests、host app status UI 和文档都能读到同一套 pipeline truth。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/09-render-pipeline-inventory-and-contract-extraction/09-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/09-render-pipeline-inventory-and-contract-extraction/09-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/09-render-pipeline-inventory-and-contract-extraction/09-01-PLAN.md
</execution_context>

<guardrails>
- 不在本计划中开放 custom pass registration 或 frame hook API。
- 不让 `RenderSession` 或 `VideraView` 接管 pipeline ownership；它们只消费 snapshot/summary。
- 不破坏现有 backend diagnostics truth。
</guardrails>

<tasks>

<task id="09-02-01" type="auto" tdd="true">
  <name>先补 session/view diagnostics 的 failing contract tests</name>
  <files>
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Rendering/RenderSession.cs
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  </read_first>
  <action>
    1. 扩展 `RenderSessionIntegrationTests.cs`，加入 `RenderSession_RenderOnce_CapturesLastPipelineSnapshot` 类测试，要求在 `RenderOnce()` 后 session 能暴露来自 engine 的 pipeline snapshot。
    2. 扩展 `VideraViewSceneIntegrationTests.cs`，加入 diagnostics tests，要求 `BackendDiagnostics` 至少包含：
       - `RenderPipelineProfile`
       - `LastFrameStageNames`
       - `UsesSoftwarePresentationCopy`
    3. 对 software backend 路径，测试必须断言 `UsesSoftwarePresentationCopy == true`；对 `LastFrameStageNames`，至少断言包含 `PrepareFrame` 与 `PresentFrame`。
    4. 新测试必须先失败，失败原因必须是 diagnostics 里还没有 pipeline truth。
  </action>
  <acceptance_criteria>
    - `RenderSessionIntegrationTests.cs` 包含 `LastPipelineSnapshot` 相关测试
    - `VideraViewSceneIntegrationTests.cs` 包含 `RenderPipelineProfile` / `LastFrameStageNames` / `UsesSoftwarePresentationCopy` 相关测试
    - 新测试在实现前稳定失败
  </acceptance_criteria>
</task>

<task id="09-02-02" type="auto" tdd="true">
  <name>让 RenderSession 和 VideraView 消费 pipeline snapshot，并统一 diagnostics truth</name>
  <files>
    src/Videra.Avalonia/Rendering/RenderSession.cs
    src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
    src/Videra.Avalonia/Controls/VideraView.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Avalonia/Rendering/RenderSession.cs
    src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
    src/Videra.Avalonia/Controls/VideraView.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  </read_first>
  <action>
    1. 在 `RenderSession.cs` 中新增 session-level snapshot surface，名称固定为 `LastPipelineSnapshot`，在每次成功 `RenderOnce()` 后从 `Engine.LastPipelineSnapshot` 同步。
    2. 在 `VideraBackendDiagnostics.cs` 中新增明确字段：
       - `string? RenderPipelineProfile`
       - `IReadOnlyList<string>? LastFrameStageNames`
       - `bool UsesSoftwarePresentationCopy`
    3. 在 `VideraView.CreateDiagnosticsSnapshot(...)` 中把以上字段填入 diagnostics：
       - `RenderPipelineProfile` 使用 engine/session snapshot 中的 profile 名称
       - `LastFrameStageNames` 使用 snapshot 的 stage name list
       - `UsesSoftwarePresentationCopy` 使用 `_renderSession.IsSoftwareBackend`
    4. 现有 backend readiness / fallback / display-server diagnostics 字段保持不变；新增 pipeline fields 不能覆盖或替换已有 truth。
    5. 不要在本计划中引入任何 `RegisterPass`、`AddRenderHook`、`IRenderPassContributor` 之类的 public extensibility API 名称；Phase 9 只允许读 pipeline summary，不允许写 pipeline structure。
  </action>
  <acceptance_criteria>
    - `RenderSession.cs` 包含 `LastPipelineSnapshot`
    - `VideraBackendDiagnostics.cs` 包含 `RenderPipelineProfile`, `LastFrameStageNames`, `UsesSoftwarePresentationCopy`
    - `VideraView.cs` 在 diagnostics snapshot 创建路径中填充这三个字段
    - `RenderSession` / `VideraView` integration tests 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~RenderSession|FullyQualifiedName~VideraViewScene"`
</verification>

<success_criteria>
1. pipeline contract 可以被 session/view diagnostics 读取。
2. diagnostics 同时保留 backend truth 和 pipeline truth。
3. 本计划不越界到完整 public extensibility API。
</success_criteria>
