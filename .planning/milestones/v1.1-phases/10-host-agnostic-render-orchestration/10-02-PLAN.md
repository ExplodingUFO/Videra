---
phase: 10-host-agnostic-render-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/Videra.Avalonia/Controls/VideraView.cs
  - src/Videra.Avalonia/Controls/VideraView.Input.cs
  - src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
  - src/Videra.Avalonia/Controls/VideraViewSessionBridge.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraViewSessionBridgeIntegrationTests.cs
autonomous: true
requirements: [PIPE-03, MAIN-01]

must_haves:
  truths:
    - "VideraView returns to being a UI shell and no longer manually coordinates low-level RenderSession lifecycle calls."
    - "A single bridge layer projects diagnostics from session/orchestrator truth into VideraBackendDiagnostics."
    - "Existing scene helpers, input behavior, and backend diagnostics remain intact after the extraction."
  artifacts:
    - path: "src/Videra.Avalonia/Controls/VideraViewSessionBridge.cs"
      provides: "Bridge from Avalonia view events/options to session orchestration"
      contains: "class VideraViewSessionBridge"
    - path: "src/Videra.Avalonia/Controls/VideraView.cs"
      provides: "Thinner view shell"
      contains: "VideraViewSessionBridge"
---

<objective>
把 `VideraView` 里剩余的 session attach/resize/handle/diagnostics orchestration 提炼成单一 bridge，让 view 回到 UI shell，同时保持当前 scene helper、input、backend diagnostics 和 native-host 行为不回退。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-01-PLAN.md
</execution_context>

<guardrails>
- 不在本计划中新增 public extensibility API。
- `VideraView` 仍保留 Avalonia UI tree、native host visual composition、pointer input、scene helper API。
- diagnostics truth 继续来自 session/orchestrator snapshot，不得重新散落成 ad hoc field assembly。
</guardrails>

<tasks>

<task id="10-02-01" type="auto" tdd="true">
  <name>先补 view-to-session bridge 的 failing integration tests</name>
  <files>
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSessionBridgeIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Avalonia/Controls/VideraView.Input.cs
    src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
    src/Videra.Avalonia/Rendering/RenderSession.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  </read_first>
  <action>
    1. 新增 `VideraViewSessionBridgeIntegrationTests.cs`，固定一个新的内部类型名 `VideraViewSessionBridge`。
    2. 新测试必须直接实例化 bridge，并覆盖这些行为：
       - backend options 变化会生成新的 diagnostics snapshot
       - native handle create / destroy 会同步 `NativeHostBound` 与 display-server diagnostics truth
       - size changes 会通过单一路径触发 session synchronization
       - session/orchestrator 的 pipeline snapshot 能投影到 `RenderPipelineProfile` 与 `LastFrameStageNames`
    3. 扩展 `VideraViewSceneIntegrationTests.cs`，确保现有 `LoadModelAsync`、`FrameAll`、`ResetCamera`、input-related behavior 仍可在 bridge 引入后工作。
    4. 新测试必须先失败，失败原因必须是 `VideraViewSessionBridge` 和单一路径 diagnostics projection 尚不存在。
  </action>
  <acceptance_criteria>
    - `VideraViewSessionBridgeIntegrationTests.cs` 存在并包含 bridge 直接实例化测试
    - `VideraViewSceneIntegrationTests.cs` 仍覆盖 scene helper 和 diagnostics truth
    - 新测试在实现前稳定失败
  </acceptance_criteria>
</task>

<task id="10-02-02" type="auto" tdd="true">
  <name>引入 VideraViewSessionBridge，并把 VideraView 收口为 UI shell</name>
  <files>
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Avalonia/Controls/VideraView.Input.cs
    src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
    src/Videra.Avalonia/Controls/VideraViewSessionBridge.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Avalonia/Controls/VideraView.Input.cs
    src/Videra.Avalonia/Controls/VideraBackendDiagnostics.cs
    src/Videra.Avalonia/Controls/VideraViewOptions.cs
    src/Videra.Avalonia/Rendering/RenderSession.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSessionBridgeIntegrationTests.cs
  </read_first>
  <action>
    1. 新增 `src/Videra.Avalonia/Controls/VideraViewSessionBridge.cs`，让它成为 `VideraView` 与 `RenderSession` 之间的唯一协调层，至少提供这些明确方法：
       - `OnViewAttached(...)`
       - `OnViewDetached()`
       - `OnSizeChanged(...)`
       - `OnBackendOptionsChanged(...)`
       - `OnNativeHandleCreated(...)`
       - `OnNativeHandleDestroyed()`
       - `CreateDiagnosticsSnapshot(...)`
    2. 把 `VideraView.cs` 中这些逻辑迁移到 bridge：
       - `CreateBackendOptionsSnapshot`
       - `ApplyRenderSessionSize`
       - `ReattachRenderSessionIfPossible`
       - diagnostics snapshot projection / refresh
       - native-handle create/destroy 时对 session 的低层同步
    3. `VideraView.cs` 保留这些职责：
       - Avalonia property surface
       - UI tree / native host creation and release
       - pointer input and camera helpers
       - scene helper API（如 `LoadModelAsync`, `FrameAll`, `ResetCamera`）
       但不再直接包含 `_renderSession.Attach(`、`_renderSession.BindHandle(`、`_renderSession.Resize(` 这些低层 orchestration 调用。
    4. `VideraBackendDiagnostics` 如需扩展，只能围绕 session/orchestrator snapshot truth；不得重新发明另一套平行状态。
    5. 若 `VideraView.Input.cs` 需要适配 bridge 引入后的 attach/detach 入口，更新它，但不要把 render orchestration 再拉回 input partial。
  </action>
  <acceptance_criteria>
    - `VideraViewSessionBridge.cs` 存在并包含 `OnViewAttached(` 与 `CreateDiagnosticsSnapshot(`
    - `VideraView.cs` 引用 `VideraViewSessionBridge`
    - `VideraView.cs` 不再包含 `_renderSession.Attach(`, `_renderSession.BindHandle(`, `_renderSession.Resize(`
    - `VideraViewSceneIntegrationTests` 和 `VideraViewSessionBridgeIntegrationTests` 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~VideraView|FullyQualifiedName~RenderSession"`
</verification>

<success_criteria>
1. `VideraView` 回到 UI shell / input / native-host surface 的边界。
2. session/orchestrator 同步通过单一 bridge 路径完成。
3. scene helper、diagnostics truth 和 input behavior 在解耦后仍然可回归验证。
</success_criteria>
