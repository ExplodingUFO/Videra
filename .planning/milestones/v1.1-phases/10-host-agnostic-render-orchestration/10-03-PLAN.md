---
phase: 10-host-agnostic-render-orchestration
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - ARCHITECTURE.md
  - docs/zh-CN/ARCHITECTURE.md
  - tests/Videra.Core.Tests/Repository/RepositoryArchitectureTests.cs
  - tests/Videra.Core.Tests/Repository/RepositoryLocalizationTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/RenderSessionOrchestrationIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraViewSessionBridgeIntegrationTests.cs
autonomous: true
requirements: [PIPE-03, MAIN-01]

must_haves:
  truths:
    - "The new render orchestration boundary is described in architecture docs and enforced by repository guards."
    - "Host-agnostic orchestration remains free of Avalonia UI types."
    - "Phase 10 still does not overclaim public extensibility APIs."
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Contributor-facing description of the new render/session/view boundary"
      contains: "RenderSessionOrchestrator"
    - path: "tests/Videra.Core.Tests/Repository/RepositoryArchitectureTests.cs"
      provides: "Guards against orchestration boundary drift"
      contains: "RenderSessionOrchestrator"
---

<objective>
用 docs、repository guards 和最终回归矩阵锁定新的 orchestration boundary，确保 `PIPE-03` 与 `MAIN-01` 不只是实现出来，而且不会很快漂移回旧的 coupling 形态。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-01-PLAN.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-02-PLAN.md
</execution_context>

<guardrails>
- 不在 docs 中声称 public custom pass / frame hook API 已 shipped。
- repository guards 要固定 boundary truth，而不是只检查“文件存在”。
- 中英文 architecture docs 使用同一套核心 boundary vocabulary。
</guardrails>

<tasks>

<task id="10-03-01" type="auto" tdd="true">
  <name>更新 architecture docs，并增加 boundary drift guards</name>
  <files>
    ARCHITECTURE.md
    docs/zh-CN/ARCHITECTURE.md
    tests/Videra.Core.Tests/Repository/RepositoryArchitectureTests.cs
    tests/Videra.Core.Tests/Repository/RepositoryLocalizationTests.cs
  </files>
  <read_first>
    ARCHITECTURE.md
    docs/zh-CN/ARCHITECTURE.md
    src/Videra.Avalonia/Rendering/RenderSessionOrchestrator.cs
    src/Videra.Avalonia/Controls/VideraViewSessionBridge.cs
    src/Videra.Avalonia/Controls/VideraView.cs
    tests/Videra.Core.Tests/Repository/RepositoryArchitectureTests.cs
    tests/Videra.Core.Tests/Repository/RepositoryLocalizationTests.cs
  </read_first>
  <action>
    1. 在 `ARCHITECTURE.md` 中明确写出新的 boundary：
       - `VideraEngine` owns frame-plan / pipeline execution
       - `RenderSessionOrchestrator` owns host-agnostic session orchestration
       - `RenderSession` hosts Avalonia-specific runtime/presentation adapters
       - `VideraViewSessionBridge` translates Avalonia view events/options into session synchronization
       - `VideraView` remains the UI shell and native-host/input surface
    2. 在 `docs/zh-CN/ARCHITECTURE.md` 中同步同一套 boundary truth，保留 `RenderSessionOrchestrator` 与 `VideraViewSessionBridge` 这些类型名。
    3. 扩展 `RepositoryArchitectureTests.cs`，要求：
       - `RenderSessionOrchestrator.cs`, `RenderSessionInputs.cs`, `RenderSessionSnapshot.cs`, `VideraViewSessionBridge.cs` 存在
       - `ARCHITECTURE.md` 包含 `RenderSessionOrchestrator` 与 `VideraViewSessionBridge`
       - `RenderSessionOrchestrator.cs` 不包含 `WriteableBitmap`, `DispatcherTimer`, `NativeControlHost`
       - `VideraView.cs` 包含 `VideraViewSessionBridge`，且不包含 `_renderSession.Attach(`、`_renderSession.BindHandle(`、`_renderSession.Resize(`
    4. 扩展 `RepositoryLocalizationTests.cs`，要求 `docs/zh-CN/ARCHITECTURE.md` 镜像 `RenderSessionOrchestrator` / `VideraViewSessionBridge` 这套 boundary truth。
    5. 文档中不得出现 `RegisterPass(`、`FrameHook`、`IRenderPassContributor` 已 shipped 的表述。
  </action>
  <acceptance_criteria>
    - `ARCHITECTURE.md` 包含 `RenderSessionOrchestrator` 与 `VideraViewSessionBridge`
    - `docs/zh-CN/ARCHITECTURE.md` 包含 `RenderSessionOrchestrator` 与 `VideraViewSessionBridge`
    - `RepositoryArchitectureTests.cs` 包含对 forbidden UI types 和 `VideraView` direct session calls 的 guard
    - `RepositoryLocalizationTests.cs` 包含中文镜像 guard
  </acceptance_criteria>
</task>

<task id="10-03-02" type="auto" tdd="true">
  <name>补最终 regression matrix，证明新的 orchestration seam 可回归验证</name>
  <files>
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionOrchestrationIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSessionBridgeIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Rendering/RenderSessionOrchestrator.cs
    src/Videra.Avalonia/Controls/VideraViewSessionBridge.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionOrchestrationIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSessionBridgeIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  </read_first>
  <action>
    1. 补齐 `RenderSessionOrchestrationIntegrationTests.cs` 的最终矩阵，至少覆盖：
       - software path ready without handle
       - native path waiting for handle
       - handle destroy / rebind recreation
       - dispose no-op
       - pipeline snapshot after render
    2. 补齐 `VideraViewSessionBridgeIntegrationTests.cs` 的最终矩阵，至少覆盖：
       - options change to diagnostics refresh
       - handle create/destroy to diagnostics truth
       - size change to session synchronization
       - pipeline snapshot projection into `RenderPipelineProfile` / `LastFrameStageNames`
    3. 保留并运行已有 `RenderSessionIntegrationTests` 与 `VideraViewSceneIntegrationTests`，确保新 seam 没有破坏历史回归面。
    4. 若测试文件过于拥挤，可拆出新的 dedicated test file，但最终矩阵必须覆盖上述四组场景。
  </action>
  <acceptance_criteria>
    - `RenderSessionOrchestrationIntegrationTests.cs` 覆盖五组 orchestration 场景
    - `VideraViewSessionBridgeIntegrationTests.cs` 覆盖四组 bridge/diagnostics 场景
    - targeted integration tests 通过
    - repository tests 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~RenderSession|FullyQualifiedName~VideraView"`
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~RepositoryArchitectureTests|FullyQualifiedName~RepositoryLocalizationTests"`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/verify.ps1 -Configuration Release`
</verification>

<success_criteria>
1. 新 orchestration boundary 被 docs、tests、repo guards 同时固定。
2. `PIPE-03` 与 `MAIN-01` 有清晰、可回归验证的证据链。
3. Phase 10 不越界到 public extensibility API。
</success_criteria>
