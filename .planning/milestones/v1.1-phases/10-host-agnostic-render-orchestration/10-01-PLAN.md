---
phase: 10-host-agnostic-render-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Videra.Avalonia/Rendering/RenderSession.cs
  - src/Videra.Avalonia/Rendering/RenderSessionInputs.cs
  - src/Videra.Avalonia/Rendering/RenderSessionSnapshot.cs
  - src/Videra.Avalonia/Rendering/RenderSessionOrchestrator.cs
  - tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/RenderSessionOrchestrationIntegrationTests.cs
autonomous: true
requirements: [PIPE-03, MAIN-01]

must_haves:
  truths:
    - "Pure render-session orchestration can be composed and tested without constructing VideraView or a native host."
    - "RenderSession retains Avalonia-specific presentation adapters, but no longer owns the full orchestration state machine."
    - "Dispose/no-op and pipeline snapshot contracts survive the extraction."
  artifacts:
    - path: "src/Videra.Avalonia/Rendering/RenderSessionOrchestrator.cs"
      provides: "Host-agnostic session orchestration core"
      contains: "class RenderSessionOrchestrator"
    - path: "src/Videra.Avalonia/Rendering/RenderSessionInputs.cs"
      provides: "Typed orchestration inputs"
      contains: "record"
    - path: "src/Videra.Avalonia/Rendering/RenderSessionSnapshot.cs"
      provides: "Typed orchestration snapshot"
      contains: "RenderSessionSnapshot"
---

<objective>
把 `RenderSession` 里的 host-agnostic orchestration 提炼成明确的内部协调层，让 backend lifecycle / state transition / frame render 可以在不依赖 `VideraView`、`NativeControlHost`、`WriteableBitmap`、`DispatcherTimer` 的条件下被组装和测试。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/09-render-pipeline-inventory-and-contract-extraction/09-SUMMARY.md
</execution_context>

<guardrails>
- 不引入任何 public custom-pass、frame-hook、capability-query API。
- 不把 frame-plan / pipeline ownership 从 `VideraEngine` 移走。
- `RenderSessionOrchestrator` 不得依赖 `WriteableBitmap`、`DispatcherTimer`、`NativeControlHost`。
</guardrails>

<tasks>

<task id="10-01-01" type="auto" tdd="true">
  <name>先补 headless orchestration 的 failing contract tests</name>
  <files>
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionOrchestrationIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Rendering/RenderSession.cs
    src/Videra.Avalonia/Rendering/RenderSessionHandle.cs
    src/Videra.Avalonia/Controls/VideraView.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
  </read_first>
  <action>
    1. 新增 `RenderSessionOrchestrationIntegrationTests.cs`，固定一个新的内部类型名 `RenderSessionOrchestrator`，并让测试直接实例化它，而不是通过 `VideraView` 或 Avalonia visual tree。
    2. 新测试必须覆盖这些状态与行为：
       - `WaitingForSize -> WaitingForHandle -> Ready`
       - `Software` backend 在无 native handle 时可进入 ready
       - ready 后 handle destroy / rebind 会触发 suspend + recreate
       - `Dispose()` 后再次 `Attach` / `BindHandle` / `Resize` / `RenderOnce` 保持 harmless `no-op`
       - `RenderOnce()` 后可读取最近一帧的 pipeline snapshot truth
    3. 对 native-handle 路径，测试只允许使用 fake backend、typed inputs 和 `IntPtr` handle；不得构造 `VideraView`、`NativeControlHost`、`WriteableBitmap`、`DispatcherTimer`。
    4. 扩展 `RenderSessionIntegrationTests.cs`，为现有 `RenderSession` 增加一条 contract：它现在应通过 `RenderSessionOrchestrator` 暴露 orchestration snapshot，而不是继续把全部状态藏在 façade 内。
    5. 新测试必须先失败，失败原因必须是 `RenderSessionOrchestrator` / typed snapshot seam 尚未存在。
  </action>
  <acceptance_criteria>
    - `RenderSessionOrchestrationIntegrationTests.cs` 存在并包含 `RenderSessionOrchestrator` 直接实例化测试
    - 新测试覆盖 `WaitingForSize`, `WaitingForHandle`, `Ready`, `Disposed` 至少四种状态
    - 实现前测试稳定失败，失败原因是缺少新 orchestration seam
  </acceptance_criteria>
</task>

<task id="10-01-02" type="auto" tdd="true">
  <name>提炼 RenderSessionOrchestrator 和 typed input/snapshot model，并把 RenderSession 收口为 runtime shell</name>
  <files>
    src/Videra.Avalonia/Rendering/RenderSession.cs
    src/Videra.Avalonia/Rendering/RenderSessionInputs.cs
    src/Videra.Avalonia/Rendering/RenderSessionSnapshot.cs
    src/Videra.Avalonia/Rendering/RenderSessionOrchestrator.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Rendering/RenderSession.cs
    src/Videra.Avalonia/Rendering/RenderSessionHandle.cs
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionOrchestrationIntegrationTests.cs
  </read_first>
  <action>
    1. 新增 `RenderSessionInputs.cs`，把 session orchestration 需要的输入显式建模为 typed object，至少包含：
       - `GraphicsBackendPreference RequestedBackend`
       - `VideraBackendOptions? BackendOptions`
       - `IntPtr Handle`
       - `bool HandleBound`
       - `uint Width`
       - `uint Height`
       - `float RenderScale`
    2. 新增 `RenderSessionSnapshot.cs`，把 orchestration 当前真相显式建模，至少包含：
       - session state
       - handle bound / generation
       - `LastBackendResolution`
       - `LastInitializationError`
       - display-server diagnostics
       - `LastPipelineSnapshot`
       - 是否使用 software presentation copy 的 intent
    3. 新增 `RenderSessionOrchestrator.cs`，让它拥有 backend reference、session state、typed inputs、resolution/error/display metadata、pipeline snapshot，以及统一的 `Attach` / `BindHandle` / `Resize` / `RenderOnce` / `Dispose` 协调入口。
    4. 重写 `RenderSession.cs`，让它保留这些 Avalonia-specific adapter 责任：
       - `IRenderLoopDriver`
       - `_bitmapFactory`
       - `WriteableBitmap` creation/copy
       - `_requestRender`
       其余 orchestration state（如 `_preference`, `_backendOptions`, `_width`, `_height`, `_state`, `LastBackendResolution`, `LastInitializationError`, `LastPipelineSnapshot`）迁移到 `RenderSessionOrchestrator`。
    5. `RenderSession.cs` 应通过 orchestrator snapshot 暴露现有 public/internal truth，并保留 `BackendReady` 事件与 current behavior。
    6. 保留 Phase 7 契约：`Dispose()` 之后所有公开入口保持 harmless `no-op`，不得重建 backend。
    7. `RenderSessionOrchestrator.cs` 中不得出现 `WriteableBitmap`、`DispatcherTimer`、`NativeControlHost` 这三个字符串。
  </action>
  <acceptance_criteria>
    - `RenderSessionInputs.cs`, `RenderSessionSnapshot.cs`, `RenderSessionOrchestrator.cs` 都存在
    - `RenderSession.cs` 包含 `RenderSessionOrchestrator` 引用
    - `RenderSessionOrchestrator.cs` 不包含 `WriteableBitmap`, `DispatcherTimer`, `NativeControlHost`
    - `RenderSessionIntegrationTests` 和 `RenderSessionOrchestrationIntegrationTests` 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~RenderSession"`
</verification>

<success_criteria>
1. 纯 render-session orchestration 可在无 view / 无 native host 条件下被组装和测试。
2. `RenderSession` 只保留 runtime shell / presentation adapter 职责。
3. Phase 7 与 Phase 9 的 lifecycle + pipeline truth 都未回退。
</success_criteria>
