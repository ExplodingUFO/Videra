---
phase: 12-developer-facing-samples-docs-and-compatibility-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - samples/Videra.ExtensibilitySample/Videra.ExtensibilitySample.csproj
  - samples/Videra.ExtensibilitySample/App.axaml
  - samples/Videra.ExtensibilitySample/App.axaml.cs
  - samples/Videra.ExtensibilitySample/Program.cs
  - samples/Videra.ExtensibilitySample/Assets/reference-cube.obj
  - samples/Videra.ExtensibilitySample/Extensibility/RecordingContributor.cs
  - samples/Videra.ExtensibilitySample/Views/MainWindow.axaml
  - samples/Videra.ExtensibilitySample/Views/MainWindow.axaml.cs
  - samples/Videra.ExtensibilitySample/README.md
  - Videra.slnx
autonomous: true
requirements:
  - MAIN-02
must_haves:
  truths:
    - "A library consumer can open one focused sample and see contributor registration, frame hook registration, and capability/diagnostics queries using only public APIs."
    - "The reference sample is Avalonia-first through `VideraView.Engine`, not a pointer back to `Videra.Demo` internals."
  artifacts:
    - path: "samples/Videra.ExtensibilitySample/Videra.ExtensibilitySample.csproj"
      provides: "Runnable narrow reference sample project"
      contains: "<TargetFramework>net8.0</TargetFramework>"
    - path: "samples/Videra.ExtensibilitySample/Views/MainWindow.axaml.cs"
      provides: "Public API registration flow for contributors, frame hooks, model load, and capability queries"
      contains: "RegisterPassContributor"
    - path: "samples/Videra.ExtensibilitySample/README.md"
      provides: "How-to-run reference usage notes"
      contains: "VideraView.Engine"
  key_links:
    - from: "samples/Videra.ExtensibilitySample/Views/MainWindow.axaml.cs"
      to: "src/Videra.Avalonia/Controls/VideraView.cs"
      via: "VideraView.Engine, VideraView.RenderCapabilities, and VideraView.BackendDiagnostics"
      pattern: "View3D\\.(Engine|RenderCapabilities|BackendDiagnostics)"
    - from: "Videra.slnx"
      to: "samples/Videra.ExtensibilitySample/Videra.ExtensibilitySample.csproj"
      via: "solution membership"
      pattern: "Videra\\.ExtensibilitySample"
---

<objective>
Build a dedicated, narrow developer-facing sample for the public render extensibility surface introduced in Phase 11.

Purpose: Close `MAIN-02` with a copyable reference flow that external consumers can follow without digging through `Videra.Demo` or internal session seams.
Output: A new `samples/Videra.ExtensibilitySample` project, bundled sample asset, sample README, and solution wiring.
</objective>

<execution_context>
@C:/Users/SuperDragon/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SuperDragon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-public-extensibility-apis/11-SUMMARY.md
@.planning/phases/11-public-extensibility-apis/11-VERIFICATION.md
@.planning/phases/12-developer-facing-samples-docs-and-compatibility-guards/12-RESEARCH.md
@samples/Videra.Demo/Videra.Demo.csproj
@samples/Videra.Demo/README.md
@src/Videra.Avalonia/README.md
@src/Videra.Avalonia/Controls/VideraView.cs
@src/Videra.Avalonia/Controls/VideraView.Scene.cs
@src/Videra.Avalonia/Controls/VideraView.Camera.cs
@src/Videra.Core/Graphics/VideraEngine.cs
@src/Videra.Core/Graphics/RenderPipeline/Extensibility/IRenderPassContributor.cs
@src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderPassContributionContext.cs
@src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookPoint.cs
@src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookContext.cs
@src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderCapabilitySnapshot.cs

<interfaces>
From `src/Videra.Core/Graphics/VideraEngine.cs`:
```csharp
public void RegisterPassContributor(RenderPassSlot slot, IRenderPassContributor contributor);
public void ReplacePassContributor(RenderPassSlot slot, IRenderPassContributor contributor);
public void RegisterFrameHook(RenderFrameHookPoint hookPoint, Action<RenderFrameHookContext> hook);
public RenderCapabilitySnapshot GetRenderCapabilities();
```

From `src/Videra.Avalonia/Controls/VideraView.cs` and partials:
```csharp
public VideraEngine Engine { get; }
public VideraBackendDiagnostics BackendDiagnostics { get; }
public RenderCapabilitySnapshot RenderCapabilities { get; }
public async Task<ModelLoadResult> LoadModelAsync(string path, CancellationToken cancellationToken = default);
public bool FrameAll();
```

From `src/Videra.Core/Graphics/RenderPipeline/Extensibility/IRenderPassContributor.cs`:
```csharp
public enum RenderPassSlot { Grid = 0, SolidGeometry, Wireframe, Axis }
public interface IRenderPassContributor
{
    void Contribute(RenderPassContributionContext context);
}
```

From `src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookPoint.cs`:
```csharp
public enum RenderFrameHookPoint
{
    FrameBegin = 0,
    SceneSubmit,
    FrameEnd
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the narrow extensibility sample project</name>
  <read_first>
    - samples/Videra.Demo/Videra.Demo.csproj
    - samples/Videra.Demo/README.md
    - src/Videra.Avalonia/README.md
    - src/Videra.Avalonia/Controls/VideraView.cs
    - src/Videra.Avalonia/Controls/VideraView.Scene.cs
    - src/Videra.Avalonia/Controls/VideraView.Camera.cs
    - src/Videra.Core/Graphics/VideraEngine.cs
    - src/Videra.Core/Graphics/RenderPipeline/Extensibility/IRenderPassContributor.cs
    - src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookContext.cs
  </read_first>
  <files>
    samples/Videra.ExtensibilitySample/Videra.ExtensibilitySample.csproj
    samples/Videra.ExtensibilitySample/App.axaml
    samples/Videra.ExtensibilitySample/App.axaml.cs
    samples/Videra.ExtensibilitySample/Program.cs
    samples/Videra.ExtensibilitySample/Assets/reference-cube.obj
    samples/Videra.ExtensibilitySample/Extensibility/RecordingContributor.cs
    samples/Videra.ExtensibilitySample/Views/MainWindow.axaml
    samples/Videra.ExtensibilitySample/Views/MainWindow.axaml.cs
  </files>
  <action>Create `samples/Videra.ExtensibilitySample` as a minimal Avalonia app patterned after `samples/Videra.Demo` for package/runtime setup but intentionally much narrower per `MAIN-02`. Use `VideraView` as the only 3D surface, one bundled `Assets/reference-cube.obj` as the only sample asset, and a small status panel that shows the contributor observation, frame-hook observation, `RenderCapabilities`, and `BackendDiagnostics` values after the sample loads. In `MainWindow.axaml.cs`, register exactly one `IRenderPassContributor` against `RenderPassSlot.SolidGeometry` and exactly one `FrameEnd` hook through `View3D.Engine`; the contributor should record slot/backend/pipeline metadata only and must not use internal types, demo helpers, or `SoftwareBackend`. Trigger the sample flow by calling `LoadModelAsync("Assets/reference-cube.obj")`, `FrameAll()`, then render the capability/diagnostic summary. Do not reference `RenderSession`, `RenderSessionOrchestrator`, `VideraViewSessionBridge`, `DemoMeshFactory`, or any `Videra.Core.Graphics.Software.*` type.</action>
  <verify>
    <automated>dotnet build samples/Videra.ExtensibilitySample/Videra.ExtensibilitySample.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "RegisterPassContributor|RegisterFrameHook|RenderCapabilities|BackendDiagnostics|LoadModelAsync|FrameAll\\(" samples/Videra.ExtensibilitySample` finds the public API flow in the sample.
    - `rg -n "RenderSession|RenderSessionOrchestrator|VideraViewSessionBridge|DemoMeshFactory|SoftwareBackend" samples/Videra.ExtensibilitySample` returns no matches.
    - `dotnet build samples/Videra.ExtensibilitySample/Videra.ExtensibilitySample.csproj -c Release` succeeds.
  </acceptance_criteria>
  <done>The repository contains a buildable `samples/Videra.ExtensibilitySample` project that demonstrates contributor registration, frame-hook registration, public model loading, framing, and runtime capability/diagnostic queries without relying on demo-only or internal seams.</done>
</task>

<task type="auto">
  <name>Task 2: Add sample README and solution wiring</name>
  <read_first>
    - Videra.slnx
    - samples/Videra.Demo/README.md
    - src/Videra.Core/README.md
    - src/Videra.Avalonia/README.md
    - .planning/phases/12-developer-facing-samples-docs-and-compatibility-guards/12-RESEARCH.md
  </read_first>
  <files>
    samples/Videra.ExtensibilitySample/README.md
    Videra.slnx
  </files>
  <action>Add `samples/Videra.ExtensibilitySample/README.md` that states this is the primary narrow reference for the public extension model per `MAIN-02`, lists the exact public APIs exercised (`VideraView.Engine`, `RegisterPassContributor(...)`, `RegisterFrameHook(...)`, `RenderCapabilities`, `BackendDiagnostics`, `LoadModelAsync`, `FrameAll()`), gives the exact run command, and explicitly says package discovery/plugin loading remain out of scope. Add the new sample project to `Videra.slnx` so it participates in normal repository builds. Keep the README sample-focused; do not duplicate the full demo manual.</action>
  <verify>
    <automated>dotnet sln Videra.slnx list</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet sln Videra.slnx list` includes `samples/Videra.ExtensibilitySample/Videra.ExtensibilitySample.csproj`.
    - `rg -n "VideraView.Engine|RegisterPassContributor|RegisterFrameHook|RenderCapabilities|BackendDiagnostics|LoadModelAsync|FrameAll\\(|package discovery|plugin loading" samples/Videra.ExtensibilitySample/README.md` finds the expected contract text.
  </acceptance_criteria>
  <done>The new sample is discoverable as a repository project and ships its own README that explains exactly which public extension APIs it demonstrates and what remains out of scope.</done>
</task>

</tasks>

<verification>
- `dotnet build samples/Videra.ExtensibilitySample/Videra.ExtensibilitySample.csproj -c Release`
- `dotnet sln Videra.slnx list`
</verification>

<success_criteria>
- A new `samples/Videra.ExtensibilitySample` project exists and builds.
- The sample demonstrates public contributor, hook, capability, and diagnostics usage through `VideraView.Engine`.
- The sample README makes it clear this is the narrow reference path for external developers.
</success_criteria>

<output>
After completion, create `.planning/phases/12-developer-facing-samples-docs-and-compatibility-guards/12-01-SUMMARY.md`
</output>
