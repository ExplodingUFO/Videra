---
phase: 11-public-extensibility-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Videra.Core/Graphics/VideraEngine.cs
  - src/Videra.Core/Graphics/VideraEngine.Rendering.cs
  - src/Videra.Core/Graphics/RenderPipeline/Extensibility/IRenderPassContributor.cs
  - src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderPassContributionContext.cs
  - src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookPoint.cs
  - src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookContext.cs
  - src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderCapabilitySnapshot.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraEngineExtensibilityIntegrationTests.cs
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03]

must_haves:
  truths:
    - "Public pass-contributor and frame-hook APIs are defined in Videra.Core, not in Avalonia/session adapters."
    - "VideraEngine owns contributor registration, replacement, and hook dispatch on top of stable contract vocabulary."
    - "Core-side runtime and capability truth is queryable through typed public API after a completed frame."
  artifacts:
    - path: "src/Videra.Core/Graphics/RenderPipeline/Extensibility/IRenderPassContributor.cs"
      provides: "Public custom pass contributor contract"
      contains: "interface IRenderPassContributor"
    - path: "src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookPoint.cs"
      provides: "Stable lifecycle hook vocabulary"
      contains: "enum RenderFrameHookPoint"
    - path: "tests/Videra.Core.IntegrationTests/Rendering/VideraEngineExtensibilityIntegrationTests.cs"
      provides: "Core public extensibility regression coverage"
      contains: "VideraEngineExtensibilityIntegrationTests"
---

<objective>
在 `Videra.Core` 中建立第一批 public extensibility contract，让 `VideraEngine` 成为 custom pass contributor、frame hook 和 Core-side runtime/capability query 的唯一 public owner。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/11-public-extensibility-apis/11-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/11-public-extensibility-apis/11-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/10-host-agnostic-render-orchestration/10-SUMMARY.md
</execution_context>

<guardrails>
- 不暴露 `RenderSessionSnapshot`、`RenderSessionOrchestrator`、`VideraViewSessionBridge`。
- 不把 `VideraEngine` 的 frame ownership 移给 contributor 或 hook API。
- hook vocabulary 与 pass vocabulary 分离；不要把所有 lifecycle concern 都塞进 `RenderPipelineStage`。
- capability query 只承诺 runtime/library truth，不实现完整 GPU feature discovery。
</guardrails>

<tasks>

<task id="11-01-01" type="auto" tdd="true">
  <name>先补 Core public extensibility 的 failing contract tests</name>
  <files>
    tests/Videra.Core.IntegrationTests/Rendering/VideraEngineExtensibilityIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    src/Videra.Core/Graphics/RenderPipeline/RenderFramePlan.cs
    src/Videra.Core/Graphics/RenderPipeline/RenderPipelineStage.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraEnginePipelineContractTests.cs
  </read_first>
  <action>
    1. 新增 `VideraEngineExtensibilityIntegrationTests.cs`，固定新的 public extensibility contract 至少包含三类能力：
       - register additional contributor
       - replace built-in contributor slot
       - register frame hook at stable hook points
    2. 新测试必须覆盖这些行为：
       - 自定义 contributor 能通过 public API 被注册，并在一帧渲染中执行
       - 内建 pass slot 可以通过 public API 被替换，替换后会改变可观测渲染/执行结果
       - frame hooks 在 `FrameBegin`、`SceneSubmit`、`FrameEnd` 等稳定 hook point 上按确定顺序执行
       - 一帧结束后，public query API 可以读到 pipeline/runtime/capability truth
    3. 测试应直接使用 public Core API，不依赖 `VideraView`、`RenderSession`、internal snapshot 或反射。
    4. 新测试必须先失败，失败原因必须是 public extensibility contract 尚不存在。
  </action>
  <acceptance_criteria>
    - `VideraEngineExtensibilityIntegrationTests.cs` 存在并直接使用 public Core API
    - 新测试至少覆盖 contributor register、slot replace、frame hook、runtime query 四组场景
    - 实现前测试稳定失败，失败原因是缺少 public extensibility contract
  </acceptance_criteria>
</task>

<task id="11-01-02" type="auto" tdd="true">
  <name>新增 public contributor/hook/query contract，并让 VideraEngine 拥有注册与执行语义</name>
  <files>
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    src/Videra.Core/Graphics/RenderPipeline/Extensibility/IRenderPassContributor.cs
    src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderPassContributionContext.cs
    src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookPoint.cs
    src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderFrameHookContext.cs
    src/Videra.Core/Graphics/RenderPipeline/Extensibility/RenderCapabilitySnapshot.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    src/Videra.Core/Graphics/RenderPipeline/RenderFramePlan.cs
    src/Videra.Core/Graphics/RenderPipeline/RenderPipelineSnapshot.cs
    src/Videra.Core/Graphics/Abstractions/ICommandExecutor.cs
    src/Videra.Core/Graphics/Abstractions/IResourceFactory.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraEngineExtensibilityIntegrationTests.cs
  </read_first>
  <action>
    1. 在 `src/Videra.Core/Graphics/RenderPipeline/Extensibility/` 下新增 public contract types，至少包括：
       - `IRenderPassContributor`
       - pass contribution context
       - `RenderFrameHookPoint`
       - frame hook context
       - capability/runtime query snapshot
    2. typed context 只能暴露稳定 Core abstractions 和 read-only truth，例如：
       - `ICommandExecutor`
       - `IResourceFactory`
       - camera / viewport / render scale
       - current frame plan / pipeline snapshot
       - read-only scene/style state
       不得依赖 Avalonia 类型或 session 内部对象。
    3. 为 `VideraEngine` 增加 public register/replace/query surface，使 host app 可以：
       - 注册附加 contributor
       - 替换稳定 slot 的默认 contributor
       - 注册 frame lifecycle hooks
       - 查询当前 runtime/capability truth
    4. 调整 `VideraEngine.Rendering.cs`，让 pass execution 不再完全硬编码，而是通过新的 contributor/hook dispatch 路径执行，同时保持 Phase 9 的 stage vocabulary 和 pipeline snapshot truth。
    5. 保持现有默认渲染行为不回退；如果没有任何自定义 contributor/hook，默认渲染结果必须与 Phase 10 基线一致。
  </action>
  <acceptance_criteria>
    - `IRenderPassContributor.cs`, `RenderPassContributionContext.cs`, `RenderFrameHookPoint.cs`, `RenderFrameHookContext.cs`, `RenderCapabilitySnapshot.cs` 都存在
    - `VideraEngine` 具备 public register/replace/query surface
    - `VideraEngine.Rendering.cs` 通过新的 extensibility path 运行，同时保持原有 pipeline truth
    - `VideraEngineExtensibilityIntegrationTests` 和既有 pipeline contract tests 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~VideraEngineExtensibility|FullyQualifiedName~VideraEnginePipelineContract"`
</verification>

<success_criteria>
1. `VideraEngine` 成为 custom contributor、frame hook 和 Core-side runtime query 的稳定 public owner。
2. contributor/hook contract 建立在 typed context 和稳定 vocabulary 上，而不是 internal runtime object。
3. 默认渲染行为与 Phase 10 基线一致。
</success_criteria>
