# Phase 180 Plan: SurfaceCharts Efficiency Scope Lock

## Goal

Freeze `v2.12` as a narrow `SurfaceCharts` efficiency milestone before implementation begins.

## Tasks

1. Define the milestone boundary.
   - Keep the milestone inside `SurfaceChartView` residency/probe/invalidation efficiency work.
   - Reuse the existing chart-local scheduling, probe, render-host, and benchmark seams.
   - Avoid new chart families, generic `Chart3D`, chart-kernel widening, viewer/runtime breadth, and platform/backend scope expansion.

2. Pin canonical vocabulary.
   - residency churn
   - prefetch prioritization
   - probe latency
   - interaction-path churn
   - invalidation/allocation pressure

3. Define implementation phases.
   - Phase 181: tile residency prioritization and prefetch tightening.
   - Phase 182: probe latency and interaction-stability tightening.
   - Phase 183: SurfaceCharts efficiency benchmarks and guardrails.

4. Update local planning.
   - Rewrite the active roadmap and requirements for `v2.12`.
   - Mark `SCE-01` as covered by this planning-only phase.
   - Reset `STATE.md` so execution resumes from Phase 181 after the scope lock is complete.

## Acceptance

- `v2.12` has a bounded, falsifiable scope.
- Explicit non-goals block chart-family widening, generic `Chart3D`, runtime/platform expansion, and compatibility work.
- The next implementation phase can start from clear requirements and clean phase boundaries.

