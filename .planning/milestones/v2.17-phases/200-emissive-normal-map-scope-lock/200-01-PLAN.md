# Phase 200 Plan

## Goal

Lock `v2.17` to one bounded emissive/normal-map renderer-consumption slice on the existing static-scene viewer/runtime line.

## Tasks

1. Freeze milestone boundary
   - Record the smallest honest next step after `v2.16`: renderer consumption of already-retained emissive and normal-map-ready material truth.
   - Reject broader lighting, shadow, environment, post-process, and material-system expansion.

2. Define the implementation phases
   - `201` renderer contract
   - `202` repo-owned proof and `10`-second validation
   - `203` docs/support/repository guardrails

3. Update milestone planning state
   - Mark `ENM-01` complete
   - Move active state to `Phase 201`

## Verification

- `git status --short --branch`
- manual roadmap/requirements review against the intended advanced-runtime route

## Non-Goals

- Any runtime, shader, backend, workflow, or package behavior change in this phase
- Any widening beyond the bounded emissive/normal-map renderer-consumption slice
