# Phase 202 Plan

## Goal

Demonstrate the new emissive/normal-map renderer-consumption slice on the existing repository-owned desktop proof hosts, while preserving explicit `10`-second survival validation.

## Tasks

1. Add one bounded proof scene/evidence path
   - Use the current viewer proof hosts only.
   - Make emissive and normal-map consumption observable on the shipped static-scene path.

2. Wire validation entrypoints if needed
   - Reuse the existing `LightingProofHoldSeconds=10` seam.
   - Keep every desktop program used in this phase alive for `10` seconds without crashing.

3. Add focused proof tests
   - Lock the proof host configuration and evidence shape.
   - Avoid docs/support/repository wording changes in this phase.

## Verification

- Focused repository/sample tests for the proof host configuration
- Actual desktop proof runs with explicit `10`-second hold on every validation app used
- `git diff --check`

## Non-Goals

- Docs/guardrails updates (Phase 203)
- New proof hosts or public sample surfaces
- Any broader renderer/material-system expansion
