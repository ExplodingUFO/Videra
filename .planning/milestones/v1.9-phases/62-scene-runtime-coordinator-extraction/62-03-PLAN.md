# Phase 62 Plan 03

## Goal

Lock the coordinator extraction into repository/runtime truth.

## Steps

1. Add repository tests that require `SceneRuntimeCoordinator` to own the extracted scene services.
2. Keep runtime/integration suites green against the thinner `VideraViewRuntime`.
3. Reduce the amount of scene-specific logic and field ownership left in `VideraViewRuntime.Scene.cs`.
