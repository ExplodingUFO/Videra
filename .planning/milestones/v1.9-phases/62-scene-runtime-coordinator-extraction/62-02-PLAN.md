# Phase 62 Plan 02

## Goal

Delegate runtime-scene operations through the coordinator so `VideraViewRuntime.Scene.cs` becomes orchestration-only again.

## Steps

1. Route load/add/replace/clear/items-change operations through `_sceneCoordinator`.
2. Move scene publication and backend-ready rehydrate paths behind coordinator methods.
3. Keep frame-prelude wiring and diagnostics refresh compatible with the existing runtime shell.
