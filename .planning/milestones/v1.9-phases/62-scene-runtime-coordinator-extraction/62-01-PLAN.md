# Phase 62 Plan 01

## Goal

Create a dedicated internal scene coordinator instead of letting `VideraViewRuntime` hold the whole scene state cluster.

## Steps

1. Introduce `SceneRuntimeCoordinator` and move scene-store, delta, residency, queue, import, and epoch ownership into it.
2. Keep the coordinator internal and avoid any public API changes.
3. Preserve enough compatibility for current tests and diagnostics surfaces.
