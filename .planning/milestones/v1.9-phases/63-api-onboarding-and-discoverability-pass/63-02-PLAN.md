# Phase 63 Plan 02

## Goal

Align the public quick-start story across viewer docs.

## Steps

1. Tighten `src/Videra.Avalonia/README.md` around the canonical first-use flow.
2. Keep root README and `docs/extensibility.md` aligned on `LoadModelAsync(...)`, `LoadModelsAsync(...)`, `FrameAll()`, `BackendDiagnostics`, and `RenderCapabilities`.
3. Keep the scene pipeline lab narrative narrow and contract-focused.
