# Phase 60 Plan 03

## Goal

Project richer scene telemetry through the stable viewer diagnostics shell and narrow sample surface.

## Steps

1. Add the new scene-upload metrics to `VideraBackendDiagnostics`.
2. Map runtime scene diagnostics through `VideraViewSessionBridge.CreateDiagnosticsSnapshot(...)`.
3. Update the narrow demo diagnostics surface and focused integration tests so the richer telemetry is visible and locked as contract.
