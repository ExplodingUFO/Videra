# Phase 60 Plan 02

## Goal

Preserve the last meaningful upload telemetry through `RuntimeFramePrelude` and runtime diagnostics refresh.

## Steps

1. Retain the last non-empty upload result instead of overwriting diagnostics with zeroed values on no-op frames.
2. Retain the last resolved budget decision so queue diagnostics explain the real runtime budget choice.
3. Recompute scene diagnostics from queue state plus the retained last meaningful upload telemetry.
