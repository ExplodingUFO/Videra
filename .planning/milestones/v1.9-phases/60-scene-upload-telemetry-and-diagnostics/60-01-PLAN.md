# Phase 60 Plan 01

## Goal

Expose upload bytes, failures, latency, and resolved budget as explicit queue/prelude telemetry.

## Steps

1. Extend `SceneUploadQueue.Drain(...)` to return uploaded bytes, failure count, duration, and resolved budget alongside uploaded records.
2. Update `SceneResidencyDiagnostics` so queue pressure and last upload telemetry are stable runtime values, not transient local variables.
3. Keep the telemetry contract internal-first so later diagnostics and benchmarks can consume the same data.
