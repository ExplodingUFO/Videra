# Phase 61 Plan 02

## Goal

Make upload-budget semantics explicit contract through focused tests.

## Steps

1. Add tests for normal backlog drain behavior under queue/object/byte budgets.
2. Add a focused test for the oversize-single-object case so the existing semantics become intentional rather than incidental.
3. Keep heuristic budget selection internal while making the resolved decision observable.
