# Phase 61 Plan 01

## Goal

Create a dedicated viewer-scene benchmark harness instead of relying on surface-chart performance evidence.

## Steps

1. Add a new BenchmarkDotNet project dedicated to viewer-scene work.
2. Wire it into the solution and grant the project access to required internal scene/runtime seams.
3. Keep the benchmark harness narrow: import, residency apply, upload drain, and a representative load/rebind flow.
