# Phase 186 Plan: Explicit Importer-Backed Load Path

## Goal

Make the packaged Avalonia consumer path opt into importer-backed loading explicitly instead of depending on the old transitive install shape.

## Tasks

1. Tighten packaged consumer smoke install truth.
   - Add a direct `Videra.Import.Obj` package reference to `smoke/Videra.ConsumerSmoke`.
   - Keep the scope on the existing packaged viewer smoke path.

2. Tighten packaged consumer smoke runtime truth.
   - Configure `VideraViewOptions.ModelImporter = static path => ObjModelImporter.Import(path)` before `LoadModelAsync(...)`.
   - Avoid new helpers or fallback discovery logic.

3. Lock the packaged truth in one focused repository guard.
   - Assert that packaged consumer smoke references `Videra.Import.Obj`.
   - Assert that the code-behind wires `ObjModelImporter.Import(...)`.

4. Verify the bounded consumer path.
   - Run the focused repository test for consumer smoke truth.
   - Run packaged viewer consumer smoke through `Invoke-ConsumerSmoke.ps1`.
   - Run `git diff --check`.

## Acceptance

- Importer-backed loading has one explicit packaged install/use path on the shipped Avalonia line.
- Viewer-only usage stays separate from importer-enabled usage.
- Packaged consumer evidence proves the explicit importer path without widening runtime/chart/platform scope.
