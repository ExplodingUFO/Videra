# Phase 184 Plan: Package Install Boundary Scope Lock

## Goal

Freeze `v2.13` as a narrow package/install-boundary milestone before implementation begins.

## Tasks

1. Define the milestone boundary.
   - Keep the milestone inside `Videra.Avalonia` install-surface tightening plus explicit importer-enabled load-path truth.
   - Reuse the existing viewer/public API line instead of widening runtime/render/material/chart/platform scope.
   - Avoid broader importer coverage, release workflow changes, compatibility work, or transitional dual-default package behavior.

2. Pin canonical vocabulary.
   - viewer-only install path
   - importer-enabled install path
   - default dependency surface
   - explicit importer-backed loading
   - package-size/install-surface evidence

3. Define implementation phases.
   - Phase 185: Avalonia dependency surface slimming.
   - Phase 186: explicit importer-backed load path.
   - Phase 187: install truth, size evidence, and guardrails.

4. Update local planning.
   - Rewrite the active roadmap and requirements for `v2.13`.
   - Mark `PIS-01` as covered by this planning-only phase.
   - Reset `STATE.md` so execution resumes from Phase 185 after the scope lock is complete.

## Acceptance

- `v2.13` has a bounded, falsifiable scope.
- Explicit non-goals block runtime/chart/platform/import breadth and compatibility work.
- The next implementation phase can start from clear requirements and clean package/install boundaries.
