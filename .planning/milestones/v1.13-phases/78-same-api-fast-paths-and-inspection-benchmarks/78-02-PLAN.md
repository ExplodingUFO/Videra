# Phase 78 Plan 02

## Scope

- keep `ExportSnapshotAsync(...)` unchanged while adding a preferred live-readback path from the active render session when compatible
- transparently fall back to the existing software export path when the fast path is unavailable
- prove the preferred readback path works without weakening export determinism or failure behavior

## Verification

- `dotnet test tests/Videra.Avalonia.Tests/Videra.Avalonia.Tests.csproj -c Release --filter "FullyQualifiedName~VideraSnapshotExportServiceTests|FullyQualifiedName~RenderSessionRuntimeTests"`
