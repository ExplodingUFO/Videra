# Phase 78 Plan 03

## Scope

- add dedicated inspection benchmarks for hit testing, clipping, and snapshot export to the existing benchmark project
- keep the benchmark inputs deterministic and workflow-friendly
- capture trend evidence with a dry benchmark run instead of treating one local number as a gate

## Verification

- `dotnet run -c Release --project benchmarks/Videra.Viewer.Benchmarks/Videra.Viewer.Benchmarks.csproj -- --filter "*InspectionBenchmarks*" --job Dry --warmupCount 1 --iterationCount 1`
