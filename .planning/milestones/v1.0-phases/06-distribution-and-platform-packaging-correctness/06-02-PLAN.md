---
phase: 06-distribution-and-platform-packaging-correctness
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/publish-nuget.yml
  - verify.ps1
  - verify.sh
  - scripts/
  - tests/Videra.Core.Tests/Repository/RepositoryReleaseReadinessTests.cs
requirements: [BUILD-01, DOCS-03]
autonomous: true
---

<objective>
让 GitHub Packages 的发布与仓库验证真正证明“包语义正确”，而不是只证明“在 Windows 上打出了 5 个文件”。本计划负责 matching-host 证据、artifact 语义检查和 repository-level gate。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/06-distribution-and-platform-packaging-correctness/06-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/06-distribution-and-platform-packaging-correctness/06-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/ROADMAP.md
@F:/CodeProjects/DotnetCore/Videra/.planning/STATE.md
</execution_context>

<guardrails>
- 不把 Linux/macOS 原生 runtime gap 偷换成“只要能 pack 就算验证”。
- 不把 workflow 改成只剩文案检查；必须保留构建/pack 层面的证据。
- 不在本计划引入 NuGet.org 双发。
</guardrails>

<tasks>

<task id="06-02-01" type="auto">
  <name>把 CI 和 publish workflow 改成 matching-host 证据模型</name>
  <files>
    .github/workflows/ci.yml
    .github/workflows/publish-nuget.yml
    verify.ps1
    verify.sh
  </files>
  <read_first>
    .github/workflows/ci.yml
    .github/workflows/publish-nuget.yml
    verify.ps1
    verify.sh
    README.md
    docs/troubleshooting.md
  </read_first>
  <action>
    1. 更新 `ci.yml`，不再只保留单一 `windows-latest` 验证路径；至少为 Windows、Linux、macOS 建立能证明 build/pack 语义的宿主矩阵或分 job 验证。
    2. 更新 `publish-nuget.yml`，不要再在一个 Windows job 里 `dotnet pack` 全部五个项目后直接发布；改成：
       - 在匹配宿主上生成/验证对应平台产物；
       - 汇总 artifact 后统一执行发布；
       - 只有所有 required validation 通过后才 push GitHub Packages。
    3. 若 `verify.ps1` / `verify.sh` 仍作为 workflow 入口脚本，就把“native validation optional”与“packaging validation required”区分清楚，不要把两者混成同一开关。
    4. 整个 workflow proof path 必须明确覆盖五个已发布包：`Videra.Core`, `Videra.Avalonia`, `Videra.Platform.Windows`, `Videra.Platform.Linux`, `Videra.Platform.macOS`。
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` 不再只有单一 `runs-on: windows-latest` 验证作业
    - `.github/workflows/publish-nuget.yml` 不再在一个 Windows job 中直接打包全部五个项目并立即发布
    - workflow 结构中可以明确看出 Windows/Linux/macOS 对应的 build/pack evidence 来源
    - workflow proof path 明确覆盖五个已发布包，而不是只覆盖 `Videra.Core` 和 `Videra.Avalonia`
  </acceptance_criteria>
</task>

<task id="06-02-02" type="auto" tdd="true">
  <name>新增 package semantic validation，检查 nuspec 依赖与元数据正确性</name>
  <files>
    .github/workflows/publish-nuget.yml
    scripts/
    tests/Videra.Core.Tests/Repository/RepositoryReleaseReadinessTests.cs
  </files>
  <read_first>
    .github/workflows/publish-nuget.yml
    Directory.Build.props
    src/Videra.Avalonia/Videra.Avalonia.csproj
    src/Videra.Core/Videra.Core.csproj
    src/Videra.Platform.Windows/Videra.Platform.Windows.csproj
    src/Videra.Platform.Linux/Videra.Platform.Linux.csproj
    src/Videra.Platform.macOS/Videra.Platform.macOS.csproj
    tests/Videra.Core.Tests/Repository/RepositoryReleaseReadinessTests.cs
  </read_first>
  <action>
    1. 在 `scripts/` 下新增一个集中化 package validation 脚本（PowerShell 即可），输入为打包输出目录与期望版本。
    2. 该脚本至少检查：
       - 每个 `.nupkg` 都存在 `.nuspec`、`README.md`、license/仓库元数据；
       - `Videra.Avalonia` 的 dependency graph 不再受构建宿主 OS 锁定；
       - 平台包的 PackageId / version / description / tags 与当前分发语义一致；
       - release tag 版本与 `.nuspec` 版本一致。
    3. 让 `publish-nuget.yml` 调用该脚本，而不是只做数量/版本/README 的浅校验。
    4. semantic validation 的目标目录必须包含五个已发布包，不允许把平台包排除在验证之外。
    5. 扩展 `RepositoryReleaseReadinessTests.cs`，把新的 workflow 与 semantic validation 约束固定成仓库级回归测试。
  </action>
  <acceptance_criteria>
    - 仓库中存在集中化 package semantic validation 脚本
    - `publish-nuget.yml` 调用该脚本而不是只做数量/README 检查
    - semantic validation 明确覆盖 `Videra.Core`, `Videra.Avalonia`, `Videra.Platform.Windows`, `Videra.Platform.Linux`, `Videra.Platform.macOS`
    - `RepositoryReleaseReadinessTests.cs` 固定了 matching-host/workflow/semantic validation 新约束
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj --filter RepositoryReleaseReadinessTests`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/verify.ps1 -Configuration Release`
- `dotnet pack F:/CodeProjects/DotnetCore/Videra/src/Videra.Core/Videra.Core.csproj -c Release -o F:/CodeProjects/DotnetCore/Videra/.artifacts/phase-06-pack-check`
- `dotnet pack F:/CodeProjects/DotnetCore/Videra/src/Videra.Avalonia/Videra.Avalonia.csproj -c Release -o F:/CodeProjects/DotnetCore/Videra/.artifacts/phase-06-pack-check`
- `dotnet pack F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.Windows/Videra.Platform.Windows.csproj -c Release -o F:/CodeProjects/DotnetCore/Videra/.artifacts/phase-06-pack-check`
- `dotnet pack F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.Linux/Videra.Platform.Linux.csproj -c Release -o F:/CodeProjects/DotnetCore/Videra/.artifacts/phase-06-pack-check`
- `dotnet pack F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.macOS/Videra.Platform.macOS.csproj -c Release -o F:/CodeProjects/DotnetCore/Videra/.artifacts/phase-06-pack-check`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/scripts/Validate-Packages.ps1 -PackageRoot F:/CodeProjects/DotnetCore/Videra/.artifacts/phase-06-pack-check -ExpectedVersion 0.1.0-alpha.1`
</verification>

<success_criteria>
1. GitHub Actions 能提供按宿主划分的 build/pack 证据，而不是 Windows-only 自证。
2. 发布链路可以检查五个已发布包的包语义和依赖图，不再只是浅层 artifact 存在性检查。
3. Workflow 约束被 repository tests 固化，避免后续回退成单机打包模型。
</success_criteria>
