---
phase: 06-distribution-and-platform-packaging-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/Videra.Avalonia/Videra.Avalonia.csproj
  - src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs
  - src/Videra.Avalonia/Composition/
  - samples/Videra.Demo/Videra.Demo.csproj
  - tests/Videra.Core.Tests/Graphics/GraphicsBackendFactoryTests.cs
  - tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
requirements: [PLAT-03, DOC-02, DOC-03]
autonomous: true
---

<objective>
收掉 `Videra.Avalonia` 当前由构建宿主 OS 决定平台能力集合的打包设计，把它改成真实的 Avalonia 入口包，并把平台后端变成显式、可发现、可缺失且可报告的运行时能力模型。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/06-distribution-and-platform-packaging-correctness/06-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/06-distribution-and-platform-packaging-correctness/06-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/ROADMAP.md
@F:/CodeProjects/DotnetCore/Videra/.planning/STATE.md
</execution_context>

<guardrails>
- 不引入 NuGet.org 迁移。
- 不新增图形后端或平台功能。
- 不保留“`Videra.Avalonia` 单包天然拥有全部原生后端”的隐式承诺。
- `VIDERA_BACKEND` 只能表达后端偏好，不能继续承担“自动安装/自动绑定缺失平台包”的语义。
</guardrails>

<tasks>

<task id="06-01-00" type="auto">
  <name>标准化 Phase 6 的 requirement/traceability 口径</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/REQUIREMENTS.md
    .planning/phases/06-distribution-and-platform-packaging-correctness/06-CONTEXT.md
  </read_first>
  <action>
    1. 明确在 `ROADMAP.md` / `STATE.md` 中记录：Phase 6 实际承接的 requirement 语义是跨平台构建/发布正确性与用户文档收口，对应 repo 现有 requirement namespace 的 `PLAT-03`, `DOC-02`, `DOC-03`。
    2. 若保留 `BUILD-01` / `DOCS-*` 叙述，也要在 Phase 6 详情中写明其与 `PLAT-03` / `DOC-02` / `DOC-03` 的映射，避免后续 plan/verification 再次出现 requirement drift。
  </action>
  <acceptance_criteria>
    - `ROADMAP.md` 和 `STATE.md` 对 Phase 6 的 requirement 口径一致
    - Phase 6 不再存在 `BUILD-01` / `PLAT-03`、`DOCS-*` / `DOC-*` 混用但未解释的情况
  </acceptance_criteria>
</task>

<task id="06-01-01" type="auto" tdd="true">
  <name>把 Videra.Avalonia 从宿主 OS 条件依赖改成运行时可发现的后端入口</name>
  <files>
    src/Videra.Avalonia/Videra.Avalonia.csproj
    src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs
    src/Videra.Avalonia/Composition/
  </files>
  <read_first>
    src/Videra.Avalonia/Videra.Avalonia.csproj
    src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs
    src/Videra.Core/Graphics/GraphicsBackendFactory.cs
    src/Videra.Core/Graphics/Abstractions/IGraphicsBackendResolver.cs
    src/Videra.Platform.Windows/Videra.Platform.Windows.csproj
    src/Videra.Platform.Linux/Videra.Platform.Linux.csproj
    src/Videra.Platform.macOS/Videra.Platform.macOS.csproj
  </read_first>
  <action>
    1. 从 `src/Videra.Avalonia/Videra.Avalonia.csproj` 移除基于 `IsOSPlatform('Windows'|'Linux'|'OSX')` 的 `DefineConstants` 和条件 `ProjectReference`，不再让包依赖闭包由构建宿主 OS 决定。
    2. 在 `src/Videra.Avalonia/Composition/` 下新增一个明确的运行时后端注册/发现 helper（名称可定，但必须留在该目录），用于：
       - 识别当前运行平台；
       - 尝试解析对应平台后端类型或注册项；
       - 在平台包缺失时给出确定性“不可用”结果，而不是依赖编译时裁剪。
    3. 将 `AvaloniaGraphicsBackendResolver` 改造成使用该运行时注册/发现 helper，而不是 `#if VIDERA_*_BACKEND` 与平台项目直接类型引用。
    4. 保持 `GraphicsBackendFactory` 的现有 fallback 语义：native backend 不可用时仍能回退到 software backend，但日志/错误信息必须能区分“偏好某 native backend”和“对应平台包未安装或不可加载”。
  </action>
  <acceptance_criteria>
    - `src/Videra.Avalonia/Videra.Avalonia.csproj` 不再包含 `VIDERA_WINDOWS_BACKEND`, `VIDERA_LINUX_BACKEND`, `VIDERA_MACOS_BACKEND`
    - `src/Videra.Avalonia/Videra.Avalonia.csproj` 不再包含对 `Videra.Platform.Windows`, `Videra.Platform.Linux`, `Videra.Platform.macOS` 的宿主 OS 条件 `ProjectReference`
    - `src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs` 不再包含 `#if VIDERA_`
    - Avalonia-side 后端解析路径能够在平台包缺失时返回可记录、可测试的“backend unavailable”结果
  </acceptance_criteria>
</task>

<task id="06-01-02" type="auto" tdd="true">
  <name>让本地 demo/source build 显式表达平台后端依赖并补测试</name>
  <files>
    samples/Videra.Demo/Videra.Demo.csproj
    tests/Videra.Core.Tests/Graphics/GraphicsBackendFactoryTests.cs
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
  </files>
  <read_first>
    samples/Videra.Demo/Videra.Demo.csproj
    tests/Videra.Core.Tests/Graphics/GraphicsBackendFactoryTests.cs
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
    README.md
  </read_first>
  <action>
    1. 更新 `samples/Videra.Demo/Videra.Demo.csproj`，不要再让 demo 通过仅引用 `Videra.Avalonia` 来隐式获得原生后端；改成在 sample 组合根中显式声明当前 source-build 需要的匹配平台后端依赖。
    2. 扩展 `GraphicsBackendFactoryTests.cs`，覆盖：
       - native preference 仍可通过 resolver 入口正常处理；
       - 缺失 native backend 时行为是确定性 fallback / unavailable，而不是编译时静默裁剪；
       - `VIDERA_BACKEND` 仍然只改变 preference 解析。
    3. 扩展 `DemoConfigurationTests.cs`，固定 sample 的显式平台依赖/组合根策略，防止后续又回到“只引 `Videra.Avalonia` 就默认拥有全部后端”的误导模型。
  </action>
  <acceptance_criteria>
    - `samples/Videra.Demo/Videra.Demo.csproj` 不再仅依赖 `Videra.Avalonia` 作为 native backend 来源
    - `tests/Videra.Core.Tests/Graphics/GraphicsBackendFactoryTests.cs` 包含“native backend unavailable is deterministic”覆盖
    - `tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs` 固定 sample 的显式平台依赖策略
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build F:/CodeProjects/DotnetCore/Videra/Videra.slnx -c Debug`
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj --filter GraphicsBackendFactoryTests`
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj --filter DemoConfigurationTests`
- grep 校验：
  - `src/Videra.Avalonia/Videra.Avalonia.csproj` 不含 `VIDERA_WINDOWS_BACKEND`
  - `src/Videra.Avalonia/Videra.Avalonia.csproj` 不含 `ProjectReference Include=\"..\\Videra.Platform.Windows\\Videra.Platform.Windows.csproj\"`
  - `src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs` 不含 `#if VIDERA_`
</verification>

<success_criteria>
1. `Videra.Avalonia` 不再由构建宿主 OS 静态决定最终平台能力集合。
2. Sample/source build 明确表达平台后端依赖，不再靠入口包隐式带齐 native backend。
3. 缺失平台包时的行为可测试、可日志化、可文档化。
</success_criteria>
