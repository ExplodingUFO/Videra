---
phase: 05-架构解耦与原生边界硬化
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - src/Videra.Core/Graphics/GraphicsBackendFactory.cs
  - src/Videra.Core/Graphics/Abstractions/IGraphicsBackend.cs
  - src/Videra.Core/Graphics/Abstractions/IResourceFactory.cs
  - src/Videra.Core/Graphics/Abstractions/ICommandExecutor.cs
  - src/Videra.Core/Graphics/Abstractions/IGraphicsBackendResolver.cs
  - src/Videra.Core/Graphics/RenderBindingSlots.cs
  - src/Videra.Core/Graphics/PrimitiveCommandKind.cs
  - src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs
  - tests/Videra.Core.Tests/Graphics/GraphicsBackendFactoryTests.cs
requirements: [PERF-01, PERF-02, PLAT-03]
autonomous: true
---

<objective>
把 backend 发现与组合从 `Videra.Core` 的反射硬编码中抽离出来，同时收紧 render contract 中的 magic numbers，让后续 render-session 与 native hardening 建立在更稳定的组合边界之上。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/ROADMAP.md
@F:/CodeProjects/DotnetCore/Videra/.planning/STATE.md
</execution_context>

<guardrails>
- 这是 additive migration，不做 package 大搬家。
- 不改变现有 `GraphicsBackendPreference` 对外语义。
- 不在本计划中引入 Rust。
- 组合层变更完成后，`Videra.Core` 不再直接依赖 `Assembly.Load(` 查找平台 backend。
</guardrails>

<tasks>

<task id="05-01-00" type="auto">
  <name>标准化 Phase 5 的 requirement 映射与 traceability</name>
  <files>
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/05-架构解耦与原生边界硬化/05-CONTEXT.md
    .planning/phases/05-架构解耦与原生边界硬化/05-RESEARCH.md
  </read_first>
  <action>
    1. 统一 `.planning/PROJECT.md`、`.planning/ROADMAP.md`、`.planning/REQUIREMENTS.md`、`.planning/STATE.md` 中对 Phase 5 的 requirement 叙述，确保使用同一组 requirement IDs：`PERF-01`, `PERF-02`, `SEC-02`, `MACOS-01`, `LINUX-02`, `PLAT-03`。
    2. 明确记录：`SEC-01` 的 importer 路径验证已在更早阶段完成，Phase 5 不重复做 importer path validation。
    3. 在 traceability/phase summary 区域加入 Phase 5 的定位：结构解耦 + native boundary hardening + Rust decision record，而不是 broad Rust adoption。
  </action>
  <acceptance_criteria>
    - `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md` 对 Phase 5 使用同一组 requirement IDs
    - planning docs 明确写出 `SEC-01` 不再是 Phase 5 目标
    - Phase 5 的描述在 planning docs 中明确为“C# first hardening + no Rust by default”
  </acceptance_criteria>
</task>

<task id="05-01-01" type="auto" tdd="true">
  <name>引入显式 backend resolver seam 并移除 Core 内反射装配</name>
  <files>
    src/Videra.Core/Graphics/GraphicsBackendFactory.cs
    src/Videra.Core/Graphics/Abstractions/IGraphicsBackendResolver.cs
    src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs
    tests/Videra.Core.Tests/Graphics/GraphicsBackendFactoryTests.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/GraphicsBackendFactory.cs
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Avalonia/Videra.Avalonia.csproj
    src/Videra.Platform.Windows/Videra.Platform.Windows.csproj
    src/Videra.Platform.Linux/Videra.Platform.Linux.csproj
    src/Videra.Platform.macOS/Videra.Platform.macOS.csproj
    tests/Videra.Core.Tests/Graphics/GraphicsBackendFactoryTests.cs
  </read_first>
  <action>
    1. 在 `src/Videra.Core/Graphics/Abstractions/IGraphicsBackendResolver.cs` 新增 resolver 契约，包含一个明确方法：`IGraphicsBackend? CreateBackend(GraphicsBackendPreference preference, ILoggerFactory? loggerFactory = null)`。
    2. 在 `src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs` 实现当前平台 resolver，把现有 Windows/Linux/macOS backend 发现逻辑迁移到这里。
    3. 将 `src/Videra.Core/Graphics/GraphicsBackendFactory.cs` 改造成兼容 façade：
       - 保留 `CreateBackend(GraphicsBackendPreference, ILoggerFactory?)`
       - 新增显式配置入口，例如 `ConfigureResolver(IGraphicsBackendResolver resolver)`
       - 当 resolver 未配置时，只允许 `SoftwareBackend` 路径；不得继续 `Assembly.Load(...)`
    4. 更新测试，覆盖：
       - resolver 已配置时按 preference 返回 backend
       - resolver 缺失时 software preference 仍可工作
       - resolver 缺失时 native preference 行为是确定性 fallback，而不是 Core 内反射装配
  </action>
  <acceptance_criteria>
    - `src/Videra.Core/Graphics/GraphicsBackendFactory.cs` 不再包含 `Assembly.Load(`
    - `src/Videra.Core/Graphics/Abstractions/IGraphicsBackendResolver.cs` 存在并定义 `CreateBackend(` 契约
    - `src/Videra.Avalonia/Composition/AvaloniaGraphicsBackendResolver.cs` 存在并承载 Windows/Linux/macOS backend 解析逻辑
    - `tests/Videra.Core.Tests/Graphics/GraphicsBackendFactoryTests.cs` 增加 resolver seam 覆盖并可在不依赖平台装配的情况下运行
  </acceptance_criteria>
</task>

<task id="05-01-02" type="auto" tdd="true">
  <name>用 typed constants 收敛 render binding 和 primitive command 约定</name>
  <files>
    src/Videra.Core/Graphics/RenderBindingSlots.cs
    src/Videra.Core/Graphics/PrimitiveCommandKind.cs
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/GridRenderer.cs
    src/Videra.Core/Graphics/AxisRenderer.cs
    src/Videra.Core/Graphics/Wireframe/WireframeRenderer.cs
    src/Videra.Core/Graphics/Abstractions/ICommandExecutor.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/GridRenderer.cs
    src/Videra.Core/Graphics/AxisRenderer.cs
    src/Videra.Core/Graphics/Wireframe/WireframeRenderer.cs
    src/Videra.Core/Graphics/Abstractions/ICommandExecutor.cs
  </read_first>
  <action>
    1. 新增 `src/Videra.Core/Graphics/RenderBindingSlots.cs`，明确定义 `Vertex = 0`, `Camera = 1`, `World = 2`, `Style = 3` 等 slot 常量。
    2. 新增 `src/Videra.Core/Graphics/PrimitiveCommandKind.cs`，明确定义 `LineList = 1`, `PointList = 2`, `TriangleList = 3` 或等价 typed representation。
    3. 将 `VideraEngine`、`GridRenderer`、`AxisRenderer`、`WireframeRenderer` 中对 slot 与 primitive 的裸数字调用替换为 typed constants。
    4. 若 `ICommandExecutor` 需要保留 primitiveType 数字重载，至少在调用侧不再直接散落 magic number。
  </action>
  <acceptance_criteria>
    - `src/Videra.Core/Graphics/RenderBindingSlots.cs` 存在并定义 `Camera`, `World`, `Style`
    - `src/Videra.Core/Graphics/PrimitiveCommandKind.cs` 存在并定义 `LineList`, `PointList`, `TriangleList`
    - `src/Videra.Core/Graphics/VideraEngine.cs` 使用 `RenderBindingSlots.*` 和 `PrimitiveCommandKind.*`
    - `src/Videra.Core/Graphics/GridRenderer.cs`, `AxisRenderer.cs`, `WireframeRenderer.cs` 不再直接写死 primitive magic number
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build F:/CodeProjects/DotnetCore/Videra/Videra.slnx -c Debug`
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj --filter GraphicsBackendFactoryTests --no-restore`
- grep 校验：
  - `src/Videra.Core/Graphics/GraphicsBackendFactory.cs` 不含 `Assembly.Load(`
  - `src/Videra.Core/Graphics/VideraEngine.cs` 包含 `RenderBindingSlots.Camera`
</verification>

<success_criteria>
1. `Videra.Core` 不再直接承担平台 backend 反射发现职责。
2. render contract 的核心 binding/primitive 约定被类型化，后续计划可以在稳定符号上继续解耦。
</success_criteria>
