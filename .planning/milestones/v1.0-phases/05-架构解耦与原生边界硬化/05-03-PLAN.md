---
phase: 05-架构解耦与原生边界硬化
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/Videra.Platform.macOS/ObjCRuntime.cs
  - src/Videra.Platform.macOS/MetalBackend.cs
  - src/Videra.Platform.macOS/MetalResourceFactory.cs
  - src/Videra.Platform.macOS/MetalBuffer.cs
  - tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs
requirements: [MACOS-01, SEC-02]
autonomous: true
---

<objective>
在不重写 macOS backend 的前提下，对 ObjC/Metal 边界完成第一轮 C# 硬化：收紧对象所有权、空句柄/空指针检查、layer 生命周期与 runtime helper surface，降低最脆弱 native 路径的崩溃和泄漏风险。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.macOS/ObjCRuntime.cs
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.macOS/MetalBackend.cs
</execution_context>

<guardrails>
- 不把这一步扩成“Rust 替代 macOS backend”。
- 不新增 Metal feature scope。
- 任何运行时分配失败都必须先变成确定性 managed exception，再讨论更深层替换。
</guardrails>

<tasks>

<task id="05-03-01" type="auto" tdd="true">
  <name>加固 Metal buffer/library/pipeline 构建路径的 zero-handle 与 null-contents 防护</name>
  <files>
    src/Videra.Platform.macOS/MetalResourceFactory.cs
    src/Videra.Platform.macOS/MetalBuffer.cs
    src/Videra.Platform.macOS/ObjCRuntime.cs
  </files>
  <read_first>
    src/Videra.Platform.macOS/MetalResourceFactory.cs
    src/Videra.Platform.macOS/MetalBuffer.cs
    src/Videra.Platform.macOS/ObjCRuntime.cs
    src/Videra.Core/Exceptions/*.cs
  </read_first>
  <action>
    1. 在 `MetalResourceFactory.cs` 中，对所有 native constructor 返回的 `IntPtr` 做 zero-handle 检查，禁止在 `IntPtr.Zero` 情况下继续创建 `MetalBuffer` 或 pipeline/library wrapper。
    2. 在 `MetalBuffer.cs` 中，对 `contents` 返回值做空指针检查；若无效，抛出明确的 managed/domain exception，而不是继续写入。
    3. 在 `ObjCRuntime.cs` 中继续收敛常用 helper，避免业务文件重复写裸 `objc_msgSend` 变体。
  </action>
  <acceptance_criteria>
    - `src/Videra.Platform.macOS/MetalResourceFactory.cs` 在创建 `MetalBuffer` 前显式检查 `IntPtr.Zero`
    - `src/Videra.Platform.macOS/MetalBuffer.cs` 在写入 `contents` 前显式检查空指针
    - 失败路径抛出 managed/domain exception，而不是继续执行 native write
  </acceptance_criteria>
</task>

<task id="05-03-02" type="auto" tdd="true">
  <name>收紧 CAMetalLayer 与 depth/state 生命周期所有权</name>
  <files>
    src/Videra.Platform.macOS/MetalBackend.cs
    src/Videra.Platform.macOS/ObjCRuntime.cs
    tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs
  </files>
  <read_first>
    src/Videra.Platform.macOS/MetalBackend.cs
    src/Videra.Platform.macOS/ObjCRuntime.cs
    tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs
  </read_first>
  <action>
    1. 明确 `_metalLayer` ownership：在 `Dispose` 时正确 release 或 detach，防止 layer 泄漏。
    2. 审查 `CreateDepthStencilState()` 路径中的 selector/descriptor 用法，把与 pipeline descriptor 相关的 pixel-format 设置放回正确位置，避免把错误 selector 发送到 depth-stencil descriptor。
    3. 在 macOS lifecycle tests 中新增或补强失败路径/重复 dispose/rebind safety 的可执行断言。
  </action>
  <acceptance_criteria>
    - `src/Videra.Platform.macOS/MetalBackend.cs` 的 `Dispose` 路径显式处理 `_metalLayer`
    - `src/Videra.Platform.macOS/MetalBackend.cs` 不再把 `setDepthAttachmentPixelFormat:` 发送给 depth-stencil descriptor
    - `tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs` 包含 dispose/reinit/rebind 相关断言
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build F:/CodeProjects/DotnetCore/Videra/Videra.slnx -c Debug`
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Platform.macOS.Tests/Videra.Platform.macOS.Tests.csproj --no-restore`
- grep 校验：
  - `src/Videra.Platform.macOS/MetalBackend.cs` 含 `_metalLayer`
  - `src/Videra.Platform.macOS/MetalBuffer.cs` 含 `IntPtr.Zero` 或等价 contents guard
</verification>

<success_criteria>
1. macOS backend 的最高风险 native 构建路径具备确定性失败行为，不再依赖裸空指针写入。
2. layer/depth state 生命周期的 ownership 更明确，为后续更高层绑定替代提供更安全的中间态。
</success_criteria>

