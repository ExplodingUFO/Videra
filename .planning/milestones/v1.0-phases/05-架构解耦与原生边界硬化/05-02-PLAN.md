---
phase: 05-架构解耦与原生边界硬化
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/Videra.Avalonia/Controls/VideraView.cs
  - src/Videra.Avalonia/Controls/IVideraNativeHost.cs
  - src/Videra.Avalonia/Controls/INativeHostFactory.cs
  - src/Videra.Avalonia/Controls/DefaultNativeHostFactory.cs
  - src/Videra.Avalonia/Rendering/RenderSession.cs
  - src/Videra.Avalonia/Rendering/RenderSessionHandle.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
requirements: [PERF-01, PERF-02, PLAT-03]
autonomous: true
---

<objective>
把 `VideraView` 从全能型 runtime owner 收敛为 UI shell/input surface，引入 render session 与 native host factory seam，使 backend lifecycle、timer、handle rebind 变成显式对象职责。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-01-PLAN.md
</execution_context>

<guardrails>
- 不改变 `VideraView` 的对外属性面和基本使用方式。
- 不把 Avalonia UI concerns 移进 `VideraEngine`。
- handle destroy/create 必须以 rebind 语义处理，不能退回“只改字段”的弱模型。
</guardrails>

<tasks>

<task id="05-02-01" type="auto" tdd="true">
  <name>抽离 RenderSession，承接 backend/session/timer 生命周期</name>
  <files>
    src/Videra.Avalonia/Rendering/RenderSession.cs
    src/Videra.Avalonia/Rendering/RenderSessionHandle.cs
    src/Videra.Avalonia/Controls/VideraView.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/GraphicsBackendFactory.cs
  </read_first>
  <action>
    1. 新增 `src/Videra.Avalonia/Rendering/RenderSession.cs`，把 backend 初始化、resize、render loop timer、software frame copy、teardown 封装进去。
    2. 新增 `src/Videra.Avalonia/Rendering/RenderSessionHandle.cs`，明确表达当前绑定的 native handle/generation。
    3. `RenderSession` 至少提供这些明确方法：`Attach(...)`, `BindHandle(IntPtr handle)`, `Resize(uint width, uint height, float renderScale)`, `RenderOnce()`, `Dispose()`。
    4. 重写 `VideraView.cs`，让它持有 `RenderSession`，并把原来的 `InitializeGraphicsDevice`, `StartRenderLoop`, `StopRenderLoop`, `RenderFrame` 逻辑迁移出去。
  </action>
  <acceptance_criteria>
    - `src/Videra.Avalonia/Rendering/RenderSession.cs` 存在并定义 `BindHandle(`, `Resize(`, `Dispose(`
    - `src/Videra.Avalonia/Controls/VideraView.cs` 包含 `RenderSession` 字段或属性
    - `src/Videra.Avalonia/Controls/VideraView.cs` 不再包含 `InitializeGraphicsDevice(` 方法
    - `src/Videra.Avalonia/Controls/VideraView.cs` 不再包含 `StartRenderLoop(` 和 `StopRenderLoop(` 方法
  </acceptance_criteria>
</task>

<task id="05-02-02" type="auto" tdd="true">
  <name>引入 NativeHostFactory 并显式处理 handle 重建语义</name>
  <files>
    src/Videra.Avalonia/Controls/INativeHostFactory.cs
    src/Videra.Avalonia/Controls/DefaultNativeHostFactory.cs
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Avalonia/Controls/VideraNativeHost.cs
    src/Videra.Avalonia/Controls/VideraLinuxNativeHost.cs
    src/Videra.Avalonia/Controls/VideraMacOSNativeHost.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Controls/IVideraNativeHost.cs
    src/Videra.Avalonia/Controls/VideraNativeHost.cs
    src/Videra.Avalonia/Controls/VideraLinuxNativeHost.cs
    src/Videra.Avalonia/Controls/VideraMacOSNativeHost.cs
    src/Videra.Avalonia/Controls/VideraView.cs
  </read_first>
  <action>
    1. 新增 `INativeHostFactory` 与 `DefaultNativeHostFactory`，返回 `IVideraNativeHost`，由 factory 统一选择 Windows/Linux/macOS host。
    2. `VideraView` 不再在 `EnsureNativeHost()` 中直接写 `OperatingSystem.IsWindows()/IsLinux()/IsMacOS()` 分支来 new host，而是调用 factory。
    3. 在 `VideraView` + `RenderSession` 协作中，把 `HandleDestroyed` / `HandleCreated` 明确建模为 rebind：
       - destroy 后 session 不得继续认为旧 handle 可用
       - create 后 session 必须重绑，而不是仅更新字段后走 resize
    4. 必要时补一层 generation/version tracking，防止 stale handle 被继续渲染使用。
  </action>
  <acceptance_criteria>
    - `src/Videra.Avalonia/Controls/INativeHostFactory.cs` 存在
    - `src/Videra.Avalonia/Controls/DefaultNativeHostFactory.cs` 存在并返回 `IVideraNativeHost`
    - `src/Videra.Avalonia/Controls/VideraView.cs` 的 host 创建路径不再直接包含 `OperatingSystem.IsWindows()`, `OperatingSystem.IsLinux()`, `OperatingSystem.IsMacOS()` 分支 new host 逻辑
    - `src/Videra.Avalonia/Rendering/RenderSession.cs` 存在针对 handle destroy/create 的显式 rebind 路径
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build F:/CodeProjects/DotnetCore/Videra/Videra.slnx -c Debug`
- grep 校验：
  - `src/Videra.Avalonia/Controls/VideraView.cs` 不含 `InitializeGraphicsDevice(`
  - `src/Videra.Avalonia/Controls/VideraView.cs` 包含 `DefaultNativeHostFactory` 或 `INativeHostFactory`
</verification>

<success_criteria>
1. `VideraView` 不再独占 backend/session/timer/native-host 的全生命周期。
2. native handle 重建变成显式 rebind 语义，为后续 native hardening 提供稳定入口。
</success_criteria>

