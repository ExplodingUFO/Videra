---
phase: 05-架构解耦与原生边界硬化
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/Videra.Platform.Linux/VulkanBuffer.cs
  - src/Videra.Platform.Linux/VulkanResourceFactory.cs
  - src/Videra.Platform.Linux/VulkanBackend.cs
  - src/Videra.Platform.Linux/X11SurfaceCreator.cs
  - src/Videra.Avalonia/Controls/VideraLinuxNativeHost.cs
  - src/Videra.Platform.Windows/D3D11Backend.cs
  - tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs
  - tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs
requirements: [LINUX-02, SEC-02, PLAT-03]
autonomous: true
---

<objective>
对 Linux Vulkan/X11 路径做第一轮返回值与所有权硬化，并顺手补齐 Windows 低风险 error-handling/rollback 清理，让 native boundary 的失败行为更可诊断、更可回滚。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/05-架构解耦与原生边界硬化/05-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.Linux/VulkanBuffer.cs
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.Linux/X11SurfaceCreator.cs
</execution_context>

<guardrails>
- 不在本计划中实现 Wayland。
- Linux 重点是 result-checking 和 X11 ownership，不是换语言。
- Windows 只做低风险 cleanup，不扩成第二个大重构面。
</guardrails>

<tasks>

<task id="05-04-01" type="auto" tdd="true">
  <name>补齐 Vulkan 关键 native return value 检查并把失败路径转成确定性异常</name>
  <files>
    src/Videra.Platform.Linux/VulkanBuffer.cs
    src/Videra.Platform.Linux/VulkanResourceFactory.cs
    src/Videra.Platform.Linux/VulkanBackend.cs
  </files>
  <read_first>
    src/Videra.Platform.Linux/VulkanBuffer.cs
    src/Videra.Platform.Linux/VulkanResourceFactory.cs
    src/Videra.Platform.Linux/VulkanBackend.cs
    src/Videra.Core/Exceptions/*.cs
  </read_first>
  <action>
    1. 在 `VulkanBuffer.cs` 中对 `MapMemory` 结果做显式 `Result.Success` 检查，失败时不得继续使用 `mapped` 指针。
    2. 在 `VulkanResourceFactory.cs` 中对 `BindBufferMemory` 结果做显式检查；失败时不得返回看似有效的 buffer wrapper。
    3. 审查 `VulkanBackend.cs` 中 acquire/submit/present 等关键步骤，把不应静默吞掉的 `Result` 统一转换为明确异常或失败返回。
  </action>
  <acceptance_criteria>
    - `src/Videra.Platform.Linux/VulkanBuffer.cs` 在使用 mapped 指针前显式检查 `Result.Success`
    - `src/Videra.Platform.Linux/VulkanResourceFactory.cs` 显式检查 `BindBufferMemory` 结果
    - `src/Videra.Platform.Linux/VulkanBackend.cs` 对 acquire/submit/present 至少关键路径不再默认静默成功
  </acceptance_criteria>
</task>

<task id="05-04-02" type="auto" tdd="true">
  <name>收紧 X11 display/window ownership，并清理 Windows 低风险 HRESULT/rollback 漏点</name>
  <files>
    src/Videra.Platform.Linux/X11SurfaceCreator.cs
    src/Videra.Avalonia/Controls/VideraLinuxNativeHost.cs
    src/Videra.Platform.Windows/D3D11Backend.cs
    tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs
    tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs
  </files>
  <read_first>
    src/Videra.Platform.Linux/X11SurfaceCreator.cs
    src/Videra.Avalonia/Controls/VideraLinuxNativeHost.cs
    src/Videra.Platform.Windows/D3D11Backend.cs
    tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs
    tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs
  </read_first>
  <action>
    1. 为 Linux 路径引入明确的 X11 handle model，使 surface creation 不再只消费裸 window handle；至少要显式表达 `Display` 与 `Window` 的所有权来源。
    2. 让 `X11SurfaceCreator` 与 `VideraLinuxNativeHost` 对 display/window ownership 的约定一致，避免“surface creator 自己开 display、host 自己持 window” 的隐式分裂模型。
    3. 在 `D3D11Backend.cs` 中补齐低风险 cleanup：
       - `ResizeBuffers` 失败要显式检查 HRESULT
       - `CreateBackBufferRTV` 失败路径要保证 local COM temporary 被释放
  </action>
  <acceptance_criteria>
    - `src/Videra.Platform.Linux/X11SurfaceCreator.cs` 与 `src/Videra.Avalonia/Controls/VideraLinuxNativeHost.cs` 共享一致的 X11 handle/ownership 模型
    - `src/Videra.Platform.Windows/D3D11Backend.cs` 显式检查 `ResizeBuffers` 结果
    - `src/Videra.Platform.Windows/D3D11Backend.cs` 的 backbuffer RTV 创建失败路径包含明确 cleanup
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build F:/CodeProjects/DotnetCore/Videra/Videra.slnx -c Debug`
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Platform.Windows.Tests/Videra.Platform.Windows.Tests.csproj --filter D3D11BackendLifecycleTests --no-restore`
- grep 校验：
  - `src/Videra.Platform.Linux/VulkanBuffer.cs` 含 `Result.Success`
  - `src/Videra.Platform.Windows/D3D11Backend.cs` 含 `ResizeBuffers`
</verification>

<success_criteria>
1. Linux native 路径的关键 native return value 与 X11 ownership 不再是隐式假设。
2. Windows 路径的已知低风险 rollback/error-handling 漏点被补齐，但不引入额外大重构。
</success_criteria>

