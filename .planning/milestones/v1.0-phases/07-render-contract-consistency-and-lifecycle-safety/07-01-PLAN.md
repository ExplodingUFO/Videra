---
phase: 07-render-contract-consistency-and-lifecycle-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Videra.Core/Graphics/VideraEngine.cs
  - src/Videra.Core/Graphics/VideraEngine.Rendering.cs
  - src/Videra.Core/Graphics/VideraEngine.Resources.cs
  - src/Videra.Avalonia/Rendering/RenderSession.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
requirements: [RES-01, RES-02, PERF-01]
autonomous: true
---

<objective>
把 `VideraEngine` 与 `RenderSession` 收口成显式生命周期状态模型，并固定 `Dispose` 后的 harmless `no-op` 契约，先解决资源释放、tick、resize、handle rebind 的交错风险。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/ROADMAP.md
@F:/CodeProjects/DotnetCore/Videra/.planning/STATE.md
</execution_context>

<guardrails>
- 不在本计划里修改软件深度状态或 wireframe 绘制语义。
- 不在 `Dispose` 后抛出新的 public-facing 异常；优先 no-op。
- 不把 `RenderSession` 彻底改造成新的总协调器；维持当前 boundary，只显式化状态和 ownership。
</guardrails>

<tasks>

<task id="07-01-01" type="auto" tdd="true">
  <name>先把 lifecycle no-op 和 state-transition 合同钉成 failing integration tests</name>
  <files>
    tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
  </files>
  <read_first>
    tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Avalonia/Rendering/RenderSession.cs
  </read_first>
  <action>
    1. 为 `VideraEngine` 增加合同测试，明确：
       - `Dispose()` 可重复调用；
       - `Dispose()` 之后 `Draw`, `Resize`, `AddObject`, `RemoveObject`, `ClearObjects`, `Suspend` 都不会抛异常；
       - `Dispose()` 之后不会重新变成 initialized/active。
    2. 为 `RenderSession` 增加合同测试，明确：
       - `Dispose()` 可重复调用；
       - `Dispose()` 之后 `Attach`, `BindHandle`, `Resize`, `RenderOnce` 都不会抛异常；
       - `BindHandle(IntPtr.Zero)` 后的正常 rebind 在 dispose 前仍有效，但 dispose 后不会重新创建 backend。
    3. 如果现有测试文件过于拥挤，可在同目录新建 lifecycle contract test 文件，但必须让测试名清楚表达“disposed is no-op”或“session does not reactivate after dispose”。
  </action>
  <acceptance_criteria>
    - `RenderSessionIntegrationTests` 或同目录新测试文件包含 `Dispose` 后 `Attach` / `BindHandle` / `RenderOnce` no-op 覆盖
    - `VideraEngineIntegrationTests` 或同目录新测试文件包含 `Dispose` 后 `Draw` / `Resize` / scene mutation no-op 覆盖
    - 目标测试在实现前稳定失败，且失败原因是当前 lifecycle contract 不满足，而不是测试写错
  </acceptance_criteria>
</task>

<task id="07-01-02" type="auto" tdd="true">
  <name>给 VideraEngine 引入显式生命周期状态并收口 public entrypoints</name>
  <files>
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    src/Videra.Core/Graphics/VideraEngine.Resources.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    src/Videra.Core/Graphics/VideraEngine.Resources.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
  </read_first>
  <action>
    1. 在 `VideraEngine` 中引入 internal lifecycle state，至少覆盖 `Uninitialized`, `Active`, `Suspended`, `Disposed`。
    2. 统一 `Initialize`, `Suspend`, `Draw`, `Resize`, `Dispose`, `AddObject`, `RemoveObject`, `ClearObjects` 的入口守卫：
       - `Disposed` 后统一 no-op；
       - `Suspend` 只释放 graphics resources，不清空场景意图；
       - `Initialize` 对 `Disposed` 实例不复活。
    3. 让渲染与资源释放路径依赖显式状态，而不是只依赖 `_disposed` 和字段是否为 null。
  </action>
  <acceptance_criteria>
    - `VideraEngine.cs` / partial files 中存在明确的 lifecycle state 定义
    - `VideraEngine` 相关合同测试通过
    - `Draw()` 不再在 disposed/suspended edge case 上依赖隐式字段组合才能“碰巧安全”
  </acceptance_criteria>
</task>

<task id="07-01-03" type="auto" tdd="true">
  <name>给 RenderSession 引入显式 session state 并统一 tick/attach/rebind/dispose 契约</name>
  <files>
    src/Videra.Avalonia/Rendering/RenderSession.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Rendering/RenderSession.cs
    src/Videra.Avalonia/Controls/VideraView.cs
    tests/Videra.Core.IntegrationTests/Rendering/RenderSessionIntegrationTests.cs
    src/Videra.Core/Graphics/VideraEngine.cs
  </read_first>
  <action>
    1. 在 `RenderSession` 中引入 explicit state，至少覆盖 `Detached`, `WaitingForSize`, `WaitingForHandle`, `Ready`, `Faulted`, `Disposed`。
    2. 让 `Attach`, `BindHandle`, `Resize`, `RenderOnce`, `Suspend`, `Dispose`, `TryInitialize` 都先走统一状态转换逻辑，再决定是否创建 backend/bitmap/render-loop。
    3. 保证 `Dispose` 胜出：
       - render-loop 停止只发生一次；
       - disposed 后不再创建 backend；
       - disposed 后不再重启 render-loop；
       - disposed 后 handle rebind 和 resize 都是 no-op。
  </action>
  <acceptance_criteria>
    - `RenderSession.cs` 中存在明确的 session state 定义
    - `RenderSession` 合同测试通过
    - `Dispose` 后不会因为 `Attach` / `BindHandle` / `Resize` 再次创建 backend
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~VideraEngine|FullyQualifiedName~RenderSession"`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/verify.ps1 -Configuration Release`
</verification>

<success_criteria>
1. `VideraEngine` 与 `RenderSession` 都拥有显式 lifecycle state。
2. `Dispose` 后的公开入口统一采用 harmless `no-op`。
3. render-loop tick、handle rebind、resize 与 suspend/dispose 的交错不再依赖隐式字段状态。
</success_criteria>
