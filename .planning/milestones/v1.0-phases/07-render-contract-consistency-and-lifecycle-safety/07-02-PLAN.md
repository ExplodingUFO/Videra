---
phase: 07-render-contract-consistency-and-lifecycle-safety
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/Videra.Core/Graphics/Software/SoftwareBackend.cs
  - src/Videra.Core/Graphics/Software/SoftwareCommandExecutor.cs
  - src/Videra.Core/Graphics/Wireframe/WireframeRenderer.cs
  - tests/Videra.Core.Tests/Graphics/Software/SoftwareBackendTests.cs
  - tests/Videra.Core.Tests/Graphics/Software/SoftwareRasterizerTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/WireframeRendererIntegrationTests.cs
requirements: [DEPTH-01, RES-01, PERF-01]
autonomous: true
---

<objective>
让软件后端真正实现深度状态 API，并把 `Overlay` / `VisibleOnly` / `AllEdges` / `WireframeOnly` 四种线框模式收口成真实、可观测的深度语义。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-01-PLAN.md
</execution_context>

<guardrails>
- 不把软件后端 contract 修复退化成继续保留 `SetDepthState` / `ResetDepthState` no-op。
- 不只写 “does not throw” 测试；要补 framebuffer 或像素级断言。
- 不在本计划里重新定义 style preset 契约；本计划只负责 depth + wireframe pass 行为真实化。
</guardrails>

<tasks>

<task id="07-02-01" type="auto" tdd="true">
  <name>先写出软件深度状态与线框模式差异的 failing tests</name>
  <files>
    tests/Videra.Core.Tests/Graphics/Software/SoftwareBackendTests.cs
    tests/Videra.Core.Tests/Graphics/Software/SoftwareRasterizerTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/WireframeRendererIntegrationTests.cs
  </files>
  <read_first>
    tests/Videra.Core.Tests/Graphics/Software/SoftwareBackendTests.cs
    tests/Videra.Core.Tests/Graphics/Software/SoftwareRasterizerTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/WireframeRendererIntegrationTests.cs
    src/Videra.Core/Graphics/Software/SoftwareCommandExecutor.cs
    src/Videra.Core/Graphics/Wireframe/WireframeRenderer.cs
  </read_first>
  <action>
    1. 在软件后端测试中补齐对 `SetDepthState(false,false)`, `SetDepthState(true,false)`, `SetDepthState(true,true)`, `ResetDepthState()` 的行为断言，不再只测“不抛异常”。
    2. 扩展 `WireframeRendererIntegrationTests`，通过 framebuffer / copied frame 差异证明：
       - `Overlay` 不测深也不写深；
       - `VisibleOnly` 测深但不写深；
       - `AllEdges` 先绘隐藏线再绘可见线；
       - `WireframeOnly` 需要纯线框语义。
    3. 目标测试必须在实现前失败，且失败原因来自当前软件深度状态和线框语义尚未落地。
  </action>
  <acceptance_criteria>
    - 软件测试包含 `SetDepthState` / `ResetDepthState` 的真实断言
    - `WireframeRendererIntegrationTests` 不再只依赖 `NotThrow`，至少有一组 framebuffer-level 差异断言
    - 新测试在实现前稳定失败
  </acceptance_criteria>
</task>

<task id="07-02-02" type="auto" tdd="true">
  <name>给 SoftwareCommandExecutor 落地真实 depth-test/depth-write 状态</name>
  <files>
    src/Videra.Core/Graphics/Software/SoftwareCommandExecutor.cs
    src/Videra.Core/Graphics/Software/SoftwareBackend.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/Software/SoftwareCommandExecutor.cs
    src/Videra.Core/Graphics/Software/SoftwareBackend.cs
    src/Videra.Core/Graphics/Abstractions/ICommandExecutor.cs
    tests/Videra.Core.Tests/Graphics/Software/SoftwareRasterizerTests.cs
  </read_first>
  <action>
    1. 在 `SoftwareCommandExecutor` 中引入当前 depth-test / depth-write state，而不是注释式 no-op。
    2. 让 point/line/triangle draw path 都通过同一套 depth-state 决策执行：
       - no depth test + no depth write
       - depth test only
       - depth test + depth write
    3. `ResetDepthState()` 明确恢复 solid-pass 默认值：depth test on, depth write on。
  </action>
  <acceptance_criteria>
    - `SoftwareCommandExecutor.cs` 不再包含 “No-op for software renderer” 这类深度状态实现
    - 软件深度状态目标测试通过
    - `ResetDepthState()` 恢复默认 solid-pass 语义而不是空实现
  </acceptance_criteria>
</task>

<task id="07-02-03" type="auto" tdd="true">
  <name>让 WireframeRenderer 的四种模式严格映射到深度语义</name>
  <files>
    src/Videra.Core/Graphics/Wireframe/WireframeRenderer.cs
    tests/Videra.Core.IntegrationTests/Rendering/WireframeRendererIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/Wireframe/WireframeRenderer.cs
    src/Videra.Core/Graphics/Wireframe/WireframeMode.cs
    src/Videra.Core/Graphics/Software/SoftwareCommandExecutor.cs
    tests/Videra.Core.IntegrationTests/Rendering/WireframeRendererIntegrationTests.cs
  </read_first>
  <action>
    1. 明确 `WireframeRenderer` 的四种模式语义：
       - `Overlay` => test off, write off
       - `VisibleOnly` => test on, write off
       - `AllEdges` => hidden pass test off/write off, visible pass test on/write off
       - `WireframeOnly` => test on, write on
    2. 如果需要，可把当前模式分支拆成更清晰的 helper，避免后续再次出现“注释写了语义，代码靠隐式假设”。
    3. 让 integration tests 验证四种模式输出差异真实存在。
  </action>
  <acceptance_criteria>
    - `WireframeRenderer.cs` 中四种模式的深度状态与上面定义一致
    - 线框模式 integration tests 通过
    - `WireframeOnly` 不再只是 `ShouldRenderSolid() == false` 的静态断言，而是有真实输出语义
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~SoftwareRasterizerTests|FullyQualifiedName~SoftwareBackendTests"`
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~WireframeRendererIntegrationTests"`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/verify.ps1 -Configuration Release`
</verification>

<success_criteria>
1. 软件后端的深度状态 API 成为真实语义而不是 no-op。
2. 四种线框模式的深度行为都能被软件后端验证。
3. 线框模式差异由真实渲染输出证明，而不是只靠注释或 NotThrow 测试。
</success_criteria>
