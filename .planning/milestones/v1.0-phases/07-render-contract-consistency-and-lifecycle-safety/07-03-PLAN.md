---
phase: 07-render-contract-consistency-and-lifecycle-safety
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/Videra.Core/Graphics/VideraEngine.Rendering.cs
  - src/Videra.Core/Graphics/VideraEngine.Resources.cs
  - src/Videra.Avalonia/Controls/VideraView.cs
  - src/Videra.Core/Styles/Services/RenderStyleService.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Styles/StyleEventIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
requirements: [PERF-01, DEPTH-01, RES-02]
autonomous: true
---

<objective>
把 `RenderStylePreset.Wireframe`, `Material.WireframeMode`, `WireframeRenderer.Mode` 与实际 solid/wireframe pass 选择收口成一套真实合同，不再让 style uniform 和真实渲染行为分叉。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-RESEARCH.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-01-PLAN.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/07-render-contract-consistency-and-lifecycle-safety/07-02-PLAN.md
</execution_context>

<guardrails>
- 不保留“style 写了 wireframe uniform，但实际 rendering path 不消费”的灰区。
- 不让 `RenderStylePreset.Wireframe` 与 `WireframeMode` 继续成为两套互不相干的真相。
- 显式 wireframe override 必须有清晰 precedence，不靠调用顺序猜测。
</guardrails>

<tasks>

<task id="07-03-01" type="auto" tdd="true">
  <name>先写出 style preset 与 explicit wireframe override 的 failing contract tests</name>
  <files>
    tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Styles/StyleEventIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  </files>
  <read_first>
    tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Styles/StyleEventIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
    src/Videra.Core/Styles/Presets/RenderStylePresets.cs
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
  </read_first>
  <action>
    1. 增加合同测试，明确：
       - 仅应用 `RenderStylePreset.Wireframe` 且未设置显式 wireframe override 时，真实渲染行为进入 wireframe-only 语义；
       - 若显式 `WireframeMode != None`，显式模式优先；
       - 实际 solid-pass suppression 与 wireframe-pass activation 来自同一套 effective contract。
    2. 保持测试以行为为中心，不只验证 style service 的 event/uniform 变化。
  </action>
  <acceptance_criteria>
    - integration tests 覆盖 style preset 单独生效
    - integration tests 覆盖显式 wireframe override 优先级
    - 新测试在实现前稳定失败
  </acceptance_criteria>
</task>

<task id="07-03-02" type="auto" tdd="true">
  <name>在 engine/render path 中引入 effective wireframe contract</name>
  <files>
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    src/Videra.Core/Graphics/VideraEngine.Resources.cs
    src/Videra.Core/Styles/Services/RenderStyleService.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/VideraEngine.Rendering.cs
    src/Videra.Core/Graphics/VideraEngine.Resources.cs
    src/Videra.Core/Styles/Services/RenderStyleService.cs
    src/Videra.Core/Styles/Presets/RenderStylePresets.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraEngineIntegrationTests.cs
  </read_first>
  <action>
    1. 明确 effective wireframe contract：
       - 若显式 `Wireframe.Mode != None`，使用显式模式；
       - 否则如果 `StyleService.CurrentParameters.Material.WireframeMode == true`，则有效模式为 `WireframeOnly`；
       - 否则有效模式为 `None`。
    2. 让 solid-pass 决策与 wireframe-pass 激活都使用同一 effective mode，而不是一边看 `StyleService`、一边看 `Wireframe.Mode`。
    3. 若需要，可在 engine 内引入小型 helper/adapter，避免 contract 散落在多个条件分支中。
  </action>
  <acceptance_criteria>
    - `VideraEngine.Rendering.cs` 中存在单一的 effective wireframe decision 路径
    - `RenderStylePreset.Wireframe` 在无显式 override 时会改变真实 pass 选择
    - 显式 `Wireframe.Mode` 仍能覆盖 style-driven default
  </acceptance_criteria>
</task>

<task id="07-03-03" type="auto" tdd="true">
  <name>收口 Avalonia-facing API 合同，避免 view-level 设置与 engine-level 行为分叉</name>
  <files>
    src/Videra.Avalonia/Controls/VideraView.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Avalonia/Controls/VideraView.cs
    src/Videra.Core/Graphics/VideraEngine.cs
    src/Videra.Core/Styles/Services/RenderStyleService.cs
    tests/Videra.Core.IntegrationTests/Rendering/VideraViewSceneIntegrationTests.cs
  </read_first>
  <action>
    1. 让 `VideraView.RenderStyle` 与 `VideraView.WireframeMode` 的联动符合 phase contract：
       - `RenderStyle` 可以通过 style service 触发 style-driven wireframe default；
       - `WireframeMode` 仍然作为显式 override。
    2. 让 view-level tests 验证 UI-facing 设置不会再出现“属性改了，但 engine 实际行为仍按另一套真相运行”的问题。
  </action>
  <acceptance_criteria>
    - `VideraViewSceneIntegrationTests` 或同目录新测试文件覆盖 render-style 与 wireframe-mode precedence
    - `VideraView.cs` 中不再只把 `RenderStyle` 与 `WireframeMode` 各自单向写入而没有统一 contract
    - 风格/线框合同 tests 全部通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~VideraEngine|FullyQualifiedName~StyleEvent|FullyQualifiedName~VideraViewScene"`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/verify.ps1 -Configuration Release`
</verification>

<success_criteria>
1. `RenderStylePreset.Wireframe` 与实际 wireframe rendering 行为不再脱节。
2. 显式 wireframe override 有清晰 precedence。
3. Avalonia-facing API、engine render path 与 software backend 使用同一套 wireframe 真相。
</success_criteria>
