---
phase: 08-demo-completion-and-user-feedback-truthfulness
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
  - samples/Videra.Demo/Views/MainWindow.axaml
  - tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
  - tests/Videra.Core.Tests/Samples/DemoInteractionContractTests.cs
requirements: [DOC-02, ERROR-03]
autonomous: true
---

<objective>
把 Demo 的交互 capability 明确化，移除 residual sample-only 输入路径，让按钮可用性、命令可执行性和用户实际能做的事保持一致。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/08-demo-completion-and-user-feedback-truthfulness/08-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/08-demo-completion-and-user-feedback-truthfulness/08-01-PLAN.md
</execution_context>

<guardrails>
- 不重新设计整套 Demo UI；只修 capability truth 和残留样例路径。
- 不保留“按钮可点，命令里再被动拒绝”的弱合同。
- 不继续让 `Test Wireframe` 这类重复入口与正式 `WireframeMode` 控件并存。
</guardrails>

<tasks>

<task id="08-02-01" type="auto" tdd="true">
  <name>先写出交互 capability 与 residual sample path 的 failing tests</name>
  <files>
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
    tests/Videra.Core.Tests/Samples/DemoInteractionContractTests.cs
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    samples/Videra.Demo/Views/MainWindow.axaml
  </files>
  <read_first>
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    samples/Videra.Demo/Views/MainWindow.axaml
    F:/CodeProjects/DotnetCore/Videra/.planning/phases/08-demo-completion-and-user-feedback-truthfulness/08-CONTEXT.md
  </read_first>
  <action>
    1. 新建 `DemoInteractionContractTests.cs`，覆盖至少这些合同：
       - `ImportCommand` 只有在 backend ready 且 importer attached 时才可执行
       - viewport utility（`FrameAll`, `ResetCamera`）的可执行条件来自显式 capability，而不是只有一个 raw `IsBackendReady`
       - `WireframeMode` 仍是唯一正式线框入口，移除 `TestWireframe` 后现有 `IsWireframeEnabled` / line-color visibility 仍成立
    2. 在 `DemoConfigurationTests.cs` 中补文本守卫，明确：
       - `MainWindow.axaml` 不再包含 `Content="Test Wireframe"`
       - `Import Model` / `Frame All` / `Reset Camera` 不再直接绑定 `IsEnabled="{Binding IsBackendReady}"` 作为唯一 gating 真相
    3. 目标测试要在实现前失败，并指向当前 capability / residual path contract 的不足。
  </action>
  <acceptance_criteria>
    - `DemoInteractionContractTests.cs` 覆盖 import capability、viewport capability 和 wireframe entry truth
    - `DemoConfigurationTests.cs` 固定移除 `Test Wireframe`
    - 新测试在实现前稳定失败
  </acceptance_criteria>
</task>

<task id="08-02-02" type="auto" tdd="true">
  <name>让 Import/viewport actions 走显式 capability 或 command can-execute</name>
  <files>
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    samples/Videra.Demo/Views/MainWindow.axaml
    tests/Videra.Core.Tests/Samples/DemoInteractionContractTests.cs
  </files>
  <read_first>
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    samples/Videra.Demo/Views/MainWindow.axaml
    tests/Videra.Core.Tests/Samples/DemoInteractionContractTests.cs
  </read_first>
  <action>
    1. 给 `ImportCommand` 引入显式 can-execute 条件，例如 `CanImportModels`，具体定义应至少同时要求：
       - `IsBackendReady == true`
       - `_importer is not null`
    2. 保证 `IsBackendReady`、`AttachImporter(...)`、以及任何 capability 变化都会触发对应 command 的 `NotifyCanExecuteChanged()`。
    3. 对 `FrameAll` / `ResetCamera` 保持或提升显式 capability contract，避免 XAML 继续用 `IsEnabled="{Binding IsBackendReady}"` 把真实条件压扁成一个 bit。
    4. 在 `MainWindow.axaml` 中移除对这三个按钮的 raw `IsBackendReady` 直接 `IsEnabled` 绑定；改由 command can-execute 或显式 capability 属性驱动。
  </action>
  <acceptance_criteria>
    - `MainWindowViewModel.cs` 中 `ImportCommand` 有显式 can-execute 条件
    - `MainWindow.axaml` 不再把 Import/FrameAll/ResetCamera 的 gating 直接写成 raw `IsBackendReady`
    - capability 相关行为测试通过
  </acceptance_criteria>
</task>

<task id="08-02-03" type="auto" tdd="true">
  <name>清理 residual sample-only 输入路径，保留单一线框入口</name>
  <files>
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    samples/Videra.Demo/Views/MainWindow.axaml
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
    tests/Videra.Core.Tests/Samples/DemoInteractionContractTests.cs
  </files>
  <read_first>
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    samples/Videra.Demo/Views/MainWindow.axaml
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
    tests/Videra.Core.Tests/Samples/DemoInteractionContractTests.cs
  </read_first>
  <action>
    1. 删除 `TestWireframeCommand` 及其 XAML 按钮，让线框模式切换只保留 `WireframeMode` 下拉框这一个正式入口。
    2. 保留并验证 `IsWireframeEnabled` 与线框颜色选择器的显示逻辑，确保删除 sample shortcut 后功能没有回退。
    3. 如果移除按钮后有状态提示逻辑需要迁移，则把用户反馈绑定到 `WireframeMode` 本身，而不是保留隐藏的测试入口。
  </action>
  <acceptance_criteria>
    - `MainWindowViewModel.cs` 不再包含 `TestWireframe`
    - `MainWindow.axaml` 不再包含 `Content="Test Wireframe"`
    - 线框相关交互测试通过，颜色选择器仍按 `IsWireframeEnabled` 运作
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~DemoInteractionContractTests|FullyQualifiedName~DemoConfigurationTests"`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/verify.ps1 -Configuration Release`
</verification>

<success_criteria>
1. Demo 的按钮可用性来自显式 capability truth。
2. `Import Model` / `Frame All` / `Reset Camera` 不再依赖 XAML 对 `IsBackendReady` 的简化猜测。
3. `Test Wireframe` 等 residual sample-only 入口被移除。
</success_criteria>
