---
phase: 08-demo-completion-and-user-feedback-truthfulness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
  - samples/Videra.Demo/Services/DemoSceneBootstrapper.cs
  - samples/Videra.Demo/Views/MainWindow.axaml.cs
  - tests/Videra.Core.Tests/Videra.Core.Tests.csproj
  - tests/Videra.Core.Tests/Samples/DemoStatusContractTests.cs
  - tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
requirements: [ERROR-03, DOC-02]
autonomous: true
---

<objective>
把 Demo 的等待中 / backend ready / 默认场景失败 / 初始化失败四类用户可见状态收口成单一、可测试的 sample-level contract，不再让状态栏同时出现互相冲突的真相。
</objective>

<execution_context>
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/08-demo-completion-and-user-feedback-truthfulness/08-CONTEXT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/ROADMAP.md
@F:/CodeProjects/DotnetCore/Videra/.planning/STATE.md
</execution_context>

<guardrails>
- 不在本计划里扩展新的 demo 功能；只收口已有状态与错误反馈。
- 不把状态真相重新分散到 `DemoSceneBootstrapper`, `MainWindow.axaml.cs`, `MainWindowViewModel` 三处 ad hoc string write。
- 默认场景失败时允许 backend ready，但文案必须明确是“降级可用”而非“完全成功”。
</guardrails>

<tasks>

<task id="08-01-01" type="auto" tdd="true">
  <name>先补一层 sample 行为测试，证明当前状态 contract 不够真</name>
  <files>
    tests/Videra.Core.Tests/Videra.Core.Tests.csproj
    tests/Videra.Core.Tests/Samples/DemoStatusContractTests.cs
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
  </files>
  <read_first>
    tests/Videra.Core.Tests/Videra.Core.Tests.csproj
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    samples/Videra.Demo/Services/DemoSceneBootstrapper.cs
    samples/Videra.Demo/Views/MainWindow.axaml.cs
  </read_first>
  <action>
    1. 扩展 `tests/Videra.Core.Tests/Videra.Core.Tests.csproj`，按需直接引用 `samples/Videra.Demo/Videra.Demo.csproj`，让测试能实例化 sample ViewModel / helper。
    2. 新建 `tests/Videra.Core.Tests/Samples/DemoStatusContractTests.cs`，覆盖至少四条合同：
       - waiting for backend => `IsBackendReady == false` 且状态文案表达“等待中”
       - backend ready + default cube seeded => `IsBackendReady == true` 且状态文案表达“默认场景已加载”
       - backend ready + default scene seed failure => `IsBackendReady == true` 且状态文案表达“降级可用/导入仍可用/默认场景失败”，不能是假成功
       - backend initialization failure => `IsBackendReady == false` 且状态文案表达失败
    3. 如果需要保留少量源码守卫，可在 `DemoConfigurationTests.cs` 中补一条对“bootstrapper 不再 success+overwrite”模式的文本约束。
    4. 目标测试必须在实现前失败，且失败原因来自当前 sample 没有统一 status contract，而不是测试 harness 本身错误。
  </action>
  <acceptance_criteria>
    - `Videra.Core.Tests.csproj` 能编译 sample 行为测试所需的 `samples/Videra.Demo` 类型
    - `DemoStatusContractTests.cs` 覆盖 waiting / ready / degraded / failure 四类状态
    - 至少一条测试在实现前失败，并直接指向当前 status contract 缺口
  </acceptance_criteria>
</task>

<task id="08-01-02" type="auto" tdd="true">
  <name>在 MainWindowViewModel 中引入显式 Demo status contract</name>
  <files>
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    tests/Videra.Core.Tests/Samples/DemoStatusContractTests.cs
  </files>
  <read_first>
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    tests/Videra.Core.Tests/Samples/DemoStatusContractTests.cs
    samples/Videra.Demo/Services/DemoSceneBootstrapper.cs
  </read_first>
  <action>
    1. 在 `MainWindowViewModel` 中把 ad hoc `SetBackendStatus` / `SetStatusMessage` 用法收口成命名明确的状态转换入口，例如：
       - waiting for backend/resource factory
       - backend ready with default scene
       - backend ready without default scene
       - backend initialization failed
       具体方法名可调整，但必须让调用方不再拼接彼此冲突的字符串。
    2. 保持公开 UI surface 仍然是 `IsBackendReady`, `StatusMessage`, `BackendDisplay`, `BackendDetails`，但让这些字段的写入来自显式 contract，而不是调用方随意组合。
    3. 默认场景失败时，状态文案必须带出“backend 已就绪，但默认场景失败；模型导入仍可用”这类明确语义。
  </action>
  <acceptance_criteria>
    - `MainWindowViewModel.cs` 中存在显式的 demo status transition API 或等价显式模型
    - sample 行为测试通过
    - 默认场景失败与初始化失败不再复用同一条模糊状态文案
  </acceptance_criteria>
</task>

<task id="08-01-03" type="auto" tdd="true">
  <name>让 bootstrapper 与 code-behind 走同一套状态提交路径</name>
  <files>
    samples/Videra.Demo/Services/DemoSceneBootstrapper.cs
    samples/Videra.Demo/Views/MainWindow.axaml.cs
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    tests/Videra.Core.Tests/Samples/DemoStatusContractTests.cs
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
  </files>
  <read_first>
    samples/Videra.Demo/Services/DemoSceneBootstrapper.cs
    samples/Videra.Demo/Views/MainWindow.axaml.cs
    samples/Videra.Demo/ViewModels/MainWindowViewModel.cs
    tests/Videra.Core.Tests/Samples/DemoStatusContractTests.cs
    tests/Videra.Core.Tests/Samples/DemoConfigurationTests.cs
  </read_first>
  <action>
    1. 修改 `DemoSceneBootstrapper.TryInitialize(...)`，让每个结果路径只提交一次最终用户可见状态：
       - top-level/resource factory 未就绪 => waiting
       - default cube seeded => ready + default scene loaded
       - default cube failed => ready + degraded + import available + last error
    2. 删除当前“先 `SetBackendStatus(...)` 再 `SetStatusMessage(...)` 覆盖”的失败路径写法。
    3. 修改 `MainWindow.axaml.cs` 的 `OnInitializationFailed(...)`，不要只写裸 `SetStatusMessage`；必须走显式 failure contract，并保持 `IsBackendReady == false`。
  </action>
  <acceptance_criteria>
    - `DemoSceneBootstrapper.cs` 不再出现 success+overwrite 的双写状态路径
    - `MainWindow.axaml.cs` 的初始化失败路径走统一 failure contract
    - `DemoStatusContractTests` 和相关 `DemoConfigurationTests` 全部通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter "FullyQualifiedName~DemoStatusContractTests|FullyQualifiedName~DemoConfigurationTests"`
- `pwsh -File F:/CodeProjects/DotnetCore/Videra/verify.ps1 -Configuration Release`
</verification>

<success_criteria>
1. Demo 状态栏对 waiting / ready / degraded / failure 有单一真相。
2. 默认场景失败不再冒充完全成功。
3. 初始化失败会显式保持 backend not ready。
</success_criteria>
