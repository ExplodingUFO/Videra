# Plan 103-01: Product Boundary Truth

## Objective

Make the top-level docs unambiguous that `Videra 1.0` is a native desktop viewer / inspection / surface-chart stack rather than a general 3D engine.

## Tasks

1. Update the root README boundary language so “what Videra is” and “who it is for” match the viewer-first product shape.
2. Update architecture and core docs so they describe the current viewer/runtime shape without implying broader engine parity.
3. Keep the source-first SurfaceCharts sibling boundary intact while clarifying that it is part of the product story, not the current public package line.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~Readme_ShouldDocumentPublicEntryPointsAndPackageCategories|FullyQualifiedName~ArchitectureDocs_ShouldDescribeSceneDocumentTruth_AndDirectDeviceSurfacePath|FullyQualifiedName~RootReadme_ShouldDescribeSurfaceChartsAsIndependentSiblingFamily"`
