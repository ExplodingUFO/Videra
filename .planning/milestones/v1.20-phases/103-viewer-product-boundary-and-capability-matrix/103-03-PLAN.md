# Plan 103-03: Package-Layer Matrix

## Objective

Define one package-layer vocabulary for `Core`, `Import`, `Backend`, `UI adapter`, and `Charts` so later extraction work has a stable boundary.

## Tasks

1. Extend the package guidance with a layer matrix that clearly distinguishes shipped packages, planned extracted layers, and source-first chart modules.
2. Update the core README to describe `Videra.Core` as the runtime kernel rather than the permanent home of every import/logging concern.
3. Add a repository truth test that guards the existence of the capability/layer matrix and key vocabulary.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~ProductBoundaryDocs_ShouldPublishCapabilityAndLayerMatrices"`
