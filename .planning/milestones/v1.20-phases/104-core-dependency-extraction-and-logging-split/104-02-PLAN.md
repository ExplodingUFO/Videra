# Plan 104-02

## Objective

Remove concrete logging-provider dependencies from `Videra.Core` and keep runtime composition on abstractions only.

## Tasks

1. Strip concrete Serilog provider packages from `src/Videra.Core/Videra.Core.csproj`.
2. Leave provider-specific logging composition outside `Videra.Core`.
3. Update any docs or repository guards that still imply provider coupling in the core package.

## Verification

- `Videra.Core.csproj` no longer contains the concrete Serilog provider package references.
- The runtime still builds and the public package truth no longer implies provider coupling through `Videra.Core`.
