# Plan 104-01

## Objective

Remove concrete importer dependencies from `Videra.Core` and move glTF/OBJ loading to dedicated import packages.

## Tasks

1. Remove the direct importer dependency from `src/Videra.Core/Videra.Core.csproj`.
2. Keep importer entry points in `src/Videra.Import.Gltf` and `src/Videra.Import.Obj`.
3. Rewire the Avalonia/demo/runtime composition path to consume the explicit import packages instead of relying on `Videra.Core`.

## Verification

- `Videra.Core.csproj` no longer contains the concrete importer package reference.
- Importer tests and integration tests still pass on the extracted package path.
