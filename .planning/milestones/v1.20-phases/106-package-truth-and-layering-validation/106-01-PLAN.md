# Plan 106-01: Teach One Canonical Package Stack

## Objective

Make the consumer-facing package, support, and release docs teach the same canonical viewer package stack without requiring users to infer it from source layout.

## Tasks

1. Tighten the package/support/release docs around the same `Core` / `Import` / `Avalonia` / matching-platform story.
2. Link those docs back to the capability and hosting-boundary docs where that adds clarity.
3. Keep `Videra.SurfaceCharts.*` explicitly outside the public package promise.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --no-build -m:1 --filter "FullyQualifiedName~CanonicalViewerPackageStack|FullyQualifiedName~ReleasePolicy|FullyQualifiedName~Releasing"`
