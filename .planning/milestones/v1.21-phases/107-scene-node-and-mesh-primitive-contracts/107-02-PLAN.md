# Plan 107-02

## Goal

Define mesh-primitive references so nodes can attach reusable geometry without collapsing node identity and instance reuse into one object.

## Steps

1. Add `MeshPrimitiveId` and `MeshPrimitive`.
2. Replace the public `ImportedSceneAsset` mesh-only surface with explicit `Nodes` and `Primitives`.
3. Keep the flattened render/upload payload internal so the public scene truth stays singular.
