# Plan 107-01

## Goal

Define stable scene-node contracts with backend-neutral identity, parent linkage, and local transform semantics.

## Steps

1. Add `SceneNodeId` and `SceneNode` to `Videra.Core.Scene`.
2. Keep the contract small: id, name, local transform, parent id, and attached primitive ids.
3. Update importer-facing tests to assert node hierarchy instead of only flattened mesh output.
