# Plan 107-03

## Goal

Adapt the current import/runtime/benchmark path to the new scene-node/mesh-primitive model without backend-specific public scene objects.

## Steps

1. Add one internal payload builder that flattens nodes and primitives into the deferred upload payload.
2. Update OBJ/glTF importers to emit node/primitive truth.
3. Preserve practical glTF partial-import behavior for malformed primitives.
4. Update focused tests and benchmark call sites to use the new asset contract.
