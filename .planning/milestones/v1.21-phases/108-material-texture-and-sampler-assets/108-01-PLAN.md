# Plan 108-01

## Goal

Define `MaterialInstance` as a backend-neutral runtime asset and let mesh primitives reference material identity directly.

## Tasks

1. Add stable material identity and contract types under `Videra.Core.Scene`.
2. Extend `MeshPrimitive` and `ImportedSceneAsset` so imported assets can carry material catalogs explicitly.
3. Update focused importer/runtime tests so the material catalog becomes required scene truth.
