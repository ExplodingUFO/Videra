# Plan 110-02

## Objective

Align sample truth with the shipped runtime model and public diagnostics.

## Steps

1. Update `Videra.Demo` copy to explain the retained scene/material catalog and public feature diagnostics.
2. Update `Videra.ExtensibilitySample` to surface supported and last-frame feature names through the public API.
3. Keep both samples on public viewer/runtime contracts only.

## Verification

- Demo and extensibility sample configuration tests can assert the new public runtime-truth strings directly.
