# Plan 110-01

## Objective

Update docs and package guidance so the viewer-first scene/material runtime model is explicit.

## Steps

1. Align repository entry docs on `SceneDocument -> ImportedSceneAsset -> SceneNode/MeshPrimitive/MaterialInstance/Texture2D/Sampler`.
2. Document the public render-feature vocabulary and its public diagnostics surfaces.
3. Keep package guidance focused on the existing viewer/runtime product boundary.

## Verification

- Repository doc guards can assert the scene/material runtime terms and render-feature truth across the public docs.
