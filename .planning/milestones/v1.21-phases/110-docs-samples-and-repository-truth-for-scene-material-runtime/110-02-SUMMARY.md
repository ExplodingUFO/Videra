# Summary 110-02

- `Videra.Demo` now teaches the retained scene/material catalog and public render-feature diagnostics in its visible truth.
- `Videra.ExtensibilitySample` now displays `SupportedFeatureNames`, `LastFrameFeatureNames`, and `SupportedRenderFeatureNames`.
- The sample flow stayed on public viewer/runtime APIs without leaking internal session or backend seams.
