# Summary 109-03

- Avalonia diagnostics now surface supported render-feature names and the last frame's active feature names.
- `VideraViewSessionBridge` keeps the host projection direct by unioning bridge-owned `Picking` and `Screenshot` support with runtime-owned features.
- Repository integration tests now lock the runtime-to-host feature vocabulary contract.
