# Plan 109-03

## Objective

Keep the viewer/runtime bridge explicit and direct while surfacing feature vocabulary in host diagnostics.

## Steps

1. Extend Avalonia diagnostics models with supported and last-frame feature names.
2. Project runtime feature truth through `VideraViewSessionBridge` without adding fallback adapters.
3. Add bridge and view integration tests that assert feature names reach host diagnostics unchanged.

## Verification

- Host-facing integration tests assert both runtime capabilities and last-frame diagnostics expose the expected feature names.
