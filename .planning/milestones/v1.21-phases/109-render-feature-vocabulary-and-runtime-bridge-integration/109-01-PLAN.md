# Plan 109-01

## Objective

Define one stable render-feature vocabulary for scene/material participation.

## Steps

1. Add a `RenderFeatureSet` flags contract in the render-pipeline layer.
2. Define the render-pass-slot to feature mapping in one small internal helper.
3. Extend frame-plan and pipeline snapshot contracts so active features travel with the runtime truth.

## Verification

- Integration tests can assert the active feature set and exported feature names from pipeline snapshots.
