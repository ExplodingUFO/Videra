# Summary 109-02

- `VideraEngine.Rendering` now computes active render features as part of the frame plan.
- Pass contributors and frame hooks receive direct feature-state contracts instead of reconstructing pass intent themselves.
- Engine capability snapshots now publish supported feature names alongside the flags contract.
