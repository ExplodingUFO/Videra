# Summary 109-01

- Added `RenderFeatureSet` as the stable feature vocabulary for `Opaque`, `Transparent`, `Overlay`, `Picking`, and `Screenshot`.
- Added one internal slot-to-feature map so pass participation is derived from a single truth source.
- Extended frame-plan and snapshot contracts so active features are part of runtime pipeline state rather than inferred ad hoc.
