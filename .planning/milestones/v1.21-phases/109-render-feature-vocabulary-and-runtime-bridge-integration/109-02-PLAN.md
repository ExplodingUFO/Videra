# Plan 109-02

## Objective

Align runtime and pass composition with the new feature vocabulary instead of pass-local assumptions.

## Steps

1. Thread active features through render-frame construction in `VideraEngine.Rendering`.
2. Expose active and slot-specific features on pass-contribution contexts.
3. Expose active features on frame-hook contexts and engine capability snapshots.

## Verification

- Extensibility integration tests confirm contributors, frame hooks, and capability snapshots all observe the same feature vocabulary.
