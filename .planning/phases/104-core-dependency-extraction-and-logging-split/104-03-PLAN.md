# Plan 104-03

## Objective

Prove that the slimmed core still composes through the existing viewer/runtime and packaged consumer flows.

## Tasks

1. Update consumer-smoke/package-validation inputs for the extracted import packages.
2. Verify repository tests, importer integration, and packaged consumer smoke on the new package graph.
3. Keep the implementation direct: no compatibility wrapper inside `Videra.Core`.

## Verification

- Packaged consumer smoke builds against the canonical package set.
- Importer integration tests and repository release-readiness coverage still pass on the split package graph.
