---
phase: 01-基础设施与清理
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-04"]
files_modified:
  - tests/Videra.Core.IntegrationTests/Rendering/SoftwareBackendIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Rendering/Object3DIntegrationTests.cs
  - tests/Videra.Core.IntegrationTests/Styles/StyleEventIntegrationTests.cs
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "SoftwareBackend can be initialized, used for a full render cycle, and disposed without GPU"
    - "Object3D works end-to-end with a real SoftwareResourceFactory (not mocks)"
    - "RenderStyleService fires events and applies presets correctly in integration"
  artifacts:
    - path: "tests/Videra.Core.IntegrationTests/Rendering/SoftwareBackendIntegrationTests.cs"
      provides: "SoftwareBackend pipeline integration tests"
      contains: "SoftwareBackend"
    - path: "tests/Videra.Core.IntegrationTests/Rendering/Object3DIntegrationTests.cs"
      provides: "Object3D integration tests with real resources"
      contains: "Object3D"
    - path: "tests/Videra.Core.IntegrationTests/Styles/StyleEventIntegrationTests.cs"
      provides: "Style event pipeline integration tests"
      contains: "RenderStyleService"
  key_links:
    - from: "tests/Videra.Core.IntegrationTests/Rendering/SoftwareBackendIntegrationTests.cs"
      to: "src/Videra.Core/Graphics/Software/SoftwareBackend.cs"
      via: "Direct instantiation (not mocked)"
      pattern: "new SoftwareBackend"
    - from: "tests/Videra.Core.IntegrationTests/Rendering/Object3DIntegrationTests.cs"
      to: "src/Videra.Core/Graphics/Software/SoftwareResourceFactory.cs"
      via: "Real factory passed to Object3D.Initialize"
      pattern: "SoftwareResourceFactory"
---

<objective>
Create integration tests for the rendering pipeline using the Software backend.

Purpose: Per TEST-03 -- verify that core components interact correctly end-to-end without a GPU. Integration tests differ from unit tests: they test component interactions without mocking internal implementation details. They use the Software backend which runs on CPU and requires no window handle or GPU.

Output: Integration test files for SoftwareBackend pipeline, Object3D with real resources, and RenderStyleService event flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-基础设施与清理/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-基础设施与清理/01-01-SUMMARY.md
@.planning/phases/01-基础设施与清理/01-04-SUMMARY.md

<interfaces>
<!-- Software rendering components for integration testing. -->

From src/Videra.Core/Graphics/Software/SoftwareBackend.cs:
```csharp
// CPU-based IGraphicsBackend implementation
// Does NOT require GPU or window handle
// Implements IGraphicsBackend: Initialize, BeginFrame, EndFrame, etc.
```

From src/Videra.Core/Graphics/Software/SoftwareResourceFactory.cs:
```csharp
// CPU-based IResourceFactory implementation
// Creates real IBuffer, IPipeline instances in software
```

From src/Videra.Core/Graphics/Object3D.cs:
```csharp
// 3D object with mesh data, requires IResourceFactory for initialization
// Methods: Initialize(IResourceFactory), UpdateUniforms(Matrix4x4), Dispose()
```

From src/Videra.Core/Styles/RenderStyleService.cs:
```csharp
// Style management with presets and StyleChanged event
// Methods: ApplyPreset, parameter update methods
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for rendering pipeline</name>
  <files>
    tests/Videra.Core.IntegrationTests/Rendering/SoftwareBackendIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Rendering/Object3DIntegrationTests.cs
    tests/Videra.Core.IntegrationTests/Styles/StyleEventIntegrationTests.cs
  </files>
  <read_first>
    src/Videra.Core/Graphics/Software/SoftwareBackend.cs
    src/Videra.Core/Graphics/Software/SoftwareCommandExecutor.cs
    src/Videra.Core/Graphics/Software/SoftwareResourceFactory.cs
    src/Videra.Core/Graphics/Object3D.cs
    src/Videra.Core/Styles/RenderStyleService.cs
    src/Videra.Core/Graphics/VideraEngine.cs
    tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj
  </read_first>
  <action>
Per TEST-03: Create integration tests for the rendering pipeline using the Software backend (which does not require a GPU or window handle).

Integration tests differ from unit tests: they test component interactions without mocking internal implementation details. They CAN mock external dependencies but test real interactions between components.

1. tests/Videra.Core.IntegrationTests/Rendering/SoftwareBackendIntegrationTests.cs
   READ SoftwareBackend.cs, SoftwareCommandExecutor.cs, SoftwareResourceFactory.cs first.
   Test the actual software rendering pipeline:
   - Create SoftwareBackend instance
   - Initialize with a fake IntPtr window handle
   - Verify IsInitialized becomes true
   - Get IResourceFactory and create real buffers
   - Get ICommandExecutor and execute commands
   - Verify BeginFrame/EndFrame cycle works
   - Verify Resize works
   - Verify Dispose cleans up resources

   NOTE: SoftwareBackend may require specific initialization. Read the source to understand what it needs. If it requires actual pixel dimensions, use small values (100x100).

2. tests/Videra.Core.IntegrationTests/Rendering/Object3DIntegrationTests.cs
   Test Object3D with real (software) resource factory:
   - Create MeshData with simple triangle vertices
   - Create Object3D with the mesh data
   - Initialize with SoftwareResourceFactory (real, not mocked)
   - Verify buffers are actually created
   - Update uniforms and verify no exceptions
   - Test wireframe initialization
   - Test Dispose

3. tests/Videra.Core.IntegrationTests/Styles/StyleEventIntegrationTests.cs
   Test the full style event pipeline:
   - Create RenderStyleService
   - Apply a preset (e.g., Realistic)
   - Verify StyleChanged event fires
   - Verify parameters match expected preset values
   - Apply different preset and verify changes

IMPORTANT: These tests use the Software backend which runs on CPU. They do NOT require a GPU. They verify that components interact correctly end-to-end. If the SoftwareBackend constructor or initialization has specific requirements discovered during reading, adapt the tests accordingly.

Delete the placeholder integration test file if it exists from Plan 01.
  </action>
  <acceptance_criteria>
    - tests/Videra.Core.IntegrationTests/Rendering/SoftwareBackendIntegrationTests.cs exists with [Fact] tests
    - tests/Videra.Core.IntegrationTests/Rendering/Object3DIntegrationTests.cs exists with [Fact] tests
    - tests/Videra.Core.IntegrationTests/Styles/StyleEventIntegrationTests.cs exists with [Fact] tests
    - Integration tests use real SoftwareBackend/SoftwareResourceFactory (not mocks for internal components)
    - `dotnet test Videra.slnx --filter "FullyQualifiedName~Videra.Core.IntegrationTests"` passes
    - No placeholder test files remain
  </acceptance_criteria>
  <verify>
    <automated>cd "F:/CodeProjects/DotnetCore/Videra" && dotnet test Videra.slnx --filter "FullyQualifiedName~Videra.Core.IntegrationTests" --no-restore=false 2>&1 | tail -20</automated>
  </verify>
  <done>Integration tests exist for SoftwareBackend pipeline, Object3D with real resources, and RenderStyleService event flow. All tests pass. Tests use real software renderer components, not mocks.</done>
</task>

</tasks>

<verification>
1. `dotnet test Videra.slnx --filter "FullyQualifiedName~Videra.Core.IntegrationTests"` -- passes
2. Integration test files exist in tests/Videra.Core.IntegrationTests/Rendering/ and tests/Videra.Core.IntegrationTests/Styles/
3. Tests use real SoftwareBackend/SoftwareResourceFactory, not mocks
4. No placeholder test files remain
</verification>

<success_criteria>
- Integration tests for SoftwareBackend, Object3D, and StyleService pass
- Tests use real software renderer components (not mocked internals)
- All integration tests pass via `dotnet test`
</success_criteria>

<output>
After completion, create `.planning/phases/01-基础设施与清理/01-05-SUMMARY.md`
</output>
