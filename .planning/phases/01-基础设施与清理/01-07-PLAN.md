---
phase: 01-基础设施与清理
plan: 07
type: execute
wave: 5
depends_on: ["01-06"]
files_modified:
  - tests/Tests.Common/Platform/NativeHostTestHelpers.cs
  - tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendSmokeTests.cs
  - tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs
  - tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendSmokeTests.cs
  - tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs
  - tests/Videra.Platform.macOS.Tests/Backend/MetalBackendSmokeTests.cs
  - tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs
autonomous: true
requirements: [TEST-03]
gap_closure: true

must_haves:
  truths:
    - "Windows hosts execute at least one real HWND-backed D3D11 backend initialization and lifecycle test"
    - "Linux hosts execute at least one real X11-backed Vulkan backend initialization and lifecycle/render-path test"
    - "macOS hosts execute at least one real NSView-backed Metal backend initialization and lifecycle/render-path test"
    - "TEST-03 is not considered satisfied by constructor-only assertions, IntPtr.Zero precondition checks, placeholder fixture tests, or skip-only suites"
  artifacts:
    - path: "tests/Tests.Common/Platform/NativeHostTestHelpers.cs"
      provides: "Real native host fixtures for HWND, X11 Window, and NSView-backed handles"
      contains: "NativeHostTestHelpers"
    - path: "tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs"
      provides: "Executable Windows backend initialization/lifecycle coverage on a real HWND"
      contains: "D3D11BackendLifecycleTests"
    - path: "tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs"
      provides: "Executable Linux backend initialization/lifecycle/render-path coverage on a real X11 host"
      contains: "VulkanBackendLifecycleTests"
    - path: "tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs"
      provides: "Executable macOS backend initialization/lifecycle/render-path coverage on a real NSView host"
      contains: "MetalBackendLifecycleTests"
  key_links:
    - from: "tests/Tests.Common/Platform/NativeHostTestHelpers.cs"
      to: "tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs"
      via: "real HWND fixture"
      pattern: "Create.*Hwnd|HWND|Win32"
    - from: "tests/Tests.Common/Platform/NativeHostTestHelpers.cs"
      to: "tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs"
      via: "real X11 display/window fixture"
      pattern: "Create.*X11|XOpenDisplay|XCreateSimpleWindow"
    - from: "tests/Tests.Common/Platform/NativeHostTestHelpers.cs"
      to: "tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs"
      via: "real NSView-backed fixture"
      pattern: "NSView|Create.*NsView"
    - from: "tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs"
      to: "src/Videra.Platform.Windows/D3D11Backend.cs"
      via: "Initialize(real HWND, width, height)"
      pattern: "Initialize\(.*windowHandle"
    - from: "tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs"
      to: "src/Videra.Platform.Linux/VulkanBackend.cs"
      via: "Initialize(real X11 handle, width, height) plus render-path/lifecycle assertions"
      pattern: "Initialize\(.*windowHandle"
    - from: "tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs"
      to: "src/Videra.Platform.macOS/MetalBackend.cs"
      via: "Initialize(real NSView handle, width, height) plus render-path/lifecycle assertions"
      pattern: "Initialize\(.*windowHandle"
---

<objective>
Close the last remaining TEST-03 gap from 01-VERIFICATION.md by replacing placeholder platform smoke coverage with real native-host lifecycle/render-path tests on the matching OS.

Purpose: 01-06 proved the per-OS test-project structure, but re-verification still shows that Windows, Linux, and macOS do not yet validate real platform backend behavior on native hosts.
Output: Real HWND/X11/NSView-backed platform tests that execute backend initialization and lifecycle paths on their matching operating systems, with no requirement weakening.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/CodeProjects/DotnetCore/Videra/.planning/REQUIREMENTS.md
@F:/CodeProjects/DotnetCore/Videra/.planning/ROADMAP.md
@F:/CodeProjects/DotnetCore/Videra/.planning/STATE.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/01-基础设施与清理/01-06-PLAN.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/01-基础设施与清理/01-06-SUMMARY.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/01-基础设施与清理/01-VERIFICATION.md
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.Windows/D3D11Backend.cs
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.Linux/VulkanBackend.cs
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.macOS/MetalBackend.cs

<interfaces>
From src/Videra.Platform.Windows/D3D11Backend.cs:
```csharp
public unsafe class D3D11Backend : IGraphicsBackend
{
    public bool IsInitialized { get; private set; }
    public void Initialize(IntPtr windowHandle, int width, int height)
    public void Resize(int width, int height)
    public void BeginFrame()
    public void EndFrame()
    public IResourceFactory GetResourceFactory()
    public ICommandExecutor GetCommandExecutor()
    public void Dispose()
}
```

From src/Videra.Platform.Linux/VulkanBackend.cs:
```csharp
public unsafe class VulkanBackend : IGraphicsBackend
{
    public bool IsInitialized { get; private set; }
    public void Initialize(IntPtr windowHandle, int width, int height)
    public void Resize(int width, int height)
    public void BeginFrame()
    public void EndFrame()
    public IResourceFactory GetResourceFactory()
    public ICommandExecutor GetCommandExecutor()
    public void Dispose()
}
```

From src/Videra.Platform.macOS/MetalBackend.cs:
```csharp
public unsafe class MetalBackend : IGraphicsBackend
{
    public bool IsInitialized { get; private set; }
    public void Initialize(IntPtr windowHandle, int width, int height)
    public void Resize(int width, int height)
    public void BeginFrame()
    public void EndFrame()
    public IResourceFactory GetResourceFactory()
    public ICommandExecutor GetCommandExecutor()
    public void Dispose()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder native fixtures with real per-OS test host creation</name>
  <files>
    tests/Tests.Common/Platform/NativeHostTestHelpers.cs
  </files>
  <read_first>
    tests/Tests.Common/Tests.Common.csproj
    tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendSmokeTests.cs
    tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendSmokeTests.cs
    tests/Videra.Platform.macOS.Tests/Backend/MetalBackendSmokeTests.cs
  </read_first>
  <action>
    Create the smallest test-only fixture layer that can supply real native host handles on the matching OS, because 01-VERIFICATION.md explicitly rejects reusable-fixture placeholders.

    1. Implement real disposable native-host helpers in `tests/Tests.Common/Platform/NativeHostTestHelpers.cs` for all three platforms:
       - Windows: hidden Win32 window / valid HWND for `D3D11Backend.Initialize`
       - Linux: X11 display + real Window handle for `VulkanBackend.Initialize`
       - macOS: NSWindow/NSView-backed handle suitable for `MetalBackend.Initialize`
    2. Helpers must open/create actual native resources on the matching OS and expose enough metadata for cleanup.
    3. Do not allow `IntPtr.Zero`, fake stubs, or placeholder-only assertions to count as fixture coverage.
    4. Keep the scope test-only. Do not weaken TEST-03 wording, and do not convert this gap into a constructor/precondition-only plan.
    5. If the current machine cannot execute Linux/macOS or Windows-specific native code, still implement the fixtures correctly and leave execution to the matching OS; that is an execution constraint, not a requirement change.
  </action>
  <acceptance_criteria>
    - `NativeHostTestHelpers.cs` exposes real handle acquisition and disposal paths for HWND, X11, and NSView-backed fixtures
    - The helper API is explicit that matching-host tests must pass non-zero native handles produced by real OS objects
    - No acceptance path permits `IntPtr.Zero`, constructor-only checks, placeholder-fixture assertions, or skip-only coverage to satisfy TEST-03
  </acceptance_criteria>
  <verify>
    <automated>dotnet build F:/CodeProjects/DotnetCore/Videra/Videra.slnx -c Debug</automated>
  </verify>
  <done>Matching-host test suites have a concrete native-fixture surface capable of real backend initialization rather than placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert each platform suite to real native-host lifecycle and render-path coverage</name>
  <files>
    tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendSmokeTests.cs
    tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs
    tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendSmokeTests.cs
    tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs
    tests/Videra.Platform.macOS.Tests/Backend/MetalBackendSmokeTests.cs
    tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs
  </files>
  <read_first>
    tests/Tests.Common/Platform/NativeHostTestHelpers.cs
    src/Videra.Platform.Windows/D3D11Backend.cs
    src/Videra.Platform.Linux/VulkanBackend.cs
    src/Videra.Platform.macOS/MetalBackend.cs
    F:/CodeProjects/DotnetCore/Videra/.planning/phases/01-基础设施与清理/01-VERIFICATION.md
  </read_first>
  <behavior>
    - Windows: on Windows, at least one automated test creates a real HWND, calls `D3D11Backend.Initialize`, verifies `IsInitialized`, exercises lifecycle operations (`Resize`, `BeginFrame`, `EndFrame`, resource/executor access as available), and disposes cleanly.
    - Linux: on Linux, at least one automated test creates a real X11 host, calls `VulkanBackend.Initialize`, verifies `IsInitialized`, exercises lifecycle operations and at least one real render-path-adjacent backend path, then disposes cleanly.
    - macOS: on macOS, at least one automated test creates a real NSView-backed host, calls `MetalBackend.Initialize`, verifies `IsInitialized`, exercises lifecycle operations and at least one real render-path-adjacent backend path, then disposes cleanly.
    - Non-matching hosts may guard execution, but the matching-host suite must execute the real path; skip-only suites do not close the gap.
  </behavior>
  <action>
    Replace the hollow smoke/placeholder coverage with executable native-host tests that satisfy the remaining TEST-03 gap as written.

    1. For Windows, add or replace tests so a Windows host runs real HWND-backed `D3D11Backend` initialization and lifecycle coverage. Per the remaining gap, constructor-only checks and placeholder fixture assertions must no longer be the completion signal.
    2. For Linux, add or replace tests so a Linux host runs real X11-backed `VulkanBackend` initialization plus lifecycle/render-path coverage. The existing zero-handle precondition test may remain as supplemental coverage, but it cannot be the proof of TEST-03.
    3. For macOS, add or replace tests so a macOS host runs real NSView-backed `MetalBackend` initialization plus lifecycle/render-path coverage. Placeholder NSView-fixture assertions must be removed or demoted so they are not mistaken for completion.
    4. Keep the plan strictly on TEST-03. Do not broaden into production backend refactors unless a backend bug is directly exposed by these tests and must be fixed to make real native-host execution possible.
    5. If some platform-specific execution cannot happen on the current host, the implementation still must require the real native-host path on that platform's own host. Do not weaken acceptance criteria to "builds only" or "skipped successfully".
  </action>
  <acceptance_criteria>
    - Windows suite contains at least one matching-host test that executes real HWND-backed backend initialization and lifecycle behavior
    - Linux suite contains at least one matching-host test that executes real X11-backed backend initialization and lifecycle/render-path behavior
    - macOS suite contains at least one matching-host test that executes real NSView-backed backend initialization and lifecycle/render-path behavior
    - TEST-03 is not claimable if the executable coverage is limited to constructor assertions, `IntPtr.Zero` precondition failures, placeholder-fixture tests, or host-skipped facts
    - Matching-host automated commands are the proof of completion, even if the current machine can only run one subset locally
  </acceptance_criteria>
  <verify>
    <automated>dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Platform.Windows.Tests/Videra.Platform.Windows.Tests.csproj --no-restore && dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Platform.Linux.Tests/Videra.Platform.Linux.Tests.csproj --no-restore && dotnet test F:/CodeProjects/DotnetCore/Videra/tests/Videra.Platform.macOS.Tests/Videra.Platform.macOS.Tests.csproj --no-restore</automated>
  </verify>
  <done>Each matching OS has explicit real native-host backend tests that reach initialization and lifecycle/render-path behavior, closing the remaining TEST-03 gap without relaxing the standard.</done>
</task>

</tasks>

<verification>
1. Windows completion requires a real HWND-backed backend initialization/lifecycle test to run on Windows.
2. Linux completion requires a real X11-backed backend initialization/lifecycle/render-path test to run on Linux.
3. macOS completion requires a real NSView-backed backend initialization/lifecycle/render-path test to run on macOS.
4. This plan is invalidly executed if completion is claimed using constructor-only, `IntPtr.Zero`-only, placeholder-fixture, or skip-only outcomes.
5. If the current host cannot execute every OS-specific test, execution must still preserve these requirements and finish the missing platform runs on matching hosts.
</verification>

<success_criteria>
- The only remaining Phase 1 blocker in `01-VERIFICATION.md` is closed.
- TEST-03 is proven by real native-host coverage for D3D11, Vulkan, and Metal on their matching operating systems.
- Phase 1 no longer relies on smoke/precondition/placeholder tests to justify platform backend integration coverage.
</success_criteria>

<output>
After completion, create `F:/CodeProjects/DotnetCore/Videra/.planning/phases/01-基础设施与清理/01-07-SUMMARY.md`
</output>
