---
phase: 01-基础设施与清理
plan: 06
type: execute
wave: 4
depends_on: ["01-05"]
files_modified:
  - Videra.slnx
  - tests/Tests.Common/Platform/NativeHostTestHelpers.cs
  - tests/Videra.Platform.Windows.Tests/Videra.Platform.Windows.Tests.csproj
  - tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs
  - tests/Videra.Platform.Linux.Tests/Videra.Platform.Linux.Tests.csproj
  - tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs
  - tests/Videra.Platform.macOS.Tests/Videra.Platform.macOS.Tests.csproj
  - tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs
autonomous: true
requirements: [TEST-03]
gap_closure: true

must_haves:
  truths:
    - "Phase 1 includes dedicated Windows, Linux, and macOS backend test projects for TEST-03"
    - "On Windows, Linux, and macOS hosts respectively, at least one automated test performs real native-host backend initialization against the platform backend and verifies lifecycle behavior"
    - "On non-matching hosts, the platform suites stay green by OS-guarding execution rather than pretending coverage exists"
  artifacts:
    - path: "tests/Videra.Platform.Windows.Tests/Videra.Platform.Windows.Tests.csproj"
      provides: "Windows backend integration test project"
      contains: "IsTestProject"
    - path: "tests/Videra.Platform.Linux.Tests/Videra.Platform.Linux.Tests.csproj"
      provides: "Linux backend integration test project"
      contains: "IsTestProject"
    - path: "tests/Videra.Platform.macOS.Tests/Videra.Platform.macOS.Tests.csproj"
      provides: "macOS backend integration test project"
      contains: "IsTestProject"
    - path: "tests/Tests.Common/Platform/NativeHostTestHelpers.cs"
      provides: "Minimal native host helpers for acquiring valid test window/view handles on the matching OS"
      contains: "NativeHostTestHelpers"
    - path: "tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs"
      provides: "Executable Windows native-host backend initialization and lifecycle coverage"
      contains: "D3D11Backend"
    - path: "tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs"
      provides: "Executable Linux native-host backend initialization and lifecycle coverage"
      contains: "VulkanBackend"
    - path: "tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs"
      provides: "Executable macOS native-host backend initialization and lifecycle coverage"
      contains: "MetalBackend"
  key_links:
    - from: "Videra.slnx"
      to: "tests/Videra.Platform.Windows.Tests/Videra.Platform.Windows.Tests.csproj"
      via: "Solution project entry"
      pattern: "Videra.Platform.Windows.Tests"
    - from: "Videra.slnx"
      to: "tests/Videra.Platform.Linux.Tests/Videra.Platform.Linux.Tests.csproj"
      via: "Solution project entry"
      pattern: "Videra.Platform.Linux.Tests"
    - from: "Videra.slnx"
      to: "tests/Videra.Platform.macOS.Tests/Videra.Platform.macOS.Tests.csproj"
      via: "Solution project entry"
      pattern: "Videra.Platform.macOS.Tests"
    - from: "tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs"
      to: "src/Videra.Platform.Windows/D3D11Backend.cs"
      via: "Real native window handle passed to Initialize on Windows"
      pattern: "Initialize\(.*windowHandle"
    - from: "tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs"
      to: "src/Videra.Platform.Linux/VulkanBackend.cs"
      via: "Real X11 window handle passed to Initialize on Linux"
      pattern: "Initialize\(.*windowHandle"
    - from: "tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs"
      to: "src/Videra.Platform.macOS/MetalBackend.cs"
      via: "Real NSView-backed handle passed to Initialize on macOS"
      pattern: "Initialize\(.*windowHandle"
---

<objective>
Close the remaining TEST-03 gap without changing requirement wording: add platform-specific backend test projects plus at least one real native-host backend initialization/lifecycle test per platform that executes on the matching OS.

Purpose: 01-VERIFICATION.md shows Phase 1 is blocked because current integration coverage only exercises `SoftwareBackend`; TEST-03 explicitly requires platform-specific backend integration tests.
Output: Three platform backend test projects, minimal native-host test helpers, executable Windows/Linux/macOS backend lifecycle tests, and solution wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/CodeProjects/DotnetCore/Videra/.planning/PROJECT.md
@F:/CodeProjects/DotnetCore/Videra/.planning/ROADMAP.md
@F:/CodeProjects/DotnetCore/Videra/.planning/STATE.md
@F:/CodeProjects/DotnetCore/Videra/.planning/REQUIREMENTS.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/01-基础设施与清理/01-VERIFICATION.md
@F:/CodeProjects/DotnetCore/Videra/.planning/phases/01-基础设施与清理/01-05-SUMMARY.md
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Core/Graphics/GraphicsBackendFactory.cs
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.Windows/D3D11Backend.cs
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.Linux/VulkanBackend.cs
@F:/CodeProjects/DotnetCore/Videra/src/Videra.Platform.macOS/MetalBackend.cs

<interfaces>
From src/Videra.Platform.Windows/D3D11Backend.cs:
```csharp
public unsafe class D3D11Backend : IGraphicsBackend
{
    public bool IsInitialized { get; private set; }
    public void Initialize(IntPtr windowHandle, int width, int height)
    public void Resize(int width, int height)
    public void BeginFrame()
    public void EndFrame()
    public IResourceFactory GetResourceFactory()
    public ICommandExecutor GetCommandExecutor()
    public void Dispose()
}
```

From src/Videra.Platform.Linux/VulkanBackend.cs:
```csharp
public unsafe class VulkanBackend : IGraphicsBackend
{
    public bool IsInitialized { get; private set; }
    public void Initialize(IntPtr windowHandle, int width, int height)
    public void Resize(int width, int height)
    public void Dispose()
}
```

From src/Videra.Platform.macOS/MetalBackend.cs:
```csharp
public unsafe class MetalBackend : IGraphicsBackend
{
    public bool IsInitialized { get; private set; }
    public void Initialize(IntPtr windowHandle, int width, int height)
    public void Resize(int width, int height)
    public void BeginFrame()
    public void EndFrame()
    public void Dispose()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold platform backend test projects and native-host helper surface</name>
  <files>
    Videra.slnx
    tests/Tests.Common/Platform/NativeHostTestHelpers.cs
    tests/Videra.Platform.Windows.Tests/Videra.Platform.Windows.Tests.csproj
    tests/Videra.Platform.Linux.Tests/Videra.Platform.Linux.Tests.csproj
    tests/Videra.Platform.macOS.Tests/Videra.Platform.macOS.Tests.csproj
  </files>
  <read_first>
    Videra.slnx
    tests/Tests.Common/Tests.Common.csproj
    tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj
    src/Videra.Platform.Windows/Videra.Platform.Windows.csproj
    src/Videra.Platform.Linux/Videra.Platform.Linux.csproj
    src/Videra.Platform.macOS/Videra.Platform.macOS.csproj
  </read_first>
  <action>
    Create the minimum scaffolding needed to execute TEST-03 as written, and nothing else.

    1. Add `tests/Videra.Platform.Windows.Tests/Videra.Platform.Windows.Tests.csproj`, `tests/Videra.Platform.Linux.Tests/Videra.Platform.Linux.Tests.csproj`, and `tests/Videra.Platform.macOS.Tests/Videra.Platform.macOS.Tests.csproj` using the same package baseline already established in Phase 1 (`Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`, `FluentAssertions`, `coverlet.collector`).
    2. Reference the matching platform project, `src/Videra.Core/Videra.Core.csproj`, and `tests/Tests.Common/Tests.Common.csproj` from each new test project.
    3. Add the three test projects to `Videra.slnx` so matching-host `dotnet test` executes them.
    4. Create `tests/Tests.Common/Platform/NativeHostTestHelpers.cs` with the smallest possible helper API for acquiring and disposing valid native test handles on the matching OS:
       - Windows: a disposable hidden Win32 window handle suitable for `D3D11Backend.Initialize`
       - Linux: a disposable X11 display/window handle suitable for `VulkanBackend.Initialize`
       - macOS: a disposable NSView-backed handle suitable for `MetalBackend.Initialize`
    5. Keep helpers test-only. Do not modify production runtime code, factory behavior, or requirement wording.
  </action>
  <acceptance_criteria>
    - All three platform test projects exist and reference their matching platform backend project
    - `tests/Tests.Common/Platform/NativeHostTestHelpers.cs` exists with helper methods/types for Windows, Linux, and macOS native test handles
    - `Videra.slnx` lists all three new platform test projects
    - The helper surface is explicitly designed for real native handle acquisition on the matching host, not fake `IntPtr.Zero` placeholders
  </acceptance_criteria>
  <verify>
    <automated>cd "F:/CodeProjects/DotnetCore/Videra" && dotnet sln Videra.slnx list</automated>
  </verify>
  <done>Solution wiring and test-only native host helpers exist so backend lifecycle tests can perform real initialization on the matching OS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add executable native-host backend lifecycle tests for each platform</name>
  <files>
    tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs
    tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs
    tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs
  </files>
  <read_first>
    tests/Tests.Common/Platform/NativeHostTestHelpers.cs
    src/Videra.Platform.Windows/D3D11Backend.cs
    src/Videra.Platform.Linux/VulkanBackend.cs
    src/Videra.Platform.macOS/MetalBackend.cs
    tests/Videra.Core.IntegrationTests/Rendering/SoftwareBackendIntegrationTests.cs
  </read_first>
  <behavior>
    - Windows: on Windows hosts, at least one test creates a real Win32 test window, initializes `D3D11Backend` with that native handle, verifies `IsInitialized == true`, verifies `GetResourceFactory()` and `GetCommandExecutor()` are usable, exercises `Resize`, and disposes without exception. On non-Windows hosts, the test is OS-skipped.
    - Linux: on Linux hosts, at least one test creates a real X11 test window, initializes `VulkanBackend` with that native handle, verifies `IsInitialized == true`, exercises `Resize`, and disposes without exception. On non-Linux hosts, the test is OS-skipped.
    - macOS: on macOS hosts, at least one test creates a real NSView-backed handle, initializes `MetalBackend` with that native handle, verifies `IsInitialized == true`, exercises `Resize`, `BeginFrame`, `EndFrame`, and disposes without exception. On non-macOS hosts, the test is OS-skipped.
  </behavior>
  <action>
    Implement real backend lifecycle tests that close TEST-03 on the matching OS. Smoke-only precondition tests do not satisfy this plan.

    1. In `tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs`, add an OS-guarded integration test that acquires a real hidden Win32 window from `NativeHostTestHelpers`, calls `D3D11Backend.Initialize`, asserts successful initialization, then exercises lifecycle methods and disposal.
    2. In `tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs`, add an OS-guarded integration test that acquires a real X11 window from `NativeHostTestHelpers`, calls `VulkanBackend.Initialize`, asserts successful initialization, then exercises resize/disposal.
    3. In `tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs`, add an OS-guarded integration test that acquires a real NSView-backed handle from `NativeHostTestHelpers`, calls `MetalBackend.Initialize`, asserts successful initialization, then exercises resize, frame lifecycle, and disposal.
    4. Non-matching hosts may skip these tests through xUnit OS guards, but matching hosts must execute the real native-host path. Do not satisfy the plan with skip-only, precondition-only, or `IntPtr.Zero`-only tests.
    5. Keep scope tight to TEST-03. Do not add broader render-pipeline assertions, CI work, or backend refactors.
  </action>
  <acceptance_criteria>
    - `tests/Videra.Platform.Windows.Tests/Backend/D3D11BackendLifecycleTests.cs` contains at least one Windows-only automated test that performs real native-host initialization and lifecycle assertions against `D3D11Backend`
    - `tests/Videra.Platform.Linux.Tests/Backend/VulkanBackendLifecycleTests.cs` contains at least one Linux-only automated test that performs real native-host initialization and lifecycle assertions against `VulkanBackend`
    - `tests/Videra.Platform.macOS.Tests/Backend/MetalBackendLifecycleTests.cs` contains at least one macOS-only automated test that performs real native-host initialization and lifecycle assertions against `MetalBackend`
    - On the matching OS, each platform project has at least one executable test that reaches backend initialization and verifies post-initialize lifecycle behavior on the real native host
    - The plan is not considered complete if the only executable assertions are constructor checks, `IntPtr.Zero` precondition failures, or skipped tests
  </acceptance_criteria>
  <verify>
    <automated>cd "F:/CodeProjects/DotnetCore/Videra" && dotnet test tests/Videra.Platform.Windows.Tests/Videra.Platform.Windows.Tests.csproj --no-restore && dotnet test tests/Videra.Platform.Linux.Tests/Videra.Platform.Linux.Tests.csproj --no-restore && dotnet test tests/Videra.Platform.macOS.Tests/Videra.Platform.macOS.Tests.csproj --no-restore</automated>
  </verify>
  <done>Each platform backend has real native-host initialization/lifecycle coverage that runs on its matching OS and is safely OS-guarded elsewhere, satisfying TEST-03 without narrowing the requirement.</done>
</task>

</tasks>

<verification>
1. `dotnet sln F:/CodeProjects/DotnetCore/Videra/Videra.slnx list` shows the three new platform test projects.
2. On Windows, Linux, and macOS hosts respectively, the matching platform test project executes at least one real native-host backend initialization/lifecycle test and passes.
3. Non-matching hosts skip the platform-specific tests via OS guards instead of failing.
4. No completion claim is valid unless each matching-host suite exercises actual backend initialization plus lifecycle behavior on the native host.
</verification>

<success_criteria>
- TEST-03 remains unchanged and is satisfied by dedicated Windows, Linux, and macOS backend integration tests.
- Each matching OS runs at least one executable native-host backend initialization/lifecycle test for its platform backend.
- Phase 1 no longer relies on software-backend-only coverage, smoke-only assertions, or skip-only outcomes to claim TEST-03 is complete.
</success_criteria>

<output>
After completion, create `F:/CodeProjects/DotnetCore/Videra/.planning/phases/01-基础设施与清理/01-06-SUMMARY.md`
</output>
