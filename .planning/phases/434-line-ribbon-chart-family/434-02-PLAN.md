---
phase: 434-line-ribbon-chart-family
plan: 02
type: execute
wave: 2
depends_on: ["434-01"]
files_modified:
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesKind.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/BarPlot3DSeries.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/ScatterPlot3DSeries.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/ContourPlot3DSeries.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/LinePlot3DSeries.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/RibbonPlot3DSeries.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesComposition.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DDatasetEvidence.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DOutputEvidence.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/LineChartRenderingStatus.cs
  - src/Videra.SurfaceCharts.Avalonia/Controls/RibbonChartRenderingStatus.cs
autonomous: true
requirements:
  - LINE-01
  - LINE-02
  - LINE-04

must_haves:
  truths:
    - "Plot3DSeriesKind includes Line and Ribbon enum values"
    - "Plot3DSeries constructor accepts LineChartData and RibbonChartData slots"
    - "Plot3DAddApi.Line(xs, ys, zs, name) creates and adds a LinePlot3DSeries"
    - "Plot3DAddApi.Ribbon(xs, ys, zs, radius, name) creates and adds a RibbonPlot3DSeries"
    - "Plot3D exposes ActiveLineData, ActiveRibbonData, ActiveLineSeries, ActiveRibbonSeries"
    - "Plot3DSeriesComposition.CreateLineData merges multiple visible line series"
    - "Plot3DSeriesComposition.CreateRibbonData merges multiple visible ribbon series"
    - "SurfaceLegendOverlayPresenter handles Line (LegendIndicatorKind.Line, blue) and Ribbon (LegendIndicatorKind.Swatch, purple)"
    - "VideraChartView exposes LineRenderingStatus and RibbonRenderingStatus"
    - "Plot3DDatasetEvidence handles Line and Ribbon series kinds"
    - "Plot3DOutputEvidence has FromLineStatus and FromRibbonStatus methods"
  artifacts:
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesKind.cs"
      provides: "Line and Ribbon enum values"
      contains: "Line,"
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/LinePlot3DSeries.cs"
      provides: "LinePlot3DSeries sealed class"
      contains: "sealed class LinePlot3DSeries"
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/RibbonPlot3DSeries.cs"
      provides: "RibbonPlot3DSeries sealed class"
      contains: "sealed class RibbonPlot3DSeries"
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/LineChartRenderingStatus.cs"
      provides: "LineChartRenderingStatus sealed record"
      contains: "sealed record LineChartRenderingStatus"
    - path: "src/Videra.SurfaceCharts.Avalonia/Controls/RibbonChartRenderingStatus.cs"
      provides: "RibbonChartRenderingStatus sealed record"
      contains: "sealed record RibbonChartRenderingStatus"
  key_links:
    - from: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs"
      to: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/LinePlot3DSeries.cs"
      via: "Line() method creates LinePlot3DSeries"
      pattern: "new LinePlot3DSeries"
    - from: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs"
      to: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/RibbonPlot3DSeries.cs"
      via: "Ribbon() method creates RibbonPlot3DSeries"
      pattern: "new RibbonPlot3DSeries"
    - from: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs"
      to: "src/Videra.SurfaceCharts.Core/LineChartData.cs"
      via: "LineData property"
      pattern: "LineChartData.*LineData"
    - from: "src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs"
      to: "src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesKind.cs"
      via: "switch on series.Kind for Line and Ribbon"
      pattern: "Plot3DSeriesKind.Line"
---

<objective>
Wire Line and Ribbon chart types into the Avalonia integration layer: Plot3DSeries base class, Plot3DAddApi, Plot3D, composition, legend, probe registration, rendering status, and evidence.

Purpose: Complete the 7-step integration pattern for Line and Ribbon so `Plot.Add.Line(...)` and `Plot.Add.Ribbon(...)` work end-to-end with probe, legend, and diagnostics.
Output: 2 new Avalonia files (LinePlot3DSeries, RibbonPlot3DSeries, LineChartRenderingStatus, RibbonChartRenderingStatus) + modifications to 12 existing files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/434-line-ribbon-chart-family/434-CONTEXT.md
@.planning/phases/434-line-ribbon-chart-family/434-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 that this plan consumes -->

From src/Videra.SurfaceCharts.Core/LineChartData.cs (created in Plan 01):
```csharp
public sealed class LineChartData
{
    public LineChartData(IReadOnlyList<LineSeries> series, ScatterChartMetadata metadata)
    public ScatterChartMetadata Metadata { get; }
    public IReadOnlyList<LineSeries> Series { get; }
    public int SeriesCount { get; }
    public int PointCount { get; }
}
public sealed class LineSeries
{
    public LineSeries(IReadOnlyList<ScatterPoint> points, uint color, float width = 1.5f, string? label = null)
    public IReadOnlyList<ScatterPoint> Points { get; }
    public uint Color { get; }
    public float Width { get; }
    public string? Label { get; }
}
```

From src/Videra.SurfaceCharts.Core/RibbonChartData.cs (created in Plan 01):
```csharp
public sealed class RibbonChartData
{
    public RibbonChartData(IReadOnlyList<RibbonSeries> series, ScatterChartMetadata metadata)
    public ScatterChartMetadata Metadata { get; }
    public IReadOnlyList<RibbonSeries> Series { get; }
    public int SeriesCount { get; }
}
public sealed class RibbonSeries
{
    public RibbonSeries(IReadOnlyList<ScatterPoint> points, float radius, uint color, string? label = null)
    public IReadOnlyList<ScatterPoint> Points { get; }
    public float Radius { get; }
    public uint Color { get; }
    public string? Label { get; }
}
```

From src/Videra.SurfaceCharts.Core/Rendering/LineRenderer.cs (created in Plan 01):
```csharp
public static class LineRenderer
{
    public static LineRenderScene BuildScene(LineChartData data)
}
```

From src/Videra.SurfaceCharts.Core/Rendering/RibbonRenderer.cs (created in Plan 01):
```csharp
public static class RibbonRenderer
{
    public static RibbonRenderScene BuildScene(RibbonChartData data)
}
```

From src/Videra.SurfaceCharts.Core/Picking/LineProbeStrategy.cs (created in Plan 01):
```csharp
public sealed class LineProbeStrategy : ISeriesProbeStrategy
{
    public LineProbeStrategy(LineRenderScene scene, double snapRadius = 0.05)
}
```

From src/Videra.SurfaceCharts.Core/Picking/RibbonProbeStrategy.cs (created in Plan 01):
```csharp
public sealed class RibbonProbeStrategy : ISeriesProbeStrategy
{
    public RibbonProbeStrategy(RibbonRenderScene scene, double snapRadius = 0.1)
}
```

<!-- Existing Avalonia integration points to modify -->

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesKind.cs:
```csharp
public enum Plot3DSeriesKind { Surface, Waterfall, Scatter, Bar, Contour }
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs:
```csharp
internal Plot3DSeries(
    Plot3DSeriesKind kind, string? name,
    ISurfaceTileSource? surfaceSource, ScatterChartData? scatterData,
    BarChartData? barData, ContourChartData? contourData)
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs:
```csharp
public BarPlot3DSeries Bar(BarChartData data, string? name = null)
{
    ArgumentNullException.ThrowIfNull(data);
    return (BarPlot3DSeries)_plot.AddSeries(new BarPlot3DSeries(name, data));
}
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs:
```csharp
internal BarChartData? ActiveBarData =>
    ActiveSeries?.Kind == Plot3DSeriesKind.Bar
        ? Plot3DSeriesComposition.CreateBarData(GetVisibleSeries(Plot3DSeriesKind.Bar))
        : null;
internal Plot3DSeries? ActiveBarSeries { get { ... } }
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesComposition.cs:
```csharp
public static BarChartData? CreateBarData(IReadOnlyList<Plot3DSeries> series) { ... }
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs:
```csharp
var indicatorKind = series.Kind switch {
    Plot3DSeriesKind.Surface => LegendIndicatorKind.Swatch,
    Plot3DSeriesKind.Waterfall => LegendIndicatorKind.Swatch,
    Plot3DSeriesKind.Scatter => LegendIndicatorKind.Dot,
    _ => LegendIndicatorKind.Swatch,
};
var color = series.Kind switch {
    Plot3DSeriesKind.Surface => 0xFF4DA3FF,
    Plot3DSeriesKind.Waterfall => 0xFF4DA3FF,
    Plot3DSeriesKind.Scatter => 0xFFFF6B6B,
    _ => 0xFFCCCCCC,
};
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayState.cs:
```csharp
internal enum LegendIndicatorKind { Swatch, Dot, Line }
```

From src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs:
```csharp
public BarChartRenderingStatus BarRenderingStatus { get; private set; }
private BarChartRenderingStatus CreateBarRenderingStatus() { ... }
private void UpdateBarRenderingStatus() { ... }
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DDatasetEvidence.cs:
```csharp
return series.Kind switch {
    Plot3DSeriesKind.Surface or Plot3DSeriesKind.Waterfall => CreateSurfaceEvidence(...),
    Plot3DSeriesKind.Scatter => CreateScatterEvidence(...),
    Plot3DSeriesKind.Bar => CreateBarEvidence(...),
    Plot3DSeriesKind.Contour => CreateContourEvidence(...),
    _ => throw new ArgumentOutOfRangeException(...)
};
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DOutputEvidence.cs:
```csharp
internal static Plot3DRenderingEvidence FromBarStatus(BarChartRenderingStatus status) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Line/Ribbon enum values, extend Plot3DSeries base, create series subclasses and rendering status records</name>
  <files>
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesKind.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/BarPlot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/ScatterPlot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/ContourPlot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/LinePlot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/RibbonPlot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/LineChartRenderingStatus.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/RibbonChartRenderingStatus.cs
  </files>
  <read_first>
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesKind.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/BarPlot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/ScatterPlot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/ContourPlot3DSeries.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/BarChartRenderingStatus.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/ContourChartRenderingStatus.cs,
    src/Videra.SurfaceCharts.Core/LineChartData.cs,
    src/Videra.SurfaceCharts.Core/RibbonChartData.cs
  </read_first>
  <action>
**Plot3DSeriesKind.cs** (per D-01):
Add `Line` and `Ribbon` enum values after `Contour`:
```csharp
Contour,
Line,
Ribbon,
```

**Plot3DSeries.cs** (per D-01):
Extend the constructor to accept two new data slots:
```csharp
internal Plot3DSeries(
    Plot3DSeriesKind kind, string? name,
    ISurfaceTileSource? surfaceSource, ScatterChartData? scatterData,
    BarChartData? barData, ContourChartData? contourData,
    LineChartData? lineData, RibbonChartData? ribbonData)
```
Add two new properties:
```csharp
public LineChartData? LineData { get; }
public RibbonChartData? RibbonData { get; }
```
In the constructor body, assign `LineData = lineData;` and `RibbonData = ribbonData;`.
Add `ReplaceLineData` and `ReplaceRibbonData` private protected methods following the `ReplaceBarData` pattern:
```csharp
private protected void ReplaceLineData(LineChartData data)
{
    ArgumentNullException.ThrowIfNull(data);
    if (Kind != Plot3DSeriesKind.Line) throw new InvalidOperationException("Only line series can update line data.");
    if (ReferenceEquals(LineData, data)) return;
    LineData = data;
    NotifyChanged();
}
```
Same for `ReplaceRibbonData` with `Kind != Plot3DSeriesKind.Ribbon`.

**Existing series subclasses** -- update base constructor calls to pass the two new null slots:
- `BarPlot3DSeries`: change to `base(Plot3DSeriesKind.Bar, name, surfaceSource: null, scatterData: null, data, contourData: null, lineData: null, ribbonData: null)`
- `ScatterPlot3DSeries`: change to `base(Plot3DSeriesKind.Scatter, name, surfaceSource: null, data, barData: null, contourData: null, lineData: null, ribbonData: null)`
- `ContourPlot3DSeries`: change to `base(Plot3DSeriesKind.Contour, name, surfaceSource: null, scatterData: null, barData: null, data, lineData: null, ribbonData: null)`

**LinePlot3DSeries.cs** (new file, per LINE-01):
```csharp
using Videra.SurfaceCharts.Core;
namespace Videra.SurfaceCharts.Avalonia.Controls;

public sealed class LinePlot3DSeries : Plot3DSeries
{
    internal LinePlot3DSeries(string? name, LineChartData data)
        : base(Plot3DSeriesKind.Line, name,
            surfaceSource: null, scatterData: null, barData: null, contourData: null,
            lineData: data, ribbonData: null)
    { }

    public void SetColor(uint color)
    {
        var data = LineData ?? throw new InvalidOperationException("Line series requires line data.");
        if (data.Series.Count == 1 && data.Series[0].Color == color) return;
        var series = data.Series.ToArray();
        for (var i = 0; i < series.Length; i++)
        {
            var s = series[i];
            series[i] = new LineSeries(s.Points, color, s.Width, s.Label);
        }
        ReplaceLineData(new LineChartData(series, data.Metadata));
    }

    public void SetWidth(float width)
    {
        var data = LineData ?? throw new InvalidOperationException("Line series requires line data.");
        var series = data.Series.ToArray();
        for (var i = 0; i < series.Length; i++)
        {
            var s = series[i];
            series[i] = new LineSeries(s.Points, s.Color, width, s.Label);
        }
        ReplaceLineData(new LineChartData(series, data.Metadata));
    }
}
```

**RibbonPlot3DSeries.cs** (new file, per LINE-02):
```csharp
using Videra.SurfaceCharts.Core;
namespace Videra.SurfaceCharts.Avalonia.Controls;

public sealed class RibbonPlot3DSeries : Plot3DSeries
{
    internal RibbonPlot3DSeries(string? name, RibbonChartData data)
        : base(Plot3DSeriesKind.Ribbon, name,
            surfaceSource: null, scatterData: null, barData: null, contourData: null,
            lineData: null, ribbonData: data)
    { }

    public void SetColor(uint color)
    {
        var data = RibbonData ?? throw new InvalidOperationException("Ribbon series requires ribbon data.");
        var series = data.Series.ToArray();
        for (var i = 0; i < series.Length; i++)
        {
            var s = series[i];
            series[i] = new RibbonSeries(s.Points, s.Radius, color, s.Label);
        }
        ReplaceRibbonData(new RibbonChartData(series, data.Metadata));
    }

    public void SetRadius(float radius)
    {
        var data = RibbonData ?? throw new InvalidOperationException("Ribbon series requires ribbon data.");
        var series = data.Series.ToArray();
        for (var i = 0; i < series.Length; i++)
        {
            var s = series[i];
            series[i] = new RibbonSeries(s.Points, radius, s.Color, s.Label);
        }
        ReplaceRibbonData(new RibbonChartData(series, data.Metadata));
    }
}
```

**LineChartRenderingStatus.cs** (new file, per LINE-01):
Follow `BarChartRenderingStatus` pattern:
```csharp
using Avalonia;
using Videra.SurfaceCharts.Core;
using Videra.SurfaceCharts.Rendering;
namespace Videra.SurfaceCharts.Avalonia.Controls;

public sealed record LineChartRenderingStatus
{
    public bool HasSource { get; init; }
    public bool IsReady { get; init; }
    public SurfaceChartRenderBackendKind BackendKind { get; init; } = SurfaceChartRenderBackendKind.Software;
    public bool IsInteracting { get; init; }
    public int SeriesCount { get; init; }
    public int SegmentCount { get; init; }
    public Size ViewSize { get; init; }
}
```

**RibbonChartRenderingStatus.cs** (new file, per LINE-02):
Same pattern as LineChartRenderingStatus with `SegmentCount` field.
  </action>
  <verify>
    <automated>cd /mnt/f/CodeProjects/DotnetCore/Videra && dotnet build src/Videra.SurfaceCharts.Avalonia/Videra.SurfaceCharts.Avalonia.csproj --no-restore 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Line," src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesKind.cs
    - grep -q "Ribbon," src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesKind.cs
    - grep -q "LineChartData?" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs
    - grep -q "RibbonChartData?" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs
    - grep -q "ReplaceLineData" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs
    - grep -q "ReplaceRibbonData" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeries.cs
    - grep -q "sealed class LinePlot3DSeries" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/LinePlot3DSeries.cs
    - grep -q "sealed class RibbonPlot3DSeries" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/RibbonPlot3DSeries.cs
    - grep -q "lineData: null, ribbonData: null" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/BarPlot3DSeries.cs
    - grep -q "sealed record LineChartRenderingStatus" src/Videra.SurfaceCharts.Avalonia/Controls/LineChartRenderingStatus.cs
    - grep -q "sealed record RibbonChartRenderingStatus" src/Videra.SurfaceCharts.Avalonia/Controls/RibbonChartRenderingStatus.cs
    - dotnet build succeeds with no errors
  </acceptance_criteria>
  <done>Plot3DSeriesKind includes Line and Ribbon. Plot3DSeries constructor accepts LineChartData and RibbonChartData slots with ReplaceLineData/ReplaceRibbonData. Existing series subclasses pass null for new slots. LinePlot3DSeries and RibbonPlot3DSeries are sealed classes with SetColor/SetWidth/SetRadius mutable update methods. LineChartRenderingStatus and RibbonChartRenderingStatus records exist.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Plot3DAddApi, Plot3D, and Plot3DSeriesComposition for Line/Ribbon</name>
  <files>
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesComposition.cs
  </files>
  <read_first>
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesComposition.cs,
    src/Videra.SurfaceCharts.Core/LineChartData.cs,
    src/Videra.SurfaceCharts.Core/RibbonChartData.cs,
    src/Videra.SurfaceCharts.Core/ScatterPoint.cs,
    src/Videra.SurfaceCharts.Core/ScatterChartMetadata.cs,
    src/Videra.SurfaceCharts.Core/SurfaceAxisDescriptor.cs,
    src/Videra.SurfaceCharts.Core/SurfaceValueRange.cs
  </read_first>
  <action>
**Plot3DAddApi.cs** (per D-01, LINE-01, LINE-02):

Add Line convenience overload (after the Contour methods):
```csharp
/// <summary>
/// Adds a 3D line series from coordinate arrays.
/// </summary>
public LinePlot3DSeries Line(double[] xs, double[] ys, double[] zs, string? name = null)
{
    ArgumentNullException.ThrowIfNull(xs);
    ArgumentNullException.ThrowIfNull(ys);
    ArgumentNullException.ThrowIfNull(zs);
    if (xs.Length == 0) throw new ArgumentException("Line coordinate arrays must include at least one point.", nameof(xs));
    if (xs.Length != ys.Length || xs.Length != zs.Length)
        throw new ArgumentException("Line coordinate arrays must have matching lengths.", nameof(xs));

    var points = new ScatterPoint[xs.Length];
    var xMin = double.MaxValue; var xMax = double.MinValue;
    var yMin = double.MaxValue; var yMax = double.MinValue;
    var zMin = double.MaxValue; var zMax = double.MinValue;
    for (var i = 0; i < xs.Length; i++)
    {
        var pt = new ScatterPoint(xs[i], ys[i], zs[i]);
        points[i] = pt;
        xMin = Math.Min(xMin, pt.Horizontal); xMax = Math.Max(xMax, pt.Horizontal);
        yMin = Math.Min(yMin, pt.Value); yMax = Math.Max(yMax, pt.Value);
        zMin = Math.Min(zMin, pt.Depth); zMax = Math.Max(zMax, pt.Depth);
    }
    var metadata = new ScatterChartMetadata(
        new SurfaceAxisDescriptor("X", null, xMin, xMax),
        new SurfaceAxisDescriptor("Z", null, zMin, zMax),
        new SurfaceValueRange(yMin, yMax));
    var series = new LineSeries(points, color: 0xFF4DA3FFu, label: name);
    return Line(new LineChartData([series], metadata), name);
}

/// <summary>
/// Adds a 3D line series from a full line dataset.
/// </summary>
public LinePlot3DSeries Line(LineChartData data, string? name = null)
{
    ArgumentNullException.ThrowIfNull(data);
    return (LinePlot3DSeries)_plot.AddSeries(new LinePlot3DSeries(name, data));
}
```

Add Ribbon convenience overload:
```csharp
/// <summary>
/// Adds a 3D ribbon series from coordinate arrays with a tube radius.
/// </summary>
public RibbonPlot3DSeries Ribbon(double[] xs, double[] ys, double[] zs, float radius, string? name = null)
{
    ArgumentNullException.ThrowIfNull(xs);
    ArgumentNullException.ThrowIfNull(ys);
    ArgumentNullException.ThrowIfNull(zs);
    if (xs.Length == 0) throw new ArgumentException("Ribbon coordinate arrays must include at least one point.", nameof(xs));
    if (xs.Length != ys.Length || xs.Length != zs.Length)
        throw new ArgumentException("Ribbon coordinate arrays must have matching lengths.", nameof(xs));
    if (radius <= 0f) throw new ArgumentOutOfRangeException(nameof(radius), "Ribbon radius must be positive.");

    var points = new ScatterPoint[xs.Length];
    var xMin = double.MaxValue; var xMax = double.MinValue;
    var yMin = double.MaxValue; var yMax = double.MinValue;
    var zMin = double.MaxValue; var zMax = double.MinValue;
    for (var i = 0; i < xs.Length; i++)
    {
        var pt = new ScatterPoint(xs[i], ys[i], zs[i]);
        points[i] = pt;
        xMin = Math.Min(xMin, pt.Horizontal); xMax = Math.Max(xMax, pt.Horizontal);
        yMin = Math.Min(yMin, pt.Value); yMax = Math.Max(yMax, pt.Value);
        zMin = Math.Min(zMin, pt.Depth); zMax = Math.Max(zMax, pt.Depth);
    }
    var metadata = new ScatterChartMetadata(
        new SurfaceAxisDescriptor("X", null, xMin, xMax),
        new SurfaceAxisDescriptor("Z", null, zMin, zMax),
        new SurfaceValueRange(yMin, yMax));
    var series = new RibbonSeries(points, radius, color: 0xFF9B59B6u, label: name);
    return Ribbon(new RibbonChartData([series], metadata), name);
}

/// <summary>
/// Adds a 3D ribbon series from a full ribbon dataset.
/// </summary>
public RibbonPlot3DSeries Ribbon(RibbonChartData data, string? name = null)
{
    ArgumentNullException.ThrowIfNull(data);
    return (RibbonPlot3DSeries)_plot.AddSeries(new RibbonPlot3DSeries(name, data));
}
```

**Plot3D.cs** (per D-01):

Add after `ActiveBarSeries` property (around line 158):
```csharp
internal LineChartData? ActiveLineData =>
    ActiveSeries?.Kind == Plot3DSeriesKind.Line
        ? Plot3DSeriesComposition.CreateLineData(GetVisibleSeries(Plot3DSeriesKind.Line))
        : null;

internal Plot3DSeries? ActiveLineSeries
{
    get
    {
        var activeSeries = ActiveSeries;
        return activeSeries?.Kind == Plot3DSeriesKind.Line ? activeSeries : null;
    }
}

internal RibbonChartData? ActiveRibbonData =>
    ActiveSeries?.Kind == Plot3DSeriesKind.Ribbon
        ? Plot3DSeriesComposition.CreateRibbonData(GetVisibleSeries(Plot3DSeriesKind.Ribbon))
        : null;

internal Plot3DSeries? ActiveRibbonSeries
{
    get
    {
        var activeSeries = ActiveSeries;
        return activeSeries?.Kind == Plot3DSeriesKind.Ribbon ? activeSeries : null;
    }
}
```

**Plot3DSeriesComposition.cs** (per D-01):

Add after `CreateBarData` method:
```csharp
public static LineChartData? CreateLineData(IReadOnlyList<Plot3DSeries> series)
{
    var datasets = series
        .Select(static item => item.LineData)
        .OfType<LineChartData>()
        .ToArray();

    if (datasets.Length == 0) return null;
    if (datasets.Length == 1) return datasets[0];

    // Merge: concatenate all series from all datasets, union metadata
    var allSeries = datasets.SelectMany(static data => data.Series).ToArray();
    var metadata = CreateScatterMetadata(datasets.Select(static d => d.Metadata).ToArray());
    return new LineChartData(allSeries, metadata);
}

public static RibbonChartData? CreateRibbonData(IReadOnlyList<Plot3DSeries> series)
{
    var datasets = series
        .Select(static item => item.RibbonData)
        .OfType<RibbonChartData>()
        .ToArray();

    if (datasets.Length == 0) return null;
    if (datasets.Length == 1) return datasets[0];

    var allSeries = datasets.SelectMany(static data => data.Series).ToArray();
    var metadata = CreateScatterMetadata(datasets.Select(static d => d.Metadata).ToArray());
    return new RibbonChartData(allSeries, metadata);
}
```

Note: `CreateScatterMetadata` already exists in `Plot3DSeriesComposition.cs` (private static method, lines 75-82). Use it directly for metadata union. If the existing method takes `IReadOnlyList<ScatterChartData>`, create a small overload that takes `IReadOnlyList<ScatterChartMetadata>`:
```csharp
private static ScatterChartMetadata CreateScatterMetadata(IReadOnlyList<ScatterChartMetadata> metadatas)
{
    var first = metadatas[0];
    return new ScatterChartMetadata(
        CreateAxisUnion(first.HorizontalAxis, metadatas.Select(static m => m.HorizontalAxis)),
        CreateAxisUnion(first.DepthAxis, metadatas.Select(static m => m.DepthAxis)),
        CreateValueRangeUnion(metadatas.Select(static m => m.ValueRange)));
}
```
  </action>
  <verify>
    <automated>cd /mnt/f/CodeProjects/DotnetCore/Videra && dotnet build src/Videra.SurfaceCharts.Avalonia/Videra.SurfaceCharts.Avalonia.csproj --no-restore 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "public LinePlot3DSeries Line(double\[\] xs" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs
    - grep -q "public LinePlot3DSeries Line(LineChartData data" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs
    - grep -q "public RibbonPlot3DSeries Ribbon(double\[\] xs" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs
    - grep -q "public RibbonPlot3DSeries Ribbon(RibbonChartData data" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs
    - grep -q "ActiveLineData" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs
    - grep -q "ActiveRibbonData" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs
    - grep -q "ActiveLineSeries" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs
    - grep -q "ActiveRibbonSeries" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3D.cs
    - grep -q "CreateLineData" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesComposition.cs
    - grep -q "CreateRibbonData" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DSeriesComposition.cs
    - grep -q "new LinePlot3DSeries" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs
    - grep -q "new RibbonPlot3DSeries" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DAddApi.cs
    - dotnet build succeeds with no errors
  </acceptance_criteria>
  <done>Plot3DAddApi has Line(xs,ys,zs,name), Line(data,name), Ribbon(xs,ys,zs,radius,name), Ribbon(data,name) methods. Plot3D exposes ActiveLineData/ActiveRibbonData/ActiveLineSeries/ActiveRibbonSeries. Plot3DSeriesComposition has CreateLineData and CreateRibbonData merge methods. Ribbon convenience overload validates positive radius. Both convenience overloads create ScatterChartMetadata from coordinate bounds.</done>
</task>

<task type="auto">
  <name>Task 3: Wire legend, rendering status, dataset evidence, and output evidence for Line/Ribbon</name>
  <files>
    src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DDatasetEvidence.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DOutputEvidence.cs
  </files>
  <read_first>
    src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayState.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DDatasetEvidence.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DOutputEvidence.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/BarChartRenderingStatus.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/LineChartRenderingStatus.cs,
    src/Videra.SurfaceCharts.Avalonia/Controls/RibbonChartRenderingStatus.cs
  </read_first>
  <action>
**SurfaceLegendOverlayPresenter.cs** (per LINE-04):

In `CreateLegendEntry`, update the `indicatorKind` switch to add explicit Line and Ribbon cases before the default `_`:
```csharp
Plot3DSeriesKind.Line => LegendIndicatorKind.Line,
Plot3DSeriesKind.Ribbon => LegendIndicatorKind.Swatch,
```

Update the `color` switch to add:
```csharp
Plot3DSeriesKind.Line => 0xFF4DA3FF,    // Blue
Plot3DSeriesKind.Ribbon => 0xFF9B59B6,  // Purple
```

**VideraChartView.Core.cs** (per LINE-01, LINE-02):

Add properties (after `ContourRenderingStatus`):
```csharp
public LineChartRenderingStatus LineRenderingStatus { get; private set; }
public RibbonChartRenderingStatus RibbonRenderingStatus { get; private set; }
```

In the constructor (after `BarRenderingStatus = CreateBarRenderingStatus();`):
```csharp
LineRenderingStatus = CreateLineRenderingStatus();
RibbonRenderingStatus = CreateRibbonRenderingStatus();
```

In `Refresh()` method (after `UpdateBarRenderingStatus();`):
```csharp
UpdateLineRenderingStatus();
UpdateRibbonRenderingStatus();
```

Add methods (after `CreateContourRenderingStatus`):
```csharp
private void UpdateLineRenderingStatus()
{
    var nextStatus = CreateLineRenderingStatus();
    if (LineRenderingStatus == nextStatus) return;
    LineRenderingStatus = nextStatus;
}

private LineChartRenderingStatus CreateLineRenderingStatus()
{
    var lineData = Plot.ActiveLineData;
    var hasLineData = lineData is not null;
    return new LineChartRenderingStatus
    {
        HasSource = hasLineData,
        IsReady = hasLineData,
        BackendKind = SurfaceChartRenderBackendKind.Software,
        IsInteracting = _interactionController.HasActiveGesture,
        SeriesCount = lineData?.SeriesCount ?? 0,
        SegmentCount = lineData is not null ? Math.Max(0, lineData.PointCount - lineData.SeriesCount) : 0,
        ViewSize = _runtime.ViewSize,
    };
}

private void UpdateRibbonRenderingStatus()
{
    var nextStatus = CreateRibbonRenderingStatus();
    if (RibbonRenderingStatus == nextStatus) return;
    RibbonRenderingStatus = nextStatus;
}

private RibbonChartRenderingStatus CreateRibbonRenderingStatus()
{
    var ribbonData = Plot.ActiveRibbonData;
    var hasRibbonData = ribbonData is not null;
    return new RibbonChartRenderingStatus
    {
        HasSource = hasRibbonData,
        IsReady = hasRibbonData,
        BackendKind = SurfaceChartRenderBackendKind.Software,
        IsInteracting = _interactionController.HasActiveGesture,
        SeriesCount = ribbonData?.SeriesCount ?? 0,
        SegmentCount = ribbonData is not null ? Math.Max(0, ribbonData.Series.Sum(s => Math.Max(0, s.Points.Count - 1))) : 0,
        ViewSize = _runtime.ViewSize,
    };
}
```

**Plot3DDatasetEvidence.cs** (per LINE-04):

In the `Create` method switch (line ~279), add cases before the default `_`:
```csharp
Plot3DSeriesKind.Line => CreateLineEvidence(index, isActive, series),
Plot3DSeriesKind.Ribbon => CreateRibbonEvidence(index, isActive, series),
```

Add helper methods (after `CreateContourEvidence`):
```csharp
private static Plot3DSeriesDatasetEvidence CreateLineEvidence(int index, bool isActive, Plot3DSeries series)
{
    var data = series.LineData
        ?? throw new InvalidOperationException("Line series require line data.");
    return new Plot3DSeriesDatasetEvidence(
        index, isActive, CreateIdentity(index, series), series.Name, series.Kind,
        width: 0, height: 0, sampleCount: 0,
        seriesCount: data.SeriesCount,
        pointCount: data.PointCount,
        columnarSeriesCount: 0, columnarPointCount: 0, pickablePointCount: 0,
        streamingAppendBatchCount: 0, streamingReplaceBatchCount: 0,
        streamingDroppedPointCount: 0, lastStreamingDroppedPointCount: 0,
        configuredFifoCapacity: 0,
        horizontalAxis: SurfaceAxisDatasetEvidence.Create(data.Metadata.HorizontalAxis),
        verticalAxis: SurfaceAxisDatasetEvidence.Create(data.Metadata.ValueRange),
        depthAxis: SurfaceAxisDatasetEvidence.Create(data.Metadata.DepthAxis),
        valueRange: SurfaceValueRangeDatasetEvidence.Create(data.Metadata.ValueRange),
        samplingProfile: $"line:{data.SeriesCount}s:{data.PointCount}p",
        categoryLabels: [], []);
}

private static Plot3DSeriesDatasetEvidence CreateRibbonEvidence(int index, bool isActive, Plot3DSeries series)
{
    var data = series.RibbonData
        ?? throw new InvalidOperationException("Ribbon series require ribbon data.");
    return new Plot3DSeriesDatasetEvidence(
        index, isActive, CreateIdentity(index, series), series.Name, series.Kind,
        width: 0, height: 0, sampleCount: 0,
        seriesCount: data.SeriesCount,
        pointCount: data.Series.Sum(s => s.Points.Count),
        columnarSeriesCount: 0, columnarPointCount: 0, pickablePointCount: 0,
        streamingAppendBatchCount: 0, streamingReplaceBatchCount: 0,
        streamingDroppedPointCount: 0, lastStreamingDroppedPointCount: 0,
        configuredFifoCapacity: 0,
        horizontalAxis: SurfaceAxisDatasetEvidence.Create(data.Metadata.HorizontalAxis),
        verticalAxis: SurfaceAxisDatasetEvidence.Create(data.Metadata.ValueRange),
        depthAxis: SurfaceAxisDatasetEvidence.Create(data.Metadata.DepthAxis),
        valueRange: SurfaceValueRangeDatasetEvidence.Create(data.Metadata.ValueRange),
        samplingProfile: $"ribbon:{data.SeriesCount}s",
        categoryLabels: [], []);
}
```

**Plot3DOutputEvidence.cs** (per LINE-04):

Add after `FromContourStatus`:
```csharp
internal static Plot3DRenderingEvidence FromLineStatus(LineChartRenderingStatus status)
{
    ArgumentNullException.ThrowIfNull(status);
    return new Plot3DRenderingEvidence(
        "line-rendering-status",
        status.BackendKind,
        status.IsReady,
        isFallback: false,
        fallbackReason: null,
        viewWidth: status.ViewSize.Width,
        viewHeight: status.ViewSize.Height);
}

internal static Plot3DRenderingEvidence FromRibbonStatus(RibbonChartRenderingStatus status)
{
    ArgumentNullException.ThrowIfNull(status);
    return new Plot3DRenderingEvidence(
        "ribbon-rendering-status",
        status.BackendKind,
        status.IsReady,
        isFallback: false,
        fallbackReason: null,
        viewWidth: status.ViewSize.Width,
        viewHeight: status.ViewSize.Height);
}
```
  </action>
  <verify>
    <automated>cd /mnt/f/CodeProjects/DotnetCore/Videra && dotnet build src/Videra.SurfaceCharts.Avalonia/Videra.SurfaceCharts.Avalonia.csproj --no-restore 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Plot3DSeriesKind.Line => LegendIndicatorKind.Line" src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs
    - grep -q "Plot3DSeriesKind.Ribbon => LegendIndicatorKind.Swatch" src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs
    - grep -q "Plot3DSeriesKind.Line => 0xFF4DA3FF" src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs
    - grep -q "Plot3DSeriesKind.Ribbon => 0xFF9B59B6" src/Videra.SurfaceCharts.Avalonia/Controls/Overlay/SurfaceLegendOverlayPresenter.cs
    - grep -q "LineRenderingStatus" src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs
    - grep -q "RibbonRenderingStatus" src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs
    - grep -q "CreateLineRenderingStatus" src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs
    - grep -q "CreateRibbonRenderingStatus" src/Videra.SurfaceCharts.Avalonia/Controls/VideraChartView.Core.cs
    - grep -q "Plot3DSeriesKind.Line => CreateLineEvidence" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DDatasetEvidence.cs
    - grep -q "Plot3DSeriesKind.Ribbon => CreateRibbonEvidence" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DDatasetEvidence.cs
    - grep -q "FromLineStatus" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DOutputEvidence.cs
    - grep -q "FromRibbonStatus" src/Videra.SurfaceCharts.Avalonia/Controls/Plot/Plot3DOutputEvidence.cs
    - dotnet build succeeds with no errors
  </acceptance_criteria>
  <done>Legend overlay handles Line (LegendIndicatorKind.Line, 0xFF4DA3FF blue) and Ribbon (LegendIndicatorKind.Swatch, 0xFF9B59B6 purple). VideraChartView exposes LineRenderingStatus and RibbonRenderingStatus with update methods. Plot3DDatasetEvidence handles Line and Ribbon series kinds. Plot3DOutputEvidence has FromLineStatus and FromRibbonStatus methods. Full solution builds without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> data model | Coordinate arrays from user code pass through Plot3DAddApi into LineChartData/RibbonChartData |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-434-01 | Tampering | Plot3DAddApi.Line/Ribbon | mitigate | Constructor validation on data models (NaN check on ScatterPoint, positive radius, non-empty arrays) prevents malformed data from entering the pipeline |
| T-434-02 | Denial of Service | LineRenderer/RibbonRenderer | accept | No PII, rendering is bounded by point count which is user-controlled in a desktop app context |
</threat_model>

<verification>
- Full solution `dotnet build` succeeds
- `Plot.Add.Line(xs, ys, zs, "test")` compiles and returns `LinePlot3DSeries`
- `Plot.Add.Ribbon(xs, ys, zs, 0.5f, "test")` compiles and returns `RibbonPlot3DSeries`
- Line and Ribbon series appear in legend with correct indicator kind and color
- LineRenderingStatus and RibbonRenderingStatus are populated on VideraChartView
- Plot3DDatasetEvidence and Plot3DOutputEvidence handle Line and Ribbon kinds without throwing
</verification>

<success_criteria>
- All 4 requirement IDs (LINE-01, LINE-02, LINE-03, LINE-04) are covered across both plans
- `Plot.Add.Line(xs, ys, zs, label)` creates a 3D polyline series with configurable color and width
- `Plot.Add.Ribbon(xs, ys, zs, radius, label)` creates a tube/pipe geometry series
- Per-segment coloring works via LineSeries.Color and RibbonSeries.Color
- Line and Ribbon participate in probe (LineProbeStrategy, RibbonProbeStrategy), legend (LegendIndicatorKind.Line, Swatch), and diagnostics (rendering status, evidence)
- Full solution builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/434-line-ribbon-chart-family/434-02-SUMMARY.md`
</output>
