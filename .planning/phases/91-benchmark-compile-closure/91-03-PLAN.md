# Plan 91-03: Stable Regression Evidence Alignment

## Objective

Keep the benchmark compile repair aligned with stable support/test evidence rather than brittle source-text assertions.

## Tasks

1. Review whether any temporary repository guard duplicates compile-backed evidence that already exists in `Videra.slnx` and `scripts/verify.ps1`.
2. Remove low-signal guard code that would create maintenance noise without strengthening the real CI path.
3. Re-run the touched repository tests and benchmark build after the cleanup.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter RepositoryArchitectureTests`
- `dotnet build benchmarks/Videra.Viewer.Benchmarks/Videra.Viewer.Benchmarks.csproj -c Release`
