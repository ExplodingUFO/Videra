# Plan 91-01: Benchmark Static-Service Compile Closure

## Objective

Close the current benchmark compile break by preventing viewer benchmark sources from instantiating internal static runtime services.

## Tasks

1. Capture the failing compile baseline from the benchmark/solution path that currently blocks `verify`.
2. Repair the benchmark sources with the smallest compile-correct change that preserves the benchmark intent.
3. Re-run targeted repository tests and the benchmark project build to confirm the compile break is removed without collateral drift.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter RepositoryArchitectureTests`
- `dotnet build benchmarks/Videra.Viewer.Benchmarks/Videra.Viewer.Benchmarks.csproj -c Release`
- `dotnet test Videra.slnx --configuration Release -v minimal`
