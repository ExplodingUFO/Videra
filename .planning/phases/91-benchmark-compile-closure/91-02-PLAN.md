# Plan 91-02: Shared Verify Prelude Recovery

## Objective

Prove the repo's shared verify prelude reaches test execution again instead of failing early on benchmark compile drift.

## Tasks

1. Re-run the benchmark project build to confirm the direct compile break is gone.
2. Re-run `dotnet test Videra.slnx --configuration Release -v minimal` to exercise the same solution-level prelude that `scripts/verify.ps1` depends on.
3. Capture evidence that repo test assemblies execute normally after the benchmark-source repair.

## Verification

- `dotnet build benchmarks/Videra.Viewer.Benchmarks/Videra.Viewer.Benchmarks.csproj -c Release`
- `dotnet test Videra.slnx --configuration Release -v minimal`
