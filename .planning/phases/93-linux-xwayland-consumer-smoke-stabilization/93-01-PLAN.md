# Plan 93-01: XWayland Workflow Boundary Isolation

## Objective

Prove where the Linux `xwayland` consumer smoke path actually begins and capture the session-environment mismatch that made past failures hard to interpret.

## Tasks

1. Compare `consumer-smoke.yml` / `publish-public.yml` against the working XWayland session contract already used by native validation.
2. Inspect the historical `gh-run-24610316740` smoke artifact set to identify the last emitted trace lines and resolved display server.
3. Add a failing repository contract test that requires Linux `xwayland` consumer smoke workflows to print and preserve the Wayland-session environment contract.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --filter FullyQualifiedName~LinuxXWaylandConsumerSmokeWorkflow_ShouldCaptureWaylandSessionEnvironment`
