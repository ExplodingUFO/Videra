# Plan 93-03: Real XWayland Runtime Revalidation

## Objective

Re-run the repaired Linux `xwayland` consumer smoke path on a real Linux `XWayland` environment and confirm the app completes `LoadModelAsync -> FrameAll -> snapshot/bundle export`.

## Tasks

1. Execute the repaired workflow command on a Linux host or CI runner that exposes both `DISPLAY` and `WAYLAND_DISPLAY`.
2. Confirm the resulting `consumer-smoke-result.json` reports `Succeeded=true`, `IsReady=true`, and an `XWayland`-compatible display diagnostic.
3. Confirm `inspection-snapshot.png`, `inspection-bundle/inspection-state.json`, and `inspection-bundle/asset-manifest.json` are present.

## Verification

- `xwfb-run -- bash -lc 'env | sort | grep -E "^(DISPLAY|WAYLAND_DISPLAY|XDG_RUNTIME_DIR|XDG_SESSION_TYPE)=" || true; if [[ -z "${DISPLAY:-}" ]]; then echo "DISPLAY is not set. Start an XWayland session or run this command under xwfb-run." >&2; exit 2; fi; if [[ -z "${WAYLAND_DISPLAY:-}" ]]; then echo "WAYLAND_DISPLAY is not set. Start a Wayland session with XWayland available before running this target." >&2; exit 2; fi; export XDG_SESSION_TYPE="${XDG_SESSION_TYPE:-wayland}"; pwsh -File ./scripts/Invoke-ConsumerSmoke.ps1 -Configuration Release -OutputRoot artifacts/consumer-smoke/linux-xwayland'`
