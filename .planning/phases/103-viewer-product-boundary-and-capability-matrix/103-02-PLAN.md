# Plan 103-02: Capability Matrix

## Objective

Publish one explicit capability matrix that separates the shipped `1.0` viewer/runtime surface from deferred `2.0` engine-style features.

## Tasks

1. Add a dedicated documentation page for the `1.0` vs deferred capability matrix.
2. Link that page from the README and docs index as the canonical capability truth.
3. Make the matrix concrete enough that later phases can map package/code work back to it.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~PublicDocs_ShouldExposePackageMatrixSupportMatrixAndReleasePolicy|FullyQualifiedName~ProductBoundaryDocs_ShouldPublishCapabilityAndLayerMatrices"`
