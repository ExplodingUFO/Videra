---
phase: 433-promote-bar-contour-to-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eng/public-api-contract.json
autonomous: true
requirements:
  - PROMO-01
  - PROMO-02
  - PROMO-03

must_haves:
  truths:
    - "BarPlot3DSeries and ContourPlot3DSeries appear in eng/public-api-contract.json under Videra.SurfaceCharts.Avalonia publicTypes"
    - "PublicApiContractRepositoryTests pass (contract matches source)"
    - "Existing Bar and Contour tests pass without modification"
    - "Package validation scripts confirm Bar+Contour are in the public surface"
  artifacts:
    - path: "eng/public-api-contract.json"
      provides: "Public API contract with Bar and Contour series types"
      contains: "BarPlot3DSeries"
  key_links:
    - from: "eng/public-api-contract.json"
      to: "tests/Videra.Core.Tests/Repository/PublicApiContractRepositoryTests.cs"
      via: "PublicApiContract_ShouldMatchCurrentTopLevelPublicTypes"
      pattern: "BarPlot3DSeries"
---

<objective>
Promote Bar and Contour chart families from proof-path to the public package API
contract by adding BarPlot3DSeries and ContourPlot3DSeries to the public type
surface in eng/public-api-contract.json.

Purpose: These types are already public classes in source code, already have
tests, demo scenarios, and cookbook recipes, and are already wired into the
rendering pipeline, probe infrastructure, and legend overlay. The only missing
piece is their registration in the public API contract file that controls what
NuGet package consumers can access.

Output: Updated eng/public-api-contract.json with BarPlot3DSeries and
ContourPlot3DSeries in the Videra.SurfaceCharts.Avalonia publicTypes array.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/433-promote-bar-contour-to-production/433-CONTEXT.md
@.planning/phases/432-chart-type-inventory-and-api-design/432-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From eng/public-api-contract.json:
```json
{
  "id": "Videra.SurfaceCharts.Avalonia",
  "publicTypes": [
    "Videra.SurfaceCharts.Avalonia.Controls.BarChartRenderingStatus",
    "Videra.SurfaceCharts.Avalonia.Controls.ContourChartRenderingStatus",
    ...
    "Videra.SurfaceCharts.Avalonia.Controls.IPlottable3D",
    ...
  ]
}
```

The BarPlot3DSeries and ContourPlot3DSeries types are already public sealed
classes in the source but are missing from this contract file.

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/BarPlot3DSeries.cs:
```csharp
public sealed class BarPlot3DSeries : Plot3DSeries
{
    internal BarPlot3DSeries(string? name, BarChartData data) ...
    public void SetSeriesColor(int seriesIndex, uint color) { ... }
}
```

From src/Videra.SurfaceCharts.Avalonia/Controls/Plot/ContourPlot3DSeries.cs:
```csharp
public sealed class ContourPlot3DSeries : Plot3DSeries
{
    internal ContourPlot3DSeries(string? name, ContourChartData data) ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BarPlot3DSeries and ContourPlot3DSeries to public API contract</name>
  <files>eng/public-api-contract.json</files>
  <read_first>eng/public-api-contract.json</read_first>
  <action>
Edit eng/public-api-contract.json to add two entries to the
Videra.SurfaceCharts.Avalonia package's publicTypes array:

1. Insert "Videra.SurfaceCharts.Avalonia.Controls.BarPlot3DSeries" after
   "Videra.SurfaceCharts.Avalonia.Controls.BarChartRenderingStatus" (line 377).
   The array must remain in ascending ordinal sort order.

2. Insert "Videra.SurfaceCharts.Avalonia.Controls.ContourPlot3DSeries" after
   "Videra.SurfaceCharts.Avalonia.Controls.ContourChartRenderingStatus" (line
   378). The array must remain in ascending ordinal sort order.

Do NOT change any other entries. Do NOT modify the Core package's publicTypes
(Bar and Contour Core types are already listed there). Do NOT add any other
types.
  </action>
  <verify>
    <automated>cd /mnt/f/CodeProjects/DotnetCore/Videra && dotnet test tests/Videra.Core.Tests --filter "PublicApiContract" --no-restore -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"Videra.SurfaceCharts.Avalonia.Controls.BarPlot3DSeries"' eng/public-api-contract.json
    - grep -q '"Videra.SurfaceCharts.Avalonia.Controls.ContourPlot3DSeries"' eng/public-api-contract.json
    - The publicTypes array for Videra.SurfaceCharts.Avalonia is still in ascending ordinal sort order
    - PublicApiContractRepositoryTests pass (contract matches actual source)
  </acceptance_criteria>
  <done>
eng/public-api-contract.json contains BarPlot3DSeries and ContourPlot3DSeries
in the Videra.SurfaceCharts.Avalonia publicTypes array, sorted correctly, and
PublicApiContractRepositoryTests pass confirming the contract matches source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify existing Bar and Contour tests pass unchanged</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the existing Bar and Contour test suites to confirm they pass without
modification. These tests already exist and must continue to pass after the
public API contract promotion:

1. Run Core Bar renderer tests:
   dotnet test tests/Videra.SurfaceCharts.Core.Tests --filter "BarRenderer" --no-restore

2. Run Core Contour tests:
   dotnet test tests/Videra.SurfaceCharts.Core.Tests --filter "Contour" --no-restore

3. Run Avalonia integration tests for Plot API (Bar/Contour):
   dotnet test tests/Videra.SurfaceCharts.Avalonia.IntegrationTests --filter "PlotApi" --no-restore

4. Run cookbook Bar+Contour snapshot recipe tests:
   dotnet test tests/Videra.Core.Tests --filter "SurfaceChartsCookbookBarContourSnapshotRecipeTests" --no-restore

5. Run CI truth tests:
   dotnet test tests/Videra.Core.Tests --filter "SurfaceChartsCiTruth" --no-restore

No code changes are expected. This task is verification-only.
  </action>
  <verify>
    <automated>cd /mnt/f/CodeProjects/DotnetCore/Videra && dotnet test tests/Videra.SurfaceCharts.Core.Tests --filter "BarRenderer|Contour" --no-restore -v minimal && dotnet test tests/Videra.SurfaceCharts.Avalonia.IntegrationTests --filter "PlotApi" --no-restore -v minimal && dotnet test tests/Videra.Core.Tests --filter "SurfaceChartsCookbookBarContourSnapshotRecipeTests|SurfaceChartsCiTruth|PublicApiContract" --no-restore -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - All Bar renderer tests pass
    - All Contour tests pass
    - Plot API integration tests pass (Bar and Contour)
    - Cookbook Bar+Contour snapshot recipe tests pass
    - CI truth tests pass
    - No test files were modified
  </acceptance_criteria>
  <done>
All existing Bar and Contour tests, demo scenarios, and cookbook recipes pass
without modification after the public API contract promotion. Zero test files
were changed.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `eng/public-api-contract.json` contains both BarPlot3DSeries and
   ContourPlot3DSeries in the Videra.SurfaceCharts.Avalonia publicTypes.
2. `PublicApiContractRepositoryTests.PublicApiContract_ShouldMatchCurrentTopLevelPublicTypes`
   passes (contract matches actual source).
3. All existing Bar/Contour tests pass unchanged.
4. CI truth tests pass.
5. Package validation scripts (Invoke-ReleaseDryRun.ps1,
   New-PublicPublishEvidence.ps1) reference the contract and will pick up the
   new types automatically.
</verification>

<success_criteria>
- BarPlot3DSeries appears in eng/public-api-contract.json under Videra.SurfaceCharts.Avalonia
- ContourPlot3DSeries appears in eng/public-api-contract.json under Videra.SurfaceCharts.Avalonia
- PublicApiContractRepositoryTests pass (zero drift between contract and source)
- All existing Bar/Contour tests pass without modification (PROMO-03)
- No other files were modified
</success_criteria>

<output>
After completion, create `.planning/phases/433-promote-bar-contour-to-production/433-01-SUMMARY.md`
</output>
