# Plan 92-03: Quality-Gate Evidence Revalidation

## Objective

Revalidate the broader warnings-as-errors evidence path that CI uses around the SurfaceCharts-adjacent quality gate.

## Tasks

1. Run packaged consumer smoke in build-only warnings-as-errors mode.
2. Re-run the additional curated quality-gate builds that accompany the Core tests path.
3. Record the resulting green evidence set for milestone traceability.

## Verification

- `pwsh -File ./scripts/Invoke-ConsumerSmoke.ps1 -Configuration Release -OutputRoot artifacts/consumer-smoke-quality -BuildOnly -TreatWarningsAsErrors`
- `dotnet build tests/Videra.Core.IntegrationTests/Videra.Core.IntegrationTests.csproj -c Release -p:TreatWarningsAsErrors=true`
- `dotnet build samples/Videra.MinimalSample/Videra.MinimalSample.csproj -c Release -p:TreatWarningsAsErrors=true`
