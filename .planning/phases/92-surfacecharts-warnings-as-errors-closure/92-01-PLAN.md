# Plan 92-01: Strict-Build Baseline Capture

## Objective

Determine whether the reported SurfaceCharts warnings-as-errors failures still reproduce on the current workspace.

## Tasks

1. Run the curated Core strict-build evidence path.
2. Run direct strict builds for `Videra.SurfaceCharts.Processing` and `Videra.SurfaceCharts.Avalonia`.
3. Only prepare code changes if one of those commands reproduces a real analyzer failure.

## Verification

- `dotnet build tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release -p:TreatWarningsAsErrors=true`
- `dotnet build src/Videra.SurfaceCharts.Processing/Videra.SurfaceCharts.Processing.csproj -c Release -p:TreatWarningsAsErrors=true`
- `dotnet build src/Videra.SurfaceCharts.Avalonia/Videra.SurfaceCharts.Avalonia.csproj -c Release -p:TreatWarningsAsErrors=true`
