# Plan 105-01: Publish the Canonical Composition Story

## Objective

Write one focused hosting-boundary document that explains the canonical viewer composition now that import packages are explicit.

## Tasks

1. Add a dedicated hosting-boundary doc that names the `Core` / `Import` / `Backend` / `UI adapter` / `Charts` layers and the internal seam owners.
2. Link that doc from the repository docs index and the most relevant entry docs.
3. Keep the explanation concrete and tied to real types/files instead of abstract framework language.

## Verification

- `dotnet test tests/Videra.Core.Tests/Videra.Core.Tests.csproj -c Release --no-build -m:1 --filter "FullyQualifiedName~HostingBoundaryDocs"`
